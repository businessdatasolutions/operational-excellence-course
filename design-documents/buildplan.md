# Operational Excellence Gate-systeem: Build Plan

> **Voor uitvoerende agents:** dit document is de **enige bron van waarheid voor voortgang**. Vink een taakvakje (`- [ ]` → `- [x]`) pas af nadat de bijbehorende test gate daadwerkelijk is uitgevoerd en geslaagd, nooit vooruitlopend. Twee hookify-rules in `.claude/` herinneren je hieraan bij elke `git commit`/`git push` en bij elke stop: `hookify.require-tests-before-commit.local.md` en `hookify.check-test-gate-on-stop.local.md`.
>
> **Volgorde-regel:** een main task mag pas beginnen nadat de **vorige main task in dezelfde afhankelijkheidsketen** succesvol gecommit én gepusht is (zie "Parallelisatie-overzicht" hieronder voor welke tasks wél gelijktijdig mogen). Binnen één main task: subtasks in volgorde, geen enkele commit vóór de test gate van die main task geslaagd is.

**Doel:** de architectuur uit `tdd-operational-excellence.html` bouwen tot een werkend gate-systeem: Socratische AI-gate, peer-review-gate, review-ring, wiki-kwaliteitspunten, instructor-dashboard, admin-onderdeel en studentdossier, precies zoals gespecificeerd in de LRD (`lrd-operational-excellence.html`, FR-01–FR-24) en het TDD.

**Architectuur (samenvatting, zie TDD Deel 2 voor het volledige plaatje):** vier lagen: wiki/content (ongewijzigd, `wiki-template`), data (`small-medium-data-lake`-patroon, per-team/ring/klas lakehouses), agentic services (Google ADK 2.0, `adk2-blueprint`-patroon), frontend (React + Vite, net-new). Alle nieuwe code voor de laatste drie lagen leeft in een **nieuwe, aparte repository** (voorlopige naam `oe-gate-system`) naast (niet ín) deze documentatierepository. De wiki blijft in haar eigen `wiki-template`-fork, ongewijzigd (NFR-02).

**Tech stack:** Python 3.11+ (data-laag, agents, `google-adk[a2a,db,eval]~=2.0`, DuckDB/DuckLake, pytest), TypeScript/React + Vite (frontend), Quartz v4/Node (wiki, ongewijzigd), Cloud Run + managed Postgres (deployment, `europe-west4`).

**Scope-uitbreiding (Main Tasks 12–20):** na afronding van Tasks 0–11 is de scope verbreed van "gate-systeem-prototype" naar een volledige hybride course website (onboarding → eindassessment, docentjourney, kwaliteitsmanagement via surveys, wiki-gebruiksanalyse, didactische AI-adviseur). Zie de brainstorm-beslissingen en architectuurkeuzes vastgelegd in `/Users/witoldtenhove/.claude/plans/can-we-start-building-compiled-narwhal.md`. Tasks 12–13 breiden eerst de LRD/TDD uit (pedagogiek/architectuur, geen code); Tasks 14–20 bouwen daarna de code. Dezelfde globale randvoorwaarden hieronder blijven onverkort gelden.

## Globale randvoorwaarden (gelden voor élke main task)

Deze eisen zijn projectbreed en worden niet in elke task herhaald; een subtask die ze schendt is per definitie niet af:

- **Privacy (LRD Deel 1.3, TDD Deel 9):** uitsluitend `first_name` + `team_id` + `klas_id` + `cohort` als identifiers. Nooit een studentnummer, e-mailadres, achternaam of ander HAN-systeemveld in enig datamodel, log of API-response.
- **Never-invents-garanties (TDD Deel 4.4, 5.1, 5.3, 5.7, 5.8):** elke agent-tool die cijfers, duplicaat-oordelen, citatie-geldigheid of aandachtspunten produceert, doet dat op basis van deterministische data, nooit een ongestaafde LLM-claim. Waar het TDD een `source_ref`- of vergelijkbaar traceerbaarheidsveld eist, is dat een schema-eis, geen instructie die genegeerd kan worden.
- **OKF-conformantie (FR-11, NFR-07):** elk nieuw concept-type heeft verplicht `type` in de frontmatter en volgt `design-documents/SPEC.md` §9.
- **KISS (LRD Deel 5.1):** geen functionaliteit bouwen die niet direct aan een FR/NFR uit de LRD te koppelen is. Twijfel je? Citeer het FR/NFR-nummer in je subtask; kun je dat niet, bouw het niet.
- **Elke main task levert een mens-testbaar artefact op** (zie "Test Gate" per task), niet alleen groene automatische tests. Een developer of de opdrachtgever moet het resultaat kunnen dráaien en met eigen ogen kunnen zien werken, vóórdat de task als afgerond geldt.
- **Blueprint-herkomst respecteren:** waar het TDD een component labelt als hergebruik van `wiki-template`/`small-medium-data-lake`/`adk2-blueprint`, begin je met dat blueprint kopiëren/forken; schrijf het niet vanaf nul.
- **Python: altijd een virtualenv, nooit globaal installeren.** Elk `pip install`-commando in dit plan is `.venv/bin/python -m pip install ...` (na `python3 -m venv .venv` in Task 0.6), nooit kaal `pip install`: dat installeert in de globale Python-omgeving en kan andere projecten op dezelfde machine breken (gebeurde tijdens het bouwen van Task 0 zelf; zie de les hieronder in Task 0.6).
- **Niet elke FR is een build-task:** FR-02 (kwaliteitscriteria-tekst), FR-12 (jaarlijkse Ingest-ronde), FR-16 (optionele KPI-laag) en FR-17 (DMAIC-capstoneformat), en NFR-06 (criteria wijzigen niet binnen een cohort) zijn content-/procesvereisten die docenten rechtstreeks in de wiki (Task 1) of het configuratiepad (Task 3.6) invullen, geen aparte software-subtask. Ze zijn hier expliciet benoemd zodat ze niet stilzwijgend verdwijnen, niet omdat ze genegeerd mogen worden.

---

## Parallelisatie-overzicht

Main tasks zijn genummerd in de volgorde van de kernketen (0→7). Tasks 8, 9 en 10 zijn **additief** (TDD Deel 13) en niet op het kritieke pad.

| Golf | Main tasks | Mag starten zodra | Kan als aparte subagent-opdracht |
|---|---|---|---|
| 1 | **Task 0** (scaffolding) | direct | — (eenmalig, blokkeert alles) |
| 2 | **Task 1** (wiki) + **Task 2** (data-laag) | Task 0 gepusht | Ja: twee onafhankelijke subagents, geen gedeelde bestanden |
| 3 | **Task 3** (Socratische tutor) + **Task 4** (Review-Ring + dashboard) + **Task 8** (Wiki Quality Check) + **Task 9** (admin) | Task 2 gepusht (Task 3 heeft ook Task 1 nodig) | Ja: vier onafhankelijke subagents; elk raakt een eigen submap (`agents/socratic_tutor/`, `agents/review_ring_coordinator/`, `agents/wiki_quality_check/`, `frontend/src/routes/admin/` + admin-API) |
| 4 | **Task 5** (volledige cyclus, integratie) | Task 3 én Task 4 gepusht | Nee: integratietaak, raakt bestanden van beide vorige tasks |
| 4b | **Task 10** (studentdossier) | Task 4 én Task 8 gepusht | Ja: parallel aan Task 5, eigen submap (`agents/dashboard_query/` uitbreiding + `frontend/src/routes/instructor/dossier/`) |
| 5 | **Task 6** (kalibratie) | Task 5 gepusht | Nee: vereist pilotdata uit Task 5 |
| 5b | **Task 11** (HAN Huisstijl-ontwerpsysteem) | Task 5 én Task 10 gepusht | Ja, parallel aan Task 6: CSS/componentretrofit raakt geen Python, geen overlap met kalibratie |
| 6 | **Task 7** (deployment hardening) | Task 6 gepusht (scaffolding uit 7.1–7.2 mag al eerder starten, parallel aan golf 3–5) | Deels: Dockerfile/CI-CD-scaffolding (7.1, 7.4) kan een aparte subagent zijn zodra Task 0 klaar is; de uiteindelijke smoke-test (7.6) niet |
| 7 | **Task 12** (LRD-uitbreiding) | direct (huidige stand: Tasks 0–5, 8–11 gepusht) | Nee: content-taak, samen met de opdrachtgever direct geschreven, geen subagent |
| 8 | **Task 13** (TDD-uitbreiding) | Task 12 gepusht | Nee: zelfde reden als Task 12 |
| 9 | **Task 14** (sessie- & planningslaag) + **Task 16** (onboarding-flow) + **Task 19** (wiki-gebruiksanalyse) | Task 13 gepusht | Ja: drie onafhankelijke subagents; kleine overlapkans tussen 14 en 19 op `data/smdl/ducklake.py` (beide voegen een tabel toe); laatste die pusht rebaset eerst |
| 10 | **Task 15** (aanwezigheid & in-sessie tools) + **Task 17** (student journey-view) | Task 14 gepusht | Ja: twee onafhankelijke subagents; 17 is vrijwel puur frontend, 15 raakt vooral team_api + nieuwe check-in/poll-routes |
| 11 | **Task 18** (survey/QMS-laag) | Task 15 gepusht (survey wordt getriggerd door de check-in-flow uit Task 15) | Nee: bouwt direct op Task 15's endpoints |
| 12 | **Task 20** (Course Quality Advisor Agent) | Task 18 én Task 19 gepusht | Nee: heeft zowel survey- als engagement-data nodig om iets zinnigs te kunnen rapporteren |
| 13 | **Task 22** (Navigatiestructuur & rolgescoped views) | Task 21 gepusht | Nee: raakt dezelfde onboarding-bestanden als Task 21 |
| 14 | **Task 23** (Onderwijsprogramma) | Task 22 gepusht | Nee: bouwt direct voort op Task 22's `nav-config.ts`/`AppShell` |
| 15 | **Task 24** (Studentportaal: vijf kernviews) | Task 22 én Task 23 gepusht | Nee: raakt dezelfde `nav-config.ts`/`AppShell.tsx` als Task 22/23, en de `CheckpointView.tsx`/onboarding-bestanden van Task 21/22 |
| 16 | **Task 25** (Klas-scoping: peers & docentoverzicht per klas) | Task 24 gepusht | Nee: raakt dezelfde `dashboard_query.agent`/`Programma.tsx`/`Dashboard.tsx` als Task 23/4/11 |

**Vuistregel voor de orkestrerende agent:** als twee main tasks in dezelfde golf staan én geen gedeelde bestanden aanraken (zie de submap-hints), spawn ze als aparte subagents/teammates. Laat elke subagent zijn eigen main task volledig afsluiten (incl. test gate + commit + push) vóórdat de volgende golf begint.

---

## Main Task 0: Repository- en projectscaffolding

**Waarom eerst:** alle volgende tasks hebben een plek nodig om code neer te zetten. Zonder deze task is er geen "junior developer kan dit uitvoeren" mogelijk: er is nog geen repo.

**Bestanden:**
- Create (nieuwe repo `oe-gate-system`): `README.md`, `.gitignore`, `data/`, `agents/`, `frontend/`, `infra/` (lege mappen met `.gitkeep`)

- [x] **0.1** Maak een nieuwe, lege GitHub-repository `oe-gate-system` aan. ✅ Aangemaakt onder `businessdatasolutions` (privé), zelfde org als de documentatierepository: https://github.com/businessdatasolutions/oe-gate-system
- [x] **0.2** Maak de mapstructuur aan: `data/smdl/`, `data/tests/`, `agents/shared/`, `agents/tests/`, `frontend/src/`, `frontend/tests/`, `infra/`.
- [x] **0.3** Schrijf `README.md` met: doel van de repo, link naar `lrd-operational-excellence.html` en `tdd-operational-excellence.html` in de documentatierepository, en de mapstructuur hierboven met één zin uitleg per map.
- [x] **0.4** Schrijf `.gitignore` voor Python (`__pycache__/`, `.venv/`, `*.pyc`) én Node (`node_modules/`, `dist/`); beide lagen leven in dezelfde repo.
- [x] **0.5** Voeg een minimale `data/smdl/__init__.py` en `agents/shared/__init__.py` toe zodat de package-structuur importeerbaar is.
- [x] **0.6** Voeg een `pyproject.toml` toe met de kernafhankelijkheden: `duckdb`, `pydantic`, `google-adk[a2a,db,eval]~=2.0`, `pytest`, `pytest-asyncio`. Maak daarna **eerst** een virtualenv (`python3 -m venv .venv`) vóór je iets installeert. `pip install` zonder venv installeert in de globale Python-omgeving en kan andere projecten op dezelfde machine breken (gebeurde tijdens het bouwen van Task 0 zelf; zie de les hieronder in Task 0.6). ⚠️ **Dit gebeurde daadwerkelijk tijdens het bouwen van deze task**: een kale `pip install -e .` upgradede/downgradede globale pakketten (`openai`, `fastapi`, `pydantic`, `cryptography`, `opentelemetry-*`, e.a.). Gecorrigeerd door alsnog een `.venv` aan te maken en daarin te herinstalleren; de globale pakketten zijn op verzoek van de opdrachtgever ongewijzigd gelaten (geen bekende afhankelijkheid van de oude versies). Zie de bijgewerkte, venv-verplichte instructie hierboven en in `README.md`.
- [x] **0.7** Voeg een minimale `frontend/package.json` toe (`npm create vite@latest . -- --template react-ts`) met React + Vite + TypeScript.

### Test Gate: Task 0
- **Automatisch:** ✅ `python3 -m venv .venv && .venv/bin/python -m pip install -e ".[dev]"` slaagde zonder fouten (pytest 9.1.1 geïnstalleerd, geïsoleerd); `cd frontend && npm install` slaagde zonder fouten (27 packages, 0 vulnerabilities).
- **Mens-testbaar artefact:** ✅ `npm run dev` gestart, `curl -I http://localhost:5173` gaf `HTTP/1.1 200 OK`: Vite-devserver draaide en serveerde de React-startpagina. Devserver na verificatie weer gestopt.

### Commit & push: Task 0
- [x] `git add -A && git commit -m "Scaffold oe-gate-system repo structure"`: commit `7947ad8`.
- [x] `git push origin main`: gepusht naar https://github.com/businessdatasolutions/oe-gate-system
- [x] **Main Task 0 afgerond** (12 juli 2026).

---

## Main Task 1: Wiki-content-laag operationeel

*(Kan parallel aan Task 2, golf 2, aparte subagent, raakt geen bestanden uit Task 2.)*

**Bestanden:** een fork van `wiki-template` (aparte repo, blijft altijd apart van `oe-gate-system`, TDD Deel 3.1/10.1), geen wijzigingen aan `oe-gate-system` in deze task.

- [x] **1.1** Fork/clone `https://github.com/witoldtenhove/wiki-template` als de cursus-wiki-repo. Draai lokaal `npm install && npm run serve` en bevestig dat de bestaande demo-content rendert op `localhost:8080`. ✅ Uitgevoerd vóór de Task 1-subagent werd gestart (door de orkestrerende sessie zelf, niet door de subagent): lokale kopie van `wiki-template` gemaakt (git-historie niet overgenomen, zie commit `89ab0b4`), gepusht naar `https://github.com/businessdatasolutions/oe-wiki`.
- [x] **1.2** Zet de 19 boekconcept-pagina's (Ch1–Ch19) op in `wiki/concepts/`, elk met OKF-frontmatter: verplicht `type: boek-concept`, plus `stage` (direct|design|deliver|develop) en hoofdstuktag (`ch01`…`ch19`) per de mapping in LRD Deel 8 (ná de 10e-editie-correctie: Ch15=Operations improvement, Ch16=Lean operations, Ch5="The structure and scope of supply"). ✅ 19/19 pagina's aangemaakt (`wiki/concepts/ch01-*.md`..`ch19-*.md`); stage-mapping exact volgens LRD Deel 8 (Direct=Ch1–4, Design=Ch5–8, Deliver=Ch9–14, Develop=Ch15–19); Ch1 en Ch2 bevatten de verbatim "key questions" uit de 77-pp preview-PDF (Ch1 volledig gelezen, Ch2 t/m de flexibiliteitssectie), Ch3–19 gebaseerd op de LRD's eigen Deel-8-hoofdstukbeschrijving. Elke pagina citeert Slack, Brandon-Jones & Burgess (2022), 10e editie, met hoofdstuknummer.
- [x] **1.3** Zet de bedrijfscase-bibliotheek op in `wiki/sources/`: begin met de vijf in LRD Deel 1.1 genoemde voorbeelden (IKEA, Toyota, Ryanair, Michelin, Amazon) als concrete eerste batch, elk `type: bedrijfscase-bron`. ✅ Alle 5 aangemaakt, alle 5 **echte content, geen stubs**: de 77-pp preview-PDF bleek bij inspectie te stoppen medio Ch2 (voor de IKEA-case-study aan het eind van dat hoofdstuk), dus geen van de vijf company-boxen zat in de preview. Alle vijf zijn in plaats daarvan gevonden via een gerichte, doelgerichte lezing van de volledige epub (IKEA Ch2-case-study volledig; Toyota Ch16 + Ch4; Michelin Ch9; Ryanair Ch10; Amazon Ch13+Ch15+Ch16, drie boxen). Elke pagina is een eigen samenvatting met korte, aangehaalde citaten (bewust geen bulk-verbatim-reproductie van het auteursrechtelijk beschermde studieboek op een publiek GitHub Pages-adres), geciteerd naar exact hoofdstuk en boxtitel.
- [x] **1.4** Zet de 23 Lean-tool↔Industrie-4.0-referentiepagina's uit Gomaa (2025) Tabel VII op in `wiki/concepts/`, `type: concept`, getagd `[lean-tool, industrie4.0]`: gebruik de gecorrigeerde hoofdstuktags uit LRD Deel 6.2 (Kaizen→ch15, Kanban→ch16, beide `stage: develop`). ✅ 23/23 pagina's; Tabel VII (en de ondersteunende Tabel I) integraal gelezen uit de Gomaa-PDF (20 pagina's, volledig). Kaizen→ch15/develop en Kanban→ch16/develop bevestigd exact zoals gespecificeerd; de overige 21 hoofdstuktags zijn elk met een 1-zins-redenering op de pagina zelf onderbouwd (bv. Poka-Yoke→ch17 i.p.v. ch16 omdat het een conformance-tool is, Hoshin Kanri→ch03 omdat het strategie-implementatie is). **Bekende beperking, niet stilzwijgend weggelaten:** FR-14 noemt ook een losse "Lean 4.0-paradoxen"-pagina; die staat niet in de letterlijke tekst van deze subtaak (die alleen de 23 Tabel-VII-pagina's noemt) en is daarom niet gebouwd: open punt voor een vervolgtaak als volledige FR-14-dekking gewenst is.
- [x] **1.5** Zet de 19 vooraf geselecteerde AI-Wiki-contentmap-pagina's (FR-19) op, per de fase-mapping in LRD §8.1. ✅ 19/19 pagina's (`wiki/concepts/ai-wiki/`); fase-tags herleid uit LRD §8.1's eigen tabel (opgeteld: Direct 4 + Design 2 + Deliver 5 + Develop 8 = 19 ✓). Elke pagina citeert de echte query-trace (nu ook verkregen als raw-bron: `raw/reports/2026-07-11-operational-excellence-course-content-map-query-trace.md`) en is expliciet een pointer-/contentmap-pagina, geen gefabriceerde kopie van de externe, niet-publieke AI-Wiki's paginabodies.
- [x] **1.6** Bouw of hergebruik de bestaande Quartz-extensiecomponent voor het 4D "je-bent-hier"-diagram (FR-13) op elke week-landingspagina, waarbij het huidige thema wordt gemarkeerd, exact zoals het cirkeldiagram in het bronboek. ✅ Nieuwe extensie `extensions/inject-stage-diagram.ts` gebouwd (transformer/htmlPlugin-patroon van `inject-confidence-badge.ts`, geen client-side JS, puur build-time uit frontmatter); structureel gemodelleerd naar Figure 1.13 uit het bronboek (vier stage-nodes rond een centrale "Operations management"-hub, cyclisch verbonden, huidige fase gemarkeerd + "je bent hier"-label), toegankelijk via `role="img"` + dynamische `<title>`. Gestileerd in `quartz/styles/custom.scss` (theme-reactieve CSS-variabelen, licht/donker-modus-bestendig). 4 week-landingspagina's (`wiki/weeks/{direct,design,deliver,develop}.md`) als scaffolding, bewust geen volledig 15-weekprogramma. Build-output geverifieerd: elke pagina markeert in de gerenderde HTML exact de eigen `stage`.
- [x] **1.7** Controleer dat `.github/workflows/deploy.yml` (al aanwezig in `wiki-template`) ongewijzigd werkt: push naar `main` bouwt en publiceert naar GitHub Pages. ✅ Workflow ongewijzigd gelaten (generiek `npx quartz build -d wiki` + `actions/deploy-pages`, geen repo-specifieke aannames die zouden breken). Wel gecorrigeerd: `quartz.config.ts`/`quartz.layout.ts` bevatten nog `wiki-template`-placeholders (`baseUrl: example.github.io/llm-wiki`, `pageTitle: "LLM Wiki"`, footer-GitHub-link) die de daadwerkelijke deploy naar het verkeerde adres zouden laten verwijzen: nu gezet op `businessdatasolutions.github.io/oe-wiki`.

### Test Gate: Task 1
- **Automatisch:** ✅ nieuw script `scripts/lint-wiki-batch.mjs` (hergebruikt `scripts/lint-page.mjs`' patroon/rule-engine per pagina, roept het letterlijk aan via dezelfde stdin-payload als de PostToolUse-hook, plus een eigen OKF-§9-non-empty-`type`-check en recursieve walk) liep over alle 70 nieuwe pagina's uit 1.2–1.6: **0 OKF-fouten, 0 lint-page.mjs-waarschuwingen**. `npm run build`, `npm run check` (tsc --noEmit) en `npm test` (69 bestaande unit tests) slagen alle drie zonder fouten.
- **Mens-testbaar artefact:** ✅ `npm run serve` gestart op `localhost:8080`. Als uitvoerende agent zonder browser is dit geverifieerd via `curl` + directe HTML-inspectie (de dichtstbijzijnde controleerbare gelijkwaardige stap, zoals gevraagd): alle 4 week-landingspagina's (`weeks/{direct,design,deliver,develop}`) gaven `HTTP 200` met het 4D-diagram waarin exact de eigen `stage` de `stage-diagram-node--active`-klasse en het "je bent hier"-label draagt (de SVG's `<title>` bevestigt bv. "Huidige fase: Design" op `weeks/design`); 3 boekconcept-pagina's (Ch1, Ch9, Ch16) en 3 bedrijfscase-pagina's (IKEA, Toyota, Ryanair) gaven `HTTP 200` met de juiste `<title>` en `type`-tag in de gerenderde HTML (plus 2 bonus-steekproeven: een Lean-tool- en een AI-Wiki-contentmap-pagina); een niet-bestaande URL gaf correct `HTTP 404` (negatieve controle die bevestigt dat de 200's betekenisvol zijn); alle 55 `[[wikilinks]]` in de nieuwe pagina's resolveren naar een bestaande pagina (0 broken links, apart geverifieerd). Exacte curl-commando's en resultaten staan in de commit-boodschap/PR-beschrijving zodat een mens dit met eigen ogen kan herhalen door `npm run serve` te draaien en dezelfde URL's in de browser te openen.

### Commit & push: Task 1
- [x] Commit met boodschap die refereert aan FR-11/FR-13/FR-14/FR-19. ✅ Commit `9c133dd` in `oe-wiki`: "Wiki-content-laag operationeel: boekconcepten, bedrijfscases, Lean-tool-referenties, AI-Wiki-contentmap, 4D-diagram (FR-11/FR-13/FR-14/FR-19)".
- [x] Push naar de wiki-repo's `main`. ✅ Gepusht naar https://github.com/businessdatasolutions/oe-wiki (`main`, `89ab0b4..9c133dd`).
- [x] Vink af: **Main Task 1 afgerond** (12 juli 2026).

---

## Main Task 2: Data-laag + OKF-bundelconventie per team

*(Kan parallel aan Task 1, golf 2, aparte subagent.)*

**Bestanden (in `oe-gate-system`):**
- Modify: `data/smdl/storage.py`, `data/smdl/okf.py`, `data/smdl/ducklake.py`
- Create: `data/smdl/ingest_csv.py`, `data/tests/test_okf_concepts.py`, `data/tests/test_ducklake_tables.py`

- [x] **2.1** Kopieer `storage.py`, `okf.py`, `ducklake.py`, `query.py`, `ingest_excel.py`, `ingest_pdf.py`, `service.py` uit `small-medium-data-lake` naar `data/smdl/`. ✅ Alle zeven bestanden overgenomen als startpunt en aangepast: het generieke financiële/ontologie-specifieke deel van `query.py` (synoniemresolutie tegen `ontology.py`) en `service.py`/`ingest_excel.py`/`ingest_pdf.py` (XAF-ingester, financiële Table/Document/Entity-concepten) is vervangen door dit systeem's eigen `instructor-document`/`uploaded_tables`-patroon (TDD 4.7). `ontology.py` en `ingest_xaf.py` zijn bewust niet meegenomen (geen FR/NFR-koppeling, KISS).
- [x] **2.2** Pas `Lakehouse` in `storage.py` aan: vervang `client_id` door `team_id` + `cohort` (team-niveau), en voeg twee extra constructor-varianten toe voor ring-niveau (`ring_id` + `cohort`) en klas-niveau (`klas_id` + `cohort`), drie soorten lakehouse-paden per TDD Deel 4.1/4.6: `lakehouse/<cohort>-team-<team_id>/`, `lakehouse/<cohort>-ring-<ring_id>/`, `lakehouse/<cohort>-klas-<klas_id>/`. ✅ Geïmplementeerd als `Lakehouse.for_team`/`for_ring`/`for_klas`, plus twee bonus-constructors `for_admin` (TDD 4.9, cohortbreed) en `for_system` (TDD 4.8, cohort-onafhankelijk, huisvest `action_log`), alle vijf paden handmatig geverifieerd.
- [x] **2.3** Implementeer in `okf.py` de acht concept-schema's uit TDD Deel 7.1, elk als een `Concept`-subklasse of Pydantic-model met exact de velden uit het TDD: `checkpoint-submission`, `gate-pass-event`, `escrow-state`, `reviewring-assignment`, `klas-roster`, `instructor-document`, `wiki-quality-check`, `course-period`, `team-roster`, `assessment-schedule`. Kopieer de veldnamen letterlijk uit de YAML-voorbeelden in het TDD: geen eigen veldnamen verzinnen. Het `checkpoint-submission`-schema bevat verplicht een kritische-lens-sectie (FR-15) als onderdeel van hetzelfde antwoord, geen apart veld of aparte indiening. ✅ Alle **tien** concept-types (de lijst in deze subtask-tekst noemt er tien, ondanks "acht" hierboven) als Pydantic-modellen geïmplementeerd, veldnamen 1-op-1 uit de TDD-YAML overgenomen. `CheckpointSubmission.create()` vereist `kritische_lens` als verplicht keyword-argument: structureel onmogelijk een submission te bouwen zonder FR-15-antwoord.
- [x] **2.4** Implementeer in `ducklake.py` schrijfhelpers voor de zeven tabellen uit TDD Deel 7.2: `gate_events`, `rubric_scores`, `reviewring_log`, `override_log`, `uploaded_tables`, `action_log`, `wiki_quality_log`, kolomnamen exact zoals in het TDD. ✅ Alle zeven, elk met een Pydantic-rijmodel + `write_*`/`query_*`-paar; nieuwe `ducklake.append_rows()` (INSERT, niet de blueprints REPLACE-`write_table`) zodat deze append-only logs elkaar niet overschrijven.
- [x] **2.5** Schrijf `data/smdl/ingest_csv.py`: een deterministische ingester naar het patroon van `ingest_excel.py` (TDD Deel 4.7): CSV inlezen, output als DuckLake-tabel + `instructor-document`-concept. ✅ Geschreven en handmatig end-to-end getest (zie testverslag in de agent-rapportage).
- [x] **2.6** Voeg `.csv` toe aan de routeringstabel in `service.py` (naast de bestaande `EXCEL_EXTS`/`PDF_EXTS`). ✅ `CSV_EXTS = {".csv"}` toegevoegd naast `EXCEL_EXTS`/`PDF_EXTS`.
- [x] **2.7** Schrijf `data/tests/test_okf_concepts.py`: voor elk van de acht concept-types uit 2.3 een test die een instance aanmaakt, naar een tijdelijke lakehouse schrijft, terugleest, en velden vergelijkt. ✅ Alle tien concept-types + 3 aanvullende structurele tests (type-dispatch, non-empty-type-afdwinging), 13 tests, allemaal geslaagd.
- [x] **2.8** Schrijf `data/tests/test_ducklake_tables.py`: voor elk van de zeven tabellen uit 2.4 een test die een rij schrijft en via een `SELECT`-query terugleest. ✅ Alle zeven tabellen + 3 aanvullende tests (append-only-gedrag, foutuitkomst, SUM-query over `wiki_quality_log`), 10 tests, allemaal geslaagd.
- [x] **2.9** Schrijf een smoke-test-CLI-script `data/smdl/cli.py` met een `inspect <team_id>`-commando dat een team-lakehouses OKF-bundle-inhoud en DuckLake-tabelrijen naar de terminal print (mens-leesbaar, geen JSON-dump). ✅ Geïmplementeerd en handmatig geverifieerd: zie Test Gate hieronder voor de daadwerkelijke output.
- [x] **2.10** Schrijf `agents/shared/action_log.py`: een herbruikbare decorator `@log_action(layer=...)` (NFR-08, TDD Deel 5.6) die elke functie waarop hij wordt toegepast omhult: bij succes een `action_log`-regel met `outcome=success`, bij een exception `outcome=error` + foutdetail, altijd met een `correlation_id` uit de aanroepende sessie/workflow-context. Elke volgende agent-task (3, 4, 8, 10) past deze decorator toe op zijn eigen tools: bouw hem hier één keer, generiek genoeg voor hergebruik. ✅ Werkt op zowel sync als async functies, genereert zelf een `correlation_id` indien niet meegegeven, geeft de oorspronkelijke exception altijd door (logt, slikt nooit in), en forwardt de gereserveerde kwargs (`correlation_id`/`actor_type`/`actor_id`/`target_type`/`target_id`/`lakehouse`) nooit naar de gewrapte functie zodat elke bestaande functiesignatuur ermee te wrappen is, 6 eigen tests in `agents/tests/test_action_log.py` (niet vereist door de test gate hieronder, wel gedraaid en geslaagd).
- [x] **2.11** Implementeer in `service.py` de distributiescope-routering uit TDD Deel 4.6/4.7 (FR-20): een functie `route_instructor_document(scope: Literal["team","ring","klas"], scope_id, cohort, file) -> Lakehouse` die het juiste lakehouse-pad kiest op basis van `scope` (scope `"everyone"` wordt hier expliciet **niet** afgehandeld: dat pad gaat via Task 1's wiki-terugschrijfmechaniek, niet via de data-laag). ✅ Geïmplementeerd; `scope="everyone"` en een onbekende bestandsextensie geven allebei een expliciete `ValueError` met duidelijke boodschap, handmatig geverifieerd (zie agent-rapportage).

### Test Gate: Task 2
- **Automatisch:** ✅ `.venv/bin/python -m pytest data/tests/ -v`: **23/23 geslaagd** (alle tests uit 2.7/2.8; `pytest -v` over de hele repo, incl. de niet-verplichte `agents/tests/test_action_log.py`, geeft 29/29 geslaagd).
- **Mens-testbaar artefact:** ✅ `rm -rf lakehouse && .venv/bin/python -m smdl.demo_seed && .venv/bin/python -m smdl.cli inspect team-07` daadwerkelijk gedraaid. Output toont de teamnaam (`team-07`, cohort `2026-2027-oe`), het `checkpoint-submission`-concept (titel "Checkpoint 1 — Direct", stage/hoofdstuk, bedrijfscase Ryanair, teamleden, en een leesbare preview van het FR-15-kritische-lens-antwoord), het `gate-pass-event`-concept én de bijbehorende `gate_events`-DuckLake-rij, en een aparte "Gate status summary"-sectie (`direct Gate A: OPEN`), mens-leesbaar, geen JSON-dump.

### Commit & push: Task 2
- [x] Commit met boodschap die refereert aan FR-01/FR-07/FR-09/FR-11/FR-20/FR-21/FR-22. Commit `c46301f`.
- [x] Push naar `oe-gate-system` `main`. ✅ Gepusht naar https://github.com/businessdatasolutions/oe-gate-system (`b201cd8..c46301f`).
- [x] Vink af: **Main Task 2 afgerond** (12 juli 2026).

---

## Main Task 3: Socratische Tutor Agent + evalharnas (alleen Gate 1)

*(Golf 3, parallel aan Task 4/8/9; vereist Task 1 én Task 2 gepusht.)*

**Bestanden:**
- Create: `agents/socratic_tutor/agent.py`, `agents/shared/session.py`, `agents/socratic_tutor/socratic_tutor.evalset.json`, `agents/tests/test_socratic_tutor_component.py`

- [x] **3.1** Kopieer `shared/models.py` en `shared/session.py` uit `adk2-blueprint` naar `agents/shared/`. ✅ Beide bestanden vrijwel verbatim gekopieerd (env-var-gestuurd, geen blueprint-specifieke paden om aan te passen) naast het al bestaande `agents/shared/action_log.py` uit Task 2.
- [x] **3.2** Scaffold `agents/socratic_tutor/agent.py` als `task`-modus `Agent` (patroon uit `adk2-blueprint/agents/collector/`). ✅ Tweelaags, exact als het blueprint-patroon: `socratic_tutor_worker` (`mode="task"`, leaf, alle drie tools) wordt gedelegeerd door een `mode`-loze `root_agent`-coördinator (patroon `agents/concierge/`). Dit is **empirisch vereist, niet alleen stijlkeuze**: tegen de geïnstalleerde `google-adk==2.4.0` geverifieerd dat `Runner.run_async` een harde `ValueError` geeft ("LlmAgent as root agent must have mode='chat'") als een `task`-modus agent zelf als root draait; een kale task-agent kan dus nooit direct door een `Runner` gedraaid worden, alleen als gedelegeerd sub-agent.
- [x] **3.3** Implementeer de tool `query_wiki(query: str) -> list[dict]` die de gepubliceerde wiki (Task 1) doorzoekt (hergebruik het `qmd`-hybride-zoekpatroon uit `wiki-template`, TDD Deel 5.1). ✅ **Vereenvoudiging, expliciet zoals de taakomschrijving toestaat:** deterministische keyword/grep-match over de echte `oe-wiki/wiki/**/*.md`-inhoud (YAML-frontmatter + body, titel/tags zwaarder gewogen dan body) in plaats van de volledige `qmd`-hybride-zoektool: geen LLM, dus geen verzonnen citaties. Handmatig + geautomatiseerd geverifieerd tegen de echte IKEA- en Ch02-pagina's.
- [x] **3.4** Implementeer de tool `read_team_history(team_id: str, stage: str) -> list[dict]` die alle eerdere `checkpoint-submission`-vervolgconcepten van dat team uit de data-laag (Task 2) leest. ✅ Leest alle tutor-geschreven vervolgconcepten (titel-prefix-gemarkeerd, zie 3.5) van stages die strikt vóór `stage` liggen in `direct→design→deliver→develop`, oudste eerst, leeg voor `stage="direct"` per constructie (Gate 1: Direct is de eerste D-fase, geen eerdere stage bestaat nog), generiek geschreven zodat dit correct blijft werken zodra Task 5 de overige drie stages ontgrendelt.
- [x] **3.5** Implementeer de tool `write_report(team_id, stage, team_transcript, instructor_report, rubric_score, gate_a_status)` die **twee gescheiden schrijfacties** doet: een teamgericht antwoord-schema zónder scoreveld, en een docentgericht schema mét `rubric_score`, exact de structurele scheiding uit TDD Deel 5.1/6.3. Schrijf een test die aantoont dat het teamgerichte schema geen `score`-attribuut heeft (structurele controle, niet alleen een lint-regel). ✅ Twee volledig gescheiden bestanden (nooit samen opgeslagen): het teamkanaal schrijft een `checkpoint-submission`-vervolgconcept met **uitsluitend** de Socratische vragen als body; het docentkanaal schrijft een eigen, apart vervolgconcept-bestand voor de instructeursnotities plus een `rubric_scores`-DuckLake-rij (nooit via het teamkanaal leesbaar). Twee losstaande pydantic-schema's (`TeamFacingReport`/`InstructorFacingReport`, beide `extra="forbid"`). `TeamFacingReport` heeft structureel geen scoreveld; een test bewijst dat het smokkelen van `rubric_score=` een `ValidationError` geeft bij constructie, niet alleen dat het veld ontbreekt. `gate_a_status` wordt nooit vertrouwd als LLM-bewering: de daadwerkelijk opgeslagen status wordt altijd deterministisch herberekend uit `rubric_score` tegen een kalibratie-pending drempelwaarde (TDD Deel 4.4 never-invents).
- [x] **3.6** Schrijf de Socratische systeeminstructie (nooit een verdict, altijd voortbouwen op eerdere geschiedenis, FR-03) in een apart, module-eigenaar-only-schrijfbaar configuratiebestand `agents/socratic_tutor/prompts/system_instruction.md`. ✅ Inclusief een expliciete, staande FR-15-categorie (Lean 4.0/Industry 5.0-kritische lens). Losstaand van elk per-team OKF-bundelpad: geen enkele team-/studenttool kan dit bestand bereiken (dat IS hier de structurele FR-10-afdwinging op codeniveau; deploy-time service-account-scopes zijn Task 7). Erbij: `agents/socratic_tutor/prompts/shared_criteria.md` als FR-02-configuratiepad-placeholder (structuur + `<!-- TODO (docent) -->`-secties per D-fase; de inhoud zelf is bewust buiten scope, zoals de globale randvoorwaarden voorschrijven).
- [x] **3.7** Koppel `DatabaseSessionService` (uit `shared/session.py`) met één sessie per team-per-checkpoint. ✅ `agent.py`'s `session_id_for(team_id, stage)` (`f"{team_id}--{stage}"`) + `build_session_service()` roepen `shared/session.py`'s bestaande env-flag aan; deze dev-omgeving heeft geen `DATABASE_URL` (nog geen managed Postgres, dat is Task 7), dus tests/demo draaien tegen de `InMemorySessionService`-fallback: de omschakeling naar `DatabaseSessionService` is enkel een env-var-flip, geen codewijziging.
- [x] **3.8** Schrijf `agents/socratic_tutor/socratic_tutor.evalset.json` met minimaal vijf cases die controleren dat geen enkel teamgericht antwoord scoretaal bevat ("goed", "fout", een cijfer) (AC-03). ✅ Vijf cases (IKEA/Ryanair/Toyota/Michelin/Amazon-cases, Ch01–Ch04/Direct-fase), elk met een voorbeeld-Socratisch-transcript zonder scoretaal én een verwachte tool-trajectory (delegatie naar `socratic_tutor_worker`). **Belangrijke, empirisch onderbouwde kanttekening** (zie Test Gate): ADK's tool-trajectory-matcher vereist exacte argumentgelijkheid per aanroep, dus vrije-tekst-argumenten (`team_transcript` e.d.) worden bewust niet strikt gematcht tegen een live model: zelfde ontwerpkeuze als `adk2-blueprint`'s eigen currency-evalset ("Response text is NOT gated here").
- [x] **3.9** Schrijf `agents/tests/test_socratic_tutor_component.py`: een in-memory-`Runner`-test die een mock-checkpoint-inzending door de agent stuurt en controleert dat er precies één rapport per kanaal wordt geschreven. ✅ 12 tests. De hoofdtest drijft de échte tweelaagse agent (coördinator→task-worker) door een echte `InMemoryRunner`, met een zelfgebouwde adaptieve `ScriptedLlm` (`testing.py`) die alleen de model-aanroep zelf stubt (echte tool-executie, echte ADK-`finish_task`-mechaniek, echte wiki-zoekopdracht, echte DuckLake/OKF-writes) en bevestigt precies één teamrapport + precies één instructeursrapport + precies één `rubric_scores`-rij + precies één `gate_events`-rij.
- [x] **3.10** Pas de `@log_action`-decorator uit Task 2.10 toe op `query_wiki`, `read_team_history` en `write_report` (NFR-08). ✅ Alle drie gedecoreerd; `agents/shared/action_log.py` blijkt `OE_LAKEHOUSE_ROOT` eenmalig bij module-import te lezen (niet per aanroep): expliciet opgevangen met `configure_lakehouse_root(...)`-fixtures in zowel de tests als `demo_run.py`, zodat geen enkele testrun echte `action_log`-regels in de gedeelde dev-`lakehouse/`-map achterlaat (zelfde patroon als Task 2's eigen `test_action_log.py`).

### Test Gate: Task 3
- **Automatisch:** ✅ `.venv/bin/python -m pytest agents/tests/test_socratic_tutor_component.py -v`: **12/12 geslaagd**. `.venv/bin/python -m pytest agents/socratic_tutor/ -m eval -v`: **4 geslaagd, 1 geskipt**. De live-`AgentEvaluator`-test skipt expliciet zonder `GOOGLE_API_KEY` (geen credentials aanwezig in deze omgeving). **Empirisch geverifieerd vóór het bouwen van deze guard**: `AgentEvaluator.evaluate(...)` gooit zónder credentials geen exception op, maar geeft `None` terug met nul echt-uitgevoerde evaluaties (de per-taak-fout verdwijnt stil in een interne `asyncio.gather`, waardoor een naïeve try/except een vals-positieve pass zou rapporteren); vandaar een expliciete credential-check vóór de aanroep in plaats van op een generieke except te vertrouwen. De never-scores-check zelf (`test_no_case_contains_scoring_language`, deterministische scan van de evalset-referentietranscripten) **slaagt 100%**, net als de evalset-schemavalidatie en de trajectory-structuurcheck. Volledige repo-testsuite: **126 geslaagd, 1 geskipt** (uitsluitend deze live-model-eval).
- **Mens-testbaar artefact:** ✅ Geen browser beschikbaar als uitvoerende agent (zelfde beperking als Task 1); dichtstbijzijnde controleerbare equivalent, zoals de taakomschrijving expliciet toestaat: `agents/socratic_tutor/demo_run.py` daadwerkelijk gedraaid (`.venv/bin/python -m socratic_tutor.demo_run`) met een realistische mock-Direct-checkpoint (team-03, IKEA, ch02): échte `query_wiki`-hits tegen de echte wiki, écht geschreven OKF/DuckLake-data in een geïsoleerde temp-lakehouse, LLM-stap gestubt (`ScriptedLlm`, geen API-key in deze omgeving; expliciet vermeld in de output, niet stilzwijgend). Output toont het teamgerichte en docentgerichte rapport side-by-side: het teamkanaal bevat vier Socratische vragen (IKEA-performance-objectives, Ch2-spanningsveld, FR-15-kritische-lens, Industry 4.0/5.0-afweging) zonder enige verdict-taal; het docentkanaal toont dezelfde vragen plus `rubric_score: 78.0` en `criteria_ref`. Live-geverifieerd in dezelfde run: `TeamFacingReport`'s velden bevatten geen scoreveld, een scoretaal-scan over het transcript geeft nul treffers, en een poging om `rubric_score=99` in `TeamFacingReport(...)` te smokkelen wordt afgewezen met een `ValidationError` (`extra="forbid"`), met eigen ogen leesbaar in de terminal-output.

### Commit & push: Task 3
- [x] Commit met boodschap die refereert aan FR-02/FR-03/FR-10/FR-15. ✅ Commit `e8c5783`.
- [x] Push naar `oe-gate-system` `main`. ✅ Gepusht naar https://github.com/businessdatasolutions/oe-gate-system (`e9ba394..e8c5783`).
- [x] Vink af: **Main Task 3 afgerond** (12 juli 2026).

---

## Main Task 4: Review-Ring Coordinator + Instructor Dashboard

*(Golf 3, parallel aan Task 3/8/9; vereist Task 2 gepusht.)*

**Bestanden:**
- Create: `agents/review_ring_coordinator/workflow.py`, `agents/dashboard_query/agent.py`, `frontend/src/routes/instructor/Dashboard.tsx`, `agents/tests/test_review_ring.py`

- [x] **4.1** Scaffold `agents/review_ring_coordinator/workflow.py` als deterministische ADK `Workflow` (patroon uit `adk2-blueprint/agents/approval/`): **geen LLM-call**. ✅ Empirisch geverifieerd tegen de geïnstalleerde `google-adk==2.4.0` met een tijdelijke smoke-workflow vóórdat de echte module werd geschreven: een `Workflow` met uitsluitend plain-function-nodes (geen enkele `Agent`) bouwt, draait, pauzeert op een `RequestInput` en hervat correct via `InMemoryRunner`, zelfde `process_input → draft → request_review → handle_review → {routes}`-vorm als `approval/agent.py`, alleen zonder de `draft`-Agent.
- [x] **4.2** Implementeer ringtoewijzing bij cohortstart: elk team krijgt een A→B→C→A-positie (FR-07), geschreven als `reviewring-assignment`-concept (Task 2). ✅ `assign_ring_positions(root, cohort, klas_id, team_ids)` partitioneert in vaste ringen van drie (expliciete `ValueError` bij een niet-deelbaar aantal: geen halfgespecificeerde restgroep-logica); `ring_id` (bv. `klas-a-ring-01`) is geen schemaveld op `ReviewringAssignment` maar de `write_singleton`-bestandsslug, later herleid via `Path.stem` (`find_ring_for_team`).
- [x] **4.3** Implementeer fallback-herconfiguratie (FR-08): een functie `reconfigure_ring(ring_id, departed_team_id)` die alleen de betrokken posities aanpast. ✅ Werkelijke signatuur `reconfigure_ring(root, cohort, klas_id, ring_id, departed_team_id)`: `ring_id`/`departed_team_id` letterlijk zoals hierboven gespecificeerd, plus de drie parameters die nodig zijn om het bestand daadwerkelijk te lokaliseren (dat is prose-kortschrift in de subtaaktekst, geen volledige signatuur). List-deletie herverbindt voorganger→opvolger vanzelf; geen andere ring in de klas wordt gelezen of geschreven (apart getest). **Live afgestemd met de Task 9-agent** (team-roster↔ring-koppeling, 9.4): zij lazen deze module rechtstreeks en hebben inmiddels gewired op de echte signatuur, bevestigd over en weer.
- [x] **4.4** Implementeer de escrow-onthulling met het `RequestInput`/resume-patroon (FR-18): team A's zicht op ontvangen feedback blijft gepauzeerd tot het event "eigen review ingediend" binnenkomt. ✅ `escrow_reveal_workflow` (echte ADK `Workflow`) pauzeert op `await_own_submission` tot een resume met `{"own_review_submitted": true}` binnenkomt. **Belangrijke ontwerpbeslissing:** de garantie zit niet in het workflow-event zelf maar in de lezer: `reveal_feedback()` herleidt bij élke aanroep opnieuw, uit levende data (het escrow-record van team A's eigen opvolger), of team A daadwerkelijk zijn eigen review al indiende; zelfs een hervatting met een *vals* `own_review_submitted: true`-signaal (geen echte `record_review_given()`-aanroep eronder) onthult niets, apart getest (`test_workflow_resume_true_without_real_submission_still_stays_locked`). Zelfde "server-side, niet client-side"-patroon als TDD 6.3's scoreveld-garantie, nu toegepast op peer-escrow.
- [x] **4.5** Implementeer het `/check-lapses`-endpoint (FR-04): een los aan te roepen functie die `escrow-state`-concepten met een verstreken `lapse_deadline` (Task 2, 48u) detecteert en de "ontvangen"-voorwaarde laat vervallen. ✅ `check_lapses(root, cohort, klas_id, stage, team_ids, now=...)`. **Ontwerpcorrectie gaandeweg gevonden:** `lapse_deadline` moet een vast punt zijn (reviewdeadline + 48u), niet 48u ná een inzendmoment dat bij een daadwerkelijke lapse per definitie nooit plaatsvindt, vandaar de nieuwe `open_escrow_window()`/`open_escrow_windows_for_ring()`, die het venster bij aanvang van de reviewfase vastzet, onafhankelijk van of/wanneer de gevende partij indient. Een vervallen venster registreert de gemiste review in `reviewring_log` op het conto van de **nalatige gevende** partij (niet de wachtende partij), letterlijk FR-04's "telt bij diens eigen gate B".
- [x] **4.6** Scaffold `agents/dashboard_query/agent.py`: de tool `query_dashboard(cohort)` die `gate_events`/`rubric_scores` aggregeert via DuckDB-SQL (nooit een LLM-samenvatting van getallen, TDD Deel 4.4). ✅ `build_dashboard()` doorzoekt élk team-lakehouse van het cohort (geen cross-team-catalogus, TDD 4.1, dus N losse DuckLake-catalogi geopend en client-side samengevoegd, zelfde patroon als `demo_seed.py` al per team gebruikt), aggregeert `gate_events`/`rubric_scores`/`override_log` puur via SQL (`QUALIFY row_number() ... = 1` voor "laatste status per gate", `avg()`/`count()` voor scores/overrides), en geeft élk veld terug mét de letterlijke SQL die het produceerde. `root_agent` (Gemini, hardcoded modelnaam net als het blueprint zelf) is een dunne relay-laag met precies één tool.
- [x] **4.7** Bouw `frontend/src/routes/instructor/Dashboard.tsx`: per-team statuskaart (fase, gate A/B-status), override-knop die naar een `POST /override`-endpoint schrijft dat een `gate-pass-event` met `actor: instructor` aanmaakt (FR-06) en in `override_log` landt. ✅ Statuskaarten met kleur+icoon+tekst-badges (nooit kleur alleen, WCAG 1.4.1), een samenvattingsregel ("N teams · X on track · Y pending · Z need attention") voor directe triage, en een inklapbare SQL-brongegevens-sectie (never-invents-transparantie, TDD 4.4). Geen live backend (zoals de taak expliciet toestaat): `fetchDashboard`/`postOverride` zijn getypeerde fetch-calls tegen een gedocumenteerde-maar-nog-niet-live API-vorm, met een expliciet in de console gelogde mock-fallback (nooit stilzwijgend). Écht end-to-end getest in een browser (Playwright): override-knop → formulier → submit → badge wisselt zichtbaar naar "Override", teller en samenvattingsregel updaten mee.
- [x] **4.8** Schrijf `agents/tests/test_review_ring.py`: tests voor ringtoewijzing, fallback-herconfiguratie, en dat escrow-onthulling pas plaatsvindt ná de eigen-review-submit-event. ✅ 18 tests. De FR-18-garantie wordt op **twee niveaus** bewezen: rechtstreeks tegen `reveal_feedback()` (vóór/ná eigen inzending) én tegen de échte ADK-`Workflow` via een volledige `InMemoryRunner`-pauze/hervat-cyclus, inclusief de hierboven genoemde "lieg tegen de workflow"-test. Ook: `check_lapses`-tests (unblock + correcte toeschrijving aan de nalatige partij, no-op vóór deadline, no-op als al gegeven) en reconfigure-tests (raakt alleen de betrokken ring).
- [x] **4.9** Pas de `@log_action`-decorator uit Task 2.10 toe op de workflow-transities in `review_ring_coordinator/workflow.py` en op `query_dashboard` (NFR-08). ✅ Toegepast op alle vijf kernfuncties (`assign_ring_positions`, `reconfigure_ring`, `open_escrow_window`, `record_review_given`, `reveal_feedback`, `check_lapses`, de daadwerkelijke bedrijfslogica-transities uit TDD 5.6) en op `query_dashboard`; de dunne ADK-graaf-plumbing (`start_escrow_watch` e.d.) bewust niet dubbel gedecoreerd, want die roept uitsluitend de al-gelogde functies aan.

### Test Gate: Task 4
- **Automatisch:** ✅ `.venv/bin/python -m pytest agents/tests/test_review_ring.py -v`: **18/18 geslaagd**, inclusief de expliciete FR-18-ordeningstest op zowel functie- als workflow-niveau.
- **Mens-testbaar artefact:** ✅ `data/smdl/demo_seed.py` uitgebreid met `--dashboard-demo` (3 mock-teams, team-05/07/09, de TDD's eigen voorbeeldring, met bewust verschillende statussen, plus een echte ring-assignment + escrow-window via Task 4's eigen module). `npm run dev` in `frontend/` gedraaid; de Dashboard-route is **niet** in `main.tsx`/`App.tsx` verankerd (dat is bewust Task 5's integratiewerk, en zou botsen met Task 9's gelijktijdige admin-routes): voor deze test is `main.tsx` tijdelijk naar `<Dashboard />` gewezen, geverifieerd in een échte browser (Playwright: navigatie, screenshot, override-knop end-to-end aangeklikt), en vóór deze commit weer teruggezet (`git diff` op `main.tsx` is leeg). **Gemeten tijd:** het volledige, foutloze uitlezen van alle 3 teams' fase + beide gate-statussen + verborgen score + override-aantal, van eerste screenshot tot laatste geverifieerde statusregel, was **~19 seconden**, ruim binnen het 1-minuutbudget van NFR-03/AC-04. De samenvattingsregel ("1 need attention") alleen al maakt "welk team eerst" een blik van seconden.

### Commit & push: Task 4
- [x] Commit met boodschap die refereert aan FR-04/FR-06/FR-07/FR-08/FR-18/NFR-03. ✅ Commit `5b52bae`.
- [x] Push naar `oe-gate-system` `main`. ✅ Gepusht naar https://github.com/businessdatasolutions/oe-gate-system (`e8c5783..5b52bae`).
- [x] Vink af: **Main Task 4 afgerond** (12 juli 2026).

---

## Main Task 5: Volledige cyclus (alle vier gates live + team-frontend)

*(Golf 4, integratietaak, niet parallelliseerbaar; vereist Task 3 én Task 4 gepusht.)*

**Bestanden:**
- Modify: `agents/socratic_tutor/agent.py` (verwijder Gate-1-only-restrictie), `agents/review_ring_coordinator/workflow.py`
- Create: `frontend/src/routes/team/CheckpointView.tsx`, `agents/tests/test_full_cycle_integration.py`

- [x] **5.1** Verwijder elke Gate-1-only-restrictie uit Task 3; bevestig dat de agent voor alle vier D-fasen werkt met dezelfde tools. ✅ De tools zelf (`query_wiki`/`read_team_history`/`write_report`) waren al stage-generiek geschreven sinds Task 3 (`Stage`/`STAGE_ORDER` dekten van meet af aan alle vier fasen; nergens een `if stage != "direct"`-restrictie): "Gate 1" was wat Task 3's buildplan-tekst destijds vróég te bewijzen, geen functionele restrictie in de code. Bevestigd met een nieuwe, expliciete test (`test_write_report_and_read_team_history_work_identically_for_every_stage`, geparametriseerd over design/deliver/develop) en end-to-end via de échte agent in `test_full_cycle_integration.py`. Stale "Gate 1 pilot"-formulering opgeschoond in `tools.py`/`agent.py`/de evalset-naam.
- [x] **5.2** Bouw `frontend/src/routes/team/CheckpointView.tsx`: het teamgerichte scherm: Socrates-chatkaart, Gate A/B-statuskaarten, peer-partnerkaart, footer zonder scoreveld (mapping uit TDD Deel 6.1). ✅ Elke mockup-rij 1-op-1 gemapt: faseprogressie-tracker (team-avatars uit `submitted_by`), 🦉 Socrates-chatkaart (teamgericht kanaal, nooit het antwoord), Gate A/B-statuskaarten (open ✅/pending ⏳, zelfde vocabulaire als `Dashboard.tsx`), peer-partnerkaart (gegeven/ontvangen + FR-18-escrow-tekst), footer "Geen cijfer zichtbaar hier — alleen open/dicht." Ook: **echte app-brede routing**, eindelijk, in `App.tsx`/`vite.config.ts` (react-router-dom): `/team/:teamId/:stage`, `/instructor/dashboard` (ontsluit eindelijk Task 4's `Dashboard.tsx` via een echte route), `/admin` blijft bewust een kale `<a href="/admin.html">` naar Task 9's aparte entry-point (niet samengevoegd, gedocumenteerde keuze in `App.tsx`'s eigen commentaar, want React Router kan geen tweede Vite-bundel in-place renderen en Task 9 isoleerde admin bewust hiervoor). `vite.config.ts` bouwt nu ook beide HTML-entries in productie (`admin.html` viel eerder stilzwijgend uit `npm run build`).
- [x] **5.3** Implementeer het server-side "geen cijfer zichtbaar"-contract (TDD Deel 6.3): het teamgerichte API-response-schema bevat structureel geen scoreveld: voeg een contract-test toe die faalt als iemand per ongeluk een scoreveld toevoegt aan dat schema. ✅ Nieuw `agents/team_api/schemas.py`: `TeamCheckpointView` (+ elk geneste kaart-model) met `extra="forbid"`, structureel zonder score/grade/verdict-veld op elk niveau van de boom, zelfde patroon als Task 3's `TeamFacingReport`, nu op de route-laag. `agents/tests/test_team_api_contract.py` (8 tests) bewijst dit zowel qua veldnamen als qua smokkelpoging (`ValidationError` bij een gesmokkelde `rubric_score=`), plus een live-verificatie in `test_full_cycle_integration.py` tegen de echte serverresponse-JSON (`"score"`/`"rubric"` komen letterlijk nul keer voor).
- [x] **5.4** Implementeer het binaire gate-signaal (FR-05): een functie `combine_gate_signal(gate_a_status, gate_b_status) -> Literal["open","dicht"]`, voor het team alleen zichtbaar als open/dicht. ✅ Toegevoegd aan `agents/review_ring_coordinator/workflow.py`, samen met de nieuwe `evaluate_gate_b_status`/`gate_b_conditions`: niets schreef vóór deze task een `gate-pass-event` voor gate B; Task 4 bouwde de escrow-mechaniek maar leidde er nooit een gate-status uit af, dus `dashboard_query`'s eigen `gate = 'B'`-SQL had niets te vinden. `combine_gate_signal` telt "open" én "override" (FR-06) als open. Het team ziet dit als hét enige "volgende fase"-signaal, in de footer; de losse Gate A/B-kaarten (5.2) tonen apart (zoals TDD 6.1's eigen mockup-tabel expliciet voorschrijft) alleen het binaire open/pending-procesbadge, nooit een cijfer of percentage.
- [x] **5.5** Breid de evalset uit Task 3 uit met AC-05-cases (voortbouw-op-vorig-rapport-check voor gate 2–4). ✅ Drie nieuwe cases (`design_ikea_ch06_builds_on_history`, `deliver_ryanair_ch11_builds_on_history`, `develop_toyota_ch16_builds_on_history`) naast Task 3's bestaande direct-fase case; elk referentietranscript noemt expliciet de vorige D-fase waarop het voortbouwt. Deterministisch bewezen (3 nieuwe tests in `test_socratic_tutor_eval.py`): elke AC-05-case noemt de juiste vorige fase, geen twee cases zijn tekstueel identiek, en alle AC-05-cases blijven ook AC-03-schoon (geen scoretaal).
- [x] **5.6** Schrijf `agents/tests/test_full_cycle_integration.py`: één test die één mock-team door alle vier checkpoints stuurt (submit → Gate A → Gate B → volgende fase) en op elk punt de juiste status controleert. ✅ 3 tests. De hoofdtest (`test_one_team_through_all_four_checkpoints_gates_and_signal_track_correctly`) drijft de échte tweelaagse ADK-agent (coördinator→task-worker, `ScriptedLlm`-gestubt uitsluitend op de LLM-grens, zelfde discipline als Task 3) door alle vier D-fasen in volgorde, met écht `review_ring_coordinator`-verkeer (voorganger geeft eerst: gate B blijft dicht; team geeft daarna zelf: gate B gaat open) en een écht, per checkpoint groeiende `read_team_history`; controleert op elk punt de juiste status via `team_api.build_checkpoint_view`, en dat elk rapport aantoonbaar voortbouwt op het vorige (nooit een letterlijke herhaling). Twee aanvullende gerichte tests: gate B gaat nooit open van alléén-geven zonder ontvangen, en de teamgerichte view lekt de verborgen rubriekscore nergens, ook niet ná een echte agent-run.

### Test Gate: Task 5
- **Automatisch:** ✅ `.venv/bin/python -m pytest agents/tests/test_full_cycle_integration.py -v`: **3/3 geslaagd**. `.venv/bin/python -m pytest agents/socratic_tutor/ -m eval -v`: **7 geslaagd, 1 geskipt** (dezelfde live-`AgentEvaluator`-skip als Task 3, geen `GOOGLE_API_KEY` in deze omgeving; de deterministische AC-03/AC-05-checks lopen wél en slagen 100%). Volledige repo-testsuite voor deze task alleen (geïsoleerde `git worktree` op commit `25d1261`, dus zonder het gelijktijdige Task 10-werk in dezelfde repo): **160 geslaagd, 1 geskipt** (was 146 vóór deze task).
- **Mens-testbaar artefact:** ✅ Volledige handmatige doorloop in de daadwerkelijk draaiende UI (`npm run dev` in `frontend/` + `.venv/bin/python -m team_api.server`, aangestuurd via browser-automation/Playwright), voorafgegaan door `data/smdl/demo_seed.py --team-view-demo` (nieuw, hergebruikt Task 4's `seed_dashboard_demo`-patroon: ring team-05/07/09, voorganger team-05 heeft al gegeven; team-07 zelf start bewust zónder checkpoint, zodat de indiening zelf live gebeurt, niet vooraf geseed). Op `/team/team-07/direct`: leeg scherm (Gate A/B "Nog niet gestart", geen cijfer) → checkpointformulier ingevuld en ingediend (Ryanair, ch02) → "Vraag de Socratische tutor" geklikt → écht gegenereerde, geschiedenis-bewuste Socratische vragen verschenen, Gate A → ✅ Open → eigen peer-review-tekst ingevuld en verstuurd aan team-09 → Gate B → ✅ Open (FR-18-escrow toonde de al aanwezige feedback van team-05 zichtbaar) → footer "Volgende fase: **OPEN**". Daarna naar `/team/team-07/design` genavigeerd: nieuw checkpoint ingediend, tutor gevraagd: vraag 1 citeerde letterlijk "jullie direct-checkpoint (ch02)" (AC-05 live zichtbaar, niet alleen in pytest). **Geen cijfer op enig moment:** een DOM-tekstscan gaf één "cijfer"-treffer (de footer-ontkenning zelf: "Geen cijfer zichtbaar hier"), en een directe fetch van de ruwe server-JSON bevestigde dat `"score"`/`"rubric"` letterlijk **nul keer** voorkomen in de payload. Op schijf apart gecontroleerd: het teamkanaal-bestand bevat uitsluitend de Socratische vragen; het aparte instructeurskanaal-bestand + de `rubric_scores`-DuckLake-tabel (2 rijen, direct+design) zijn de enige plek met een score. `/instructor/dashboard` en `/admin.html` apart bezocht ter bevestiging dat de nieuwe routing Task 4/9's bestaande schermen niet breekt (`Dashboard.tsx` rendert nu voor het eerst via een échte route i.p.v. een tijdelijke `main.tsx`-repoint; `admin.html` blijft volledig zelfstandig, eigen foutmelding als zijn eigen server niet draait, niet gebroken door de routingwijziging).

### Commit & push: Task 5
- [x] Commit met boodschap die refereert aan FR-05/AC-03/AC-05. ✅ Commit `25d1261`.
- [x] Push naar `oe-gate-system` `main`. ✅ Gepusht naar https://github.com/businessdatasolutions/oe-gate-system (`c18c28f..25d1261`).
- [x] Vink af: **Main Task 5 afgerond** (12 juli 2026).

---

## Main Task 6: Kalibratie

*(Golf 5; vereist Task 5 gepusht en pilotdata.)*

**Bestanden:** Create: `data/calibration/report.md`, `data/calibration/threshold_analysis.py`

- [ ] **6.1** Schrijf `data/calibration/threshold_analysis.py`: leest pilot-cohort-data uit `rubric_scores`/`override_log`/`reviewring_log`, berekent AC-05/AC-07/AC-08-percentages.
- [ ] **6.2** Kalibreer de verborgen-rubriekdrempel op basis van de uitkomst; documenteer de gekozen waarde en de reden in `data/calibration/report.md`.
- [ ] **6.3** Kalibreer de embedding-similariteitsdrempel voor duplicaatdetectie (Task 8) op dezelfde manier, tegen AC-13.
- [ ] **6.4** Werk TDD Deel 14 en LRD Deel 12 bij (in de documentatierepository) met de definitieve, gekalibreerde waarden: dit vervangt de "nog te kalibreren"-vermeldingen.

### Test Gate: Task 6
- **Automatisch:** `threshold_analysis.py` draait zonder fouten en produceert reproduceerbare cijfers uit de pilotdata.
- **Mens-testbaar artefact:** `data/calibration/report.md` is leesbaar voor de module-eigenaar en bevat expliciete voor/na-drempelwaarden met onderbouwing; de docent kan het rapport lezen en de keuze beoordelen zonder de code te hoeven lezen.

### Commit & push: Task 6
- [ ] Commit (in beide repo's: `oe-gate-system` voor de code, documentatierepository voor de LRD/TDD-updates).
- [ ] Push beide.
- [ ] Vink af: **Main Task 6 afgerond**.

---

## Main Task 7: Deploymenthardening

*(7.1/7.4 mogen als scaffolding al vanaf golf 2 parallel starten; 7.6 pas ná Task 6.)*

**Bestanden:** Create: `infra/Dockerfile`, `infra/static_server.py`, `infra/cloudrun.yaml`, `infra/cloudbuild.yaml`, `infra/smoke_test.py`, `.github/workflows/deploy-gate-system.yml`. Modify: `data/smdl/ducklake.py` (managed-catalogusmodus), `pyproject.toml` (`pg8000`), `design-documents/tdd-operational-excellence.html` (Deel 10.2/10.3/14).

**Scope-correctie tijdens de bouw (7.2).** Deze task is als enige begonnen met een expliciet in het TDD opengelaten risico ("gemount volume of object-storage-koppeling", Deel 14). Bij uitwerking bleek de volume-variant niet onhandig maar **onveilig**: Cloud Storage FUSE kent geen bestandsvergrendeling en verliest gelijktijdige schrijfacties stilzwijgend ("the last write wins and all previous writes are lost", Cloud Run-documentatie), terwijl DuckDB vergrendeling vereist en een DuckDB-bestandscatalogus per ontwerp "limited to a single client" is (DuckLake-documentatie). Gekozen, ná expliciete voorlegging aan de opdrachtgever met alternatieven en kosten: **Postgres-catalogus + Parquet-data op GCS**, DuckLake's eigen ontwerpscheiding. Zie TDD 10.2. Dit maakt 7.2 groter dan oorspronkelijk gepland (het raakt de data-laag, niet alleen infra), maar lost een risico op dat anders pas bij een echt cohort zichtbaar was geworden — als verdwenen studentenwerk.

- [x] **7.1** Schrijf `infra/Dockerfile`: multi-stage build voor de Python-agentic-/data-laag. ✅ Eén Dockerfile, drie targets (`team_api`/`admin_api`/`frontend`) + `infra/static_server.py` (SPA-fallback, geen nieuwe framework-dependency). Alle drie de images gebouwd én gedraaid geverifieerd. **Twee echte bugs gevonden en gerepareerd tijdens die verificatie:** (a) beide servers defaulten `HOST` naar `127.0.0.1` (juist voor een dev-server, fataal in een container: Cloud Run's proxy verbindt van buiten de network namespace) — de CMD aliast nu `0.0.0.0`; bewezen met een counterfactual: zónder de fix bindt de container loopback en is `/api/health` van buiten onbereikbaar (HTTP 000), mét de fix HTTP 200. (b) Geen Postgres-driver in het image, terwijl 7.3's `DATABASE_URL` er een nodig heeft → `pg8000` toegevoegd (pure Python, geen libpq/compiler in `python:3.12-slim`). Daarnaast vastgelegd: Cloud Run draait uitsluitend amd64, terwijl `docker build` op deze Apple Silicon-machine arm64 produceert — `--platform linux/amd64` gedocumenteerd in de Dockerfile-header en expliciet in de workflow, plus `infra/cloudbuild.yaml` om native (niet via trage QEMU-emulatie) te bouwen.
- [~] **7.2** Configureer Cloud Run (regio `europe-west4`) met persistente opslag. **Half af — de DuckLake-helft is klaar, de OKF-helft niet.** Zie het blok "Nog open: OKF-bundel-duurzaamheid" onder deze task; live bewezen dat een checkpoint-inzending een redeploy niet overleeft. Wat wél af is: **niet** als gemount volume — zie de scope-correctie hierboven. `smdl/ducklake.py::connect` kreeg een env-geflagde managed modus: `OE_DUCKLAKE_CATALOG_DSN` (Postgres-catalogus, één `METADATA_SCHEMA` per lakehouse — DuckLake's gedocumenteerde isolatiemechanisme, dat "geen cross-team-catalogus" nu ook op databankniveau afdwingt) + `OE_DUCKLAKE_DATA_URI` (`gs://`, alleen-aangroeiende Parquet, dus nooit vergrendeling nodig). Signatuur ongewijzigd: alle ~25 aanroepers geven `lake.catalog_path, lake.tables_data_path` door, dus de namespace is af te leiden i.p.v. door te geven — blast radius bleef één functie. Dev/test/CLI blijven ongewijzigd op de bestandscatalogus (geen draaiende databank nodig). 10 nieuwe tests (`data/tests/test_ducklake_catalog.py`), waarvan 2 skippen zonder `OE_TEST_PG_DSN`. Gemeten, niet aangenomen: drie gelijktijdige processen, 24 schrijfacties, zonder retry → bestandscatalogus 9 ok/15 `IOException`, Postgres 24/24 met alle rijen terugleesbaar.
- [x] **7.3** Configureer een managed Postgres-instantie (zelfde regio) voor `DatabaseSessionService`. ✅ Cloud SQL `oe-catalog`, PostgreSQL 16, `db-f1-micro`, `europe-west4`, databank `oe_lakehouse`. **Herziening t.o.v. TDD 10.3's "gescheiden van de DuckLake-opslag":** sinds 7.2 leeft de DuckLake-catalogus óók in Postgres, dus sessiestate en catalogusmetadata delen één instantie, gescheiden op schema-niveau i.p.v. per instantie; de onderliggende eis (ander backup-/retentiebeleid) kan op schema-niveau, een tweede instantie zou voor één cursus alleen kosten toevoegen. De domeindata (Parquet) staat nog steeds volledig buiten Postgres. TDD 10.3 bijgewerkt.
- [x] **7.4** Schrijf `.github/workflows/deploy-gate-system.yml`: aparte workflow van de wiki-pijplijn. ✅ Workload Identity Federation (geen langlevende SA-sleutel in de repo), `pytest` als deploy-gate, en een **volgorde die load-bearing is, niet stilistisch**: Vite bakt `VITE_*` build-time in de bundel, dus API's eerst deployen → echte URL's teruglezen → frontend daartegen bouwen → daarna de CORS-origin van beide API's op de frontend-URL zetten (beide servers defaulten naar `localhost:5173`; zonder die laatste stap weigert de browser elke call). Raakt de wiki-pijplijn nergens (NFR-02).
- [ ] **7.5** Bevestig dat Cloud Logging automatisch stdout/stderr van de container opvangt naast het eigen `action_log` (TDD Deel 10.6): geen extra configuratie nodig, wel te verifiëren.
- [ ] **7.6** Draai een smoke-test tegen de gedeployde omgeving: health-check-endpoint + één volledige gate-cyclus (hergebruik het scenario uit Task 5's integratietest, nu tegen de live URL). `infra/smoke_test.py` geschreven (stdlib-only, draait op een kale runner); de load-bearing assertie is een **round trip**, niet een 200: een schrijfactie die 200 teruggeeft maar niet terugleesbaar is, is precies de FUSE-faalmodus waar 7.2 omheen ontworpen is.

### Nog open: OKF-bundel-duurzaamheid (7.2, tweede helft)

**Live bewezen probleem, gevonden door 7.6's smoke-test tegen de echte deploy — niet door redeneren.** Een lakehouse bestaat uit twéé dingen (TDD Deel 4.1): DuckLake-tabellen én de OKF-bundel (`okf_bundle/<type>/<naam>.md`, gewone markdown). 7.2 loste de eerste helft op en liet de tweede stilzwijgend staan. Maar een checkpoint-inzending is een **OKF-concept**, geen DuckLake-rij: `submit_checkpoint_answer` schrijft `okf_bundle/checkpoint-submission/<...>.md` via `Path.write_text` naar `OE_LAKEHOUSE_ROOT` — in Cloud Run dus het **efemere containerbestandssysteem**.

Reproductie tegen de draaiende dienst (`oe-team-api`, team `smoke-a9c327fb`):

1. `POST /api/submit-checkpoint` → HTTP 200, pad `/app/lakehouse/2026-2027-oe-team-smoke-a9c327fb/okf_bundle/checkpoint-submission/…md`
2. `GET /api/checkpoint-view` → `has_submission: true`
3. nieuwe revisie geforceerd (nieuwe container = nieuw bestandssysteem)
4. `GET /api/checkpoint-view` → **`has_submission: false`**

De bucket is op dat moment nog volledig leeg (`gcloud storage ls` → geen objecten): deze flow raakt de GCS-datapath niet eens. **Studentenwerk overleeft een deploy dus niet.** Dit is exact het risico dat TDD Deel 14 benoemde, alleen op de laag waar ik het aanvankelijk niet zocht: ik heb de catalogus duurzaam gemaakt terwijl de eigenlijke inzendingen ephemeer bleven.

Openstaande keuze (vraagt opdrachtgever-input, niet stilzwijgend te beslissen):
- **GCS FUSE-mount alleen voor `okf_bundle/`** — hier wél verdedigbaar waar het voor een DuckDB-catalogus dat niet was: elk concept is een eigen, één keer geschreven bestand, dus "last write wins" heeft geen twee schrijvers om te laten botsen. Aandachtspunt: `Bundle`'s eigen audit-log (`log_path`) wordt wél aangevuld en is dus wél een gedeeld bestand.
- **`okf.Bundle` via de GCS-API laten lezen/schrijven** — geen FUSE, wel een echte codewijziging in de OKF-laag.
- **Concepten naar DuckLake-tabellen verplaatsen** — grootste ingreep, raakt TDD Deel 4.1's hele "OKF-bundel als bronbestand"-model.

Tot dit is opgelost is de deploy **niet productiewaardig**, hoe groen de smoke-test verder ook is.

**Geprovisioneerd (GCP-project `operational-excellence-course`, `europe-west4`):** API's aan (run/artifactregistry/sqladmin/secretmanager/cloudbuild/storage), GCS-bucket `oe-lakehouse-operational-excellence-course` (versioning aan), Artifact Registry `oe-gate`, Cloud SQL `oe-catalog` + databank `oe_lakehouse`, service-account `oe-runtime@` met least-privilege-rollen (`storage.objectAdmin` **alleen op die ene bucket**, `cloudsql.client`, `secretmanager.secretAccessor` op vier met naam genoemde secrets), en vijf Secret Manager-entries (`oe-db-password`, `oe-catalog-dsn`, `oe-database-url`, `oe-gcs-hmac-key-id`, `oe-gcs-hmac-value`). De GCS HMAC-sleutel is nodig omdat DuckDB `gs://` uitsluitend via de S3-compatibiliteits-API bereikt; een service-account volstaat daar niet.

### Test Gate: Task 7
- **Automatisch:** CI/CD-pipeline is groen op een test-PR; smoke-test-script tegen de live URL slaagt.
- **Mens-testbaar artefact:** een mens bezoekt de gedeployde frontend-URL in een browser en doorloopt handmatig één checkpoint-indiening tegen de echte, gedeployde omgeving (niet lokaal).

### Commit & push: Task 7
- [ ] Commit met boodschap die refereert aan NFR-02/NFR-04/Deel 10.
- [ ] Push naar `oe-gate-system` `main`.
- [ ] Vink af: **Main Task 7 afgerond**.

---

## Main Task 8: Wiki Quality Check Agent (additief, parallelliseerbaar)

*(Golf 3, parallel aan Task 3/4/9; vereist Task 2 gepusht.)*

**Bestanden:** Create: `agents/wiki_quality_check/agent.py`, `agents/wiki_quality_check/wiki_quality_check.evalset.json`, `agents/tests/test_wiki_quality_check.py`

- [x] **8.1** Implementeer de tool `extract_contribution(synthesis_text) -> ContributionCandidate` (LLM-stap, gestructureerde output: concepten/entiteiten/bronnen/relaties). ✅ Gemini-structured-output (`gemini-2.5-flash-lite`, `response_schema=ContributionCandidate`) wanneer `GEMINI_API_KEY`/`GOOGLE_API_KEY` beschikbaar is, anders een deterministische heuristische markdown-parser (zelfde graceful-degradation-vorm als `data/smdl/ingest_pdf.py`). Deze omgeving had geen API-key, dus de heuristische fallback is wat daadwerkelijk is uitgevoerd/getest.
- [x] **8.2** Implementeer de tool `check_duplicates(candidate) -> DuplicateResult`, deterministisch: exacte titelmatch + embedding-similariteit (Gemini text-embedding-model) tegen een instelbare drempel (placeholder-waarde, kalibratie volgt in Task 6). ✅ Exacte titelmatch (via `smdl.storage.slugify`) tegen de échte `oe-wiki/wiki/`-inhoud (66 pagina's gescand: 19 boekconcepten + 5 bedrijfscases + 23 Lean-tool-referenties + 19 AI-Wiki-pagina's). Similariteitscheck: Gemini `text-embedding-004` wanneer een API-key beschikbaar is (wél gebouwd, hier ongebruikt), anders een dependency-vrije TF-IDF/cosinus-fallback. **Expliciet een placeholder, niet de definitieve aanpak** (TDD Deel 14): zowel de drempelwaarde (0,55) als de embeddingmodelkeuze wachten op kalibratie in Task 6/AC-13.
- [x] **8.3** Implementeer de tool `validate_citations(candidate) -> CitationResult`, deterministisch: elke bron moet auteur/jaar/titel/uitgever-of-URL bevatten (gestructureerde invoer, geen vrije-tekst-parsing). ✅ Werkt uitsluitend op de al-gestructureerde `SourceCandidate`-velden; ontbrekende bron → `missing_citation`, onvolledige bron → `invalid_citation`.
- [x] **8.4** Implementeer de tool `compute_points(candidate, duplicate_result, citation_result) -> PointsBreakdown`, 1 punt/concept, 1 punt/entiteit, 1 punt/bron, 0,5 punt/relatie (LRD 6.11); schrijft naar `wiki_quality_log` (Task 2), nooit een los bijgehouden totaal. ✅ `points_total` is een `@computed_field` (pydantic), afgeleid van de vier ruwe tellingen, structureel niet instelbaar/overschrijfbaar, exact consistent met TDD Deel 7.1's eigen uitgewerkte voorbeeld (tellingen 2+1+3+1 → totaal 6,5 klopt alleen als de 0,5-wegingsfactor bij het lezen wordt toegepast, niet in de opgeslagen telling zelf). Bonus-helper `query_team_points_total` herberekent het team-totaal altijd via een live SQL-`SUM`-query, nooit een gecachete teller.
- [x] **8.5** Koppel de agent-aanroep in de Synthesize-pijplijn: vlak vóór de git-commit die een teamcase publiceert (TDD Deel 3.6), ná gate A/B, geen invloed op gate-status. ✅ `run_quality_check()` in `agent.py` ketent de vier tools in de vaste volgorde als één aanroepbare eenheid, met een docstring die exact documenteert waar de (nog niet gebouwde) Review-Ring Coordinator dit zou aanroepen: vlak vóór de `wiki/syntheses/`-git-commit, ná gate A/B, zonder invloed op gate-status.
- [x] **8.6** Schrijf `agents/wiki_quality_check/wiki_quality_check.evalset.json` met cases voor: exact duplicaat (afwijzen), inhoudelijk duplicaat (afwijzen), ontbrekende bronvermelding (afwijzen), volledig geldige bijdrage (accepteren + correcte puntentelling). ✅ Alle vier cases aanwezig (inhoudelijk-duplicaat-case is een zwaar geparafraseerde, deels verbatim-overlappende versie van de echte Toyota-pagina, scoort 0,61 tegen de 0,55-drempel). Primair geverifieerd via een deterministisch testharnas (geen live modelaanroep nodig, consistent met de never-invents-eis); een aparte `@pytest.mark.eval`-test speelt hetzelfde bestand af via de echte `AgentEvaluator` en skipt netjes zonder API-key.
- [x] **8.7** Pas de `@log_action`-decorator uit Task 2.10 toe op alle vier tools uit 8.1–8.4 (NFR-08). ✅ Toegepast en functioneel geverifieerd (niet alleen `__wrapped__`-aanwezigheid): elke tool-aanroep schrijft een `action_log`-regel, en één volledige pijplijn-run deelt één `correlation_id` over alle vier de stappen (TDD 4.8/5.6).

### Test Gate: Task 8
- **Automatisch:** ✅ `.venv/bin/python -m pytest agents/tests/test_wiki_quality_check.py -v`: **33/33 geslaagd, 1 geskipt** (de live-`AgentEvaluator`-test, correct geskipt zonder `GEMINI_API_KEY`/`GOOGLE_API_KEY`). Volledige repo-testsuite: 126 geslaagd, 1 geskipt.
- **Mens-testbaar artefact:** ✅ `agents/wiki_quality_check/demo_submit.py` daadwerkelijk drie keer gedraaid met drie verschillende testbijdragen: `--scenario exact-duplicate` (hergebruikt de titel "Toyota", exact zoals in `wiki/sources/toyota.md`) → **REJECTED / reject_reason=duplicate / 0 punten**; `--scenario missing-citation` → **REJECTED / reject_reason=missing_citation / 0 punten**; `--scenario valid` → **ACCEPTED / 5,5 punten** (2 concepten + 1 entiteit + 2 bronnen + 1 relatie×0,5); alle drie de afwijzingsredenen/puntentotalen kloppend in de daadwerkelijke terminal-output gecontroleerd.

### Commit & push: Task 8
- [x] Commit met boodschap die refereert aan FR-21/AC-13. Commit `e9ba394`.
- [x] Push naar `oe-gate-system` `main`. ✅ Gepusht naar https://github.com/businessdatasolutions/oe-gate-system (`c46301f..e9ba394`).
- [x] Vink af: **Main Task 8 afgerond** (12 juli 2026).

---

## Main Task 9: Admin-onderdeel voor cursusvoorbereiding (additief, parallelliseerbaar)

*(Golf 3, parallel aan Task 3/4/8; vereist Task 2 gepusht.)*

**Bestanden:** Create: `agents/admin_api/routes.py`, `frontend/src/routes/admin/CoursePeriod.tsx`, `frontend/src/routes/admin/Teams.tsx`, `frontend/src/routes/admin/Assessments.tsx`, `frontend/src/routes/admin/DocumentUpload.tsx`, `agents/tests/test_admin_crud.py`

- [x] **9.1** Implementeer CRUD-endpoints voor `course-period` (TDD Deel 4.9/7.1): create/read/update in het cohort-brede admin-lakehouse. ✅ `create_course_period`/`get_course_period`/`update_course_period` in `agents/admin_api/routes.py`, singleton per cohort via `Bundle.write_singleton` op `Lakehouse.for_admin`. `delete_course_period` is er ook (zie 9.6, die expliciet delete-tests voor **alle drie** typen vraagt, breder dan 9.1's eigen create/read/update-tekst), toegelicht in de docstring, niet stilzwijgend toegevoegd.
- [x] **9.2** Implementeer CRUD-endpoints voor `team-roster`: teams aanmaken/wijzigen/verwijderen, studenten (voornamen) koppelen/ontkoppelen. ✅ `create_team`/`get_team`/`list_teams`/`update_team`/`delete_team`, plus expliciete `add_team_member` ("koppelen") en `remove_team_member` ("ontkoppelen"). Privacy structureel afgedwongen (LRD Deel 1.3/TDD Deel 9): een voornaam met een spatie wordt geweigerd door `_validate_first_names`, niet slechts een UI-conventie.
- [x] **9.3** Implementeer CRUD-endpoints voor `assessment-schedule`: datum/tijdslot/locatie/docent per CBI. ✅ `create_assessment`/`get_assessment`/`list_assessments`/`update_assessment`/`delete_assessment`, event-log-stijl via `Bundle.write_new` (meerdere CBI-slots per klas mogelijk). Elk record adresseerbaar via zijn bestandsrelatieve id, met een path-traversal-guard op die id (apart getest: een kwaadaardige `assessment_id` als `../../etc/passwd` wordt geweigerd).
- [x] **9.4** Koppel `team-roster`-wijzigingen aan het bestaande fallback-mechanisme uit Task 4.3 (een teamwissel ná cohortstart wordt behandeld als ring-uitval, geen apart pad). ✅ Begon als expliciet gedocumenteerd integratiepunt (`_notify_team_roster_changed()`) omdat Task 4 nog niet bestond toen dit gebouwd werd: probeert `review_ring_coordinator.workflow` te importeren en degradeert netjes naar een gemarkeerde `PENDING_TASK_4_INTEGRATION`-status als dat faalt, in plaats van te blokkeren of een eigen ring-herconfiguratie te bouwen. Task 4 landde gedurende deze build; het integratiepunt is toen voor het echt verbonden aan de daadwerkelijke `find_ring_for_team`/`reconfigure_ring` (5-parameter-signatuur, live afgestemd met de Task 4-agent; zie hun eigen 4.3-notitie hierboven). Bewezen met een echte end-to-end-test tégen Task 4's eigen module (geen mock): een team verwijderen ná cohortstart verkleint een echte 3-team-ring naar 2 op schijf, met `reconfigured_from` gezet.
- [x] **9.5** Bouw de drie admin-frontendschermen (`CoursePeriod.tsx`, `Teams.tsx`, `Assessments.tsx`): pure formulieren, geen agent-aanroep. ✅ Alle drie gebouwd, plus `DocumentUpload.tsx` (9.8) en een lichte `AdminApp.tsx`-tabnavigatie (geen routerbibliotheek voor vier schermen, KISS). Eigen Vite-ingang (`frontend/admin.html` → `src/routes/admin/main.tsx`) in plaats van het gedeelde `index.html`/`App.tsx`/`vite.config.ts` aan te raken, om niet te botsen met Task 3/4/5's gelijktijdige routes in diezelfde bestanden: cross-origin `fetch` (CORS) naar `admin_api/server.py` in plaats van een dev-server-proxy, zelfde reden.
- [x] **9.6** Schrijf `agents/tests/test_admin_crud.py`: create/read/update/delete-tests voor alle drie de concept-types. ✅ 36 tests: volledige CRUD voor alle drie typen (incl. delete voor `course-period`), de 9.4-ringfallback-integratie (incl. een echte end-to-end-run tégen Task 4's module, niet alleen een mock), en de 9.7-uploadroutering (team/ring/klas via de data-laag, "iedereen" nooit).
- [x] **9.7** Implementeer het upload-endpoint voor instructeur-documenten (FR-20, TDD Deel 6.4): accepteert PDF/Excel/CSV + een scope-keuze (team/ring/klas/iedereen), roept bij team/ring/klas de routering uit Task 2.11 aan; bij scope "iedereen" commit het endpoint het geconverteerde bestand naar de wiki-repo (Task 1) via een git-commit, niet via de data-laag. ✅ `upload_instructor_document()` roept voor team/ring/klas `service.ingest_upload()` aan (die zelf `route_instructor_document()` uit Task 2.11 gebruikt: hergebruikt, niet herbouwd). Voor scope "iedereen": de daadwerkelijke git-commit náár de wiki-repo is **niet** uitgevoerd in deze task: deze task had geen schrijftoegang om een commit in de aparte `oe-wiki`-repo te orkestreren. Expliciet gedocumenteerd, niet stilzwijgend overgeslagen: `commit_to_wiki_repo()` valideert de staging en geeft een gemarkeerde `PENDING_WIKI_INTEGRATION`-uitkomst terug (zichtbaar in de UI én gelogd via `action_log`), met een `# TODO`-verwijzing naar het vervolgintegratiepunt. De routeringsbeslissing zelf (team/ring/klas → data-laag; iedereen → nooit data-laag) is wél volledig geïmplementeerd en getest (`test_upload_everyone_scope_never_touches_lakehouse`).
- [x] **9.8** Bouw `frontend/src/routes/admin/DocumentUpload.tsx`: bestandskiezer + scope-selector; toont vóór bevestiging expliciet welke teams het zullen zien, met een aparte, zwaardere bevestigingsstap bij scope "iedereen" (LRD 6.10, onomkeerbaar publiek). ✅ Live audience-preview (`preview_audience()`) toont per scope-keuze welke teams het zien (voor "klas": elk daadwerkelijk aangemaakt team in die klas, geen aparte `klas-roster`-afhankelijkheid nodig). Voor "iedereen": een native `<dialog>`-modal (niet `window.confirm()`) met expliciete "onomkeerbaar publiek"-tekst en een bevestigingscheckbox die de publiceerknop geblokkeerd houdt tot aangevinkt; handmatig doorlopen in een echte browser (zie Test Gate hieronder).
- [x] **9.9** Pas de `@log_action`-decorator uit Task 2.10 toe op de admin-CRUD-endpoints en het upload-endpoint (NFR-08). ✅ Elke publieke functie in `routes.py` gedecoreerd met `@log_action(layer="data")`; interne existence-check-helpers bewust ongedecoreerd om dubbele logregels per request te voorkomen. Live geverifieerd tijdens de browser-walkthrough: 38 `action_log`-regels met `actor_type=instructor`, stuk voor stuk `outcome=success`.

### Test Gate: Task 9
- **Automatisch:** ✅ `.venv/bin/python -m pytest agents/tests/test_admin_crud.py -v`: **36/36 geslaagd**. (Extra, niet vereist door deze test-gate-regel maar gedraaid en groen: `agents/tests/test_admin_server.py`, 18 tests voor twee beveiligingsfixes gevonden tijdens deze build; zie hieronder. Volledige repo-testsuite `pytest -q`: 146 geslaagd, 1 geskipt.)
- **Mens-testbaar artefact:** ✅ Volledige handmatige doorloop in de daadwerkelijk draaiende UI (`npm run dev` in `frontend/` + `.venv/bin/python -m admin_api.server`, aangestuurd via browser-automation): een cohortperiode ingesteld (`2026-2027-oe`, 17-08-2026 t/m 04-12-2026, incl. één vakantieperiode "Herfstvakantie"), een team `team-07` aangemaakt met twee voornamen (Fenna, Timo), één assessment gepland (Fenna/team-07, 08-12-2026, 13:00–13:30, Arnhem) en één testdocument geüpload met scope "team": alle vier bevestigd correct terug ná een échte paginaherlaad, gecontroleerd zowel in de UI als rechtstreeks op schijf (de daadwerkelijke OKF-markdown-bestanden in `lakehouse/`, nog steeds aanwezig ná een herstart van de Python-server, dus daadwerkelijk persistent, niet alleen in front-end state). Een tweede upload met scope "iedereen" toonde daadwerkelijk de aparte, zwaardere bevestigingsdialoog (native `<dialog>`, publiceerknop uitgeschakeld tot de bevestigingscheckbox is aangevinkt); ná bevestiging gaf het systeem eerlijk `PENDING_WIKI_INTEGRATION` terug zonder ook maar één lakehouse aan te raken (apart geverifieerd op schijf: geen nieuwe namespace-map). **Onderweg gevonden en gerepareerd, niet genegeerd:** (1) een race condition in `CoursePeriod.tsx` waarbij een trage mount-time-fetch in-progress formulierinvoer kon overschrijven, gefixt met een dirty-tracking-ref; (2) een latent bug in Task 2's `data/smdl/ducklake.py` (een nullable string-kolom werd door DuckDB als `INTEGER` geïnfereerd bij een all-None eerste rij, waardoor een latere rij met een echte string faalde), gefixt, en Task 8 paste onafhankelijk dezelfde fix toe op hun eigen `wiki_quality_log`-kolom na een tip hierover; (3) twee beveiligingsproblemen die een geautomatiseerde review tijdens deze build meldde in `admin_api/server.py` (path traversal via het upload-bestandsnaam-pad, een CORS-wildcard op een endpoint dat state-changing requests accepteert), beide gerepareerd met eigen regressietests (`test_admin_server.py`) en live tegen de draaiende server bevestigd via `curl`.

### Commit & push: Task 9
- [x] Commit met boodschap die refereert aan FR-20/FR-22/NFR-08. ✅ Commit `c18c28f`.
- [x] Push naar `oe-gate-system` `main`. ✅ Gepusht naar https://github.com/businessdatasolutions/oe-gate-system (`5b52bae..c18c28f`).
- [x] Vink af: **Main Task 9 afgerond** (12 juli 2026).

---

## Main Task 10: Studentdossier + AI-CBI-voorbereiding (additief, parallelliseerbaar)

*(Golf 4b, parallel aan Task 5; vereist Task 4 én Task 8 gepusht.)*

**Bestanden:** Create: `agents/dashboard_query/prep_agent.py` (uitbreiding van Task 4.6's agent), `frontend/src/routes/instructor/StudentDossier.tsx`, `agents/tests/test_assessment_prep.py`

- [x] **10.1** Implementeer de tool `build_student_timeline(first_name, team_id) -> list[TimelineEvent]`, deterministisch: query's over `gate_events`, `wiki_quality_log`, `reviewring_log`, gesorteerd op tijdstempel, met gebruik van DuckLake's snapshot-tijdreizen (TDD Deel 4.3). ✅ Geïmplementeerd in `agents/dashboard_query/prep_agent.py`; loopt daadwerkelijk door élke DuckLake-catalogussnapshot (`lake.snapshots()` + `AT (VERSION => n)`) i.p.v. één latest-snapshot-`SELECT *`, niet decoratief: `reviewring_log`-rijen hebben geen eigen `ts`-kolom (`ducklake.ReviewringLogRow`), dus de DuckLake-snapshot is de enige tijdstempel die dat gebeurtenistype heeft. Elk `source_ref` heeft exact TDD 5.8's eigen voorbeeldvorm (`gate_events#id`): "id" is de echte, zelfstandig herquery-bare `snapshot_id` (`SELECT * FROM lake."<table>" AT (VERSION => <id>)`).
- [x] **10.2** Implementeer de tool `generate_prep_summary(timeline) -> list[PrepPoint]`, LLM-stap; elk `PrepPoint` heeft een **verplicht** `source_ref`-veld (bv. `gate_events#<id>`). Een `PrepPoint` zonder geldig `source_ref` wordt binnen de tool zelf weggefilterd vóórdat het wordt teruggegeven: schema-afdwinging, geen instructie. ✅ Gemini structured output wanneer een API-key beschikbaar is (niet het geval in deze omgeving, zelfde situatie als Task 3/8), anders een deterministische regelgebaseerde generator. Ongeacht welk pad: `_filter_valid_points` verwijdert **onvoorwaardelijk**, binnen de functie zelf, elk kandidaat-punt wiens `source_ref` niet exact overeenkomt met een echte gebeurtenis in de meegegeven tijdlijn: bewezen met een adversarial test die de generator monkeypatcht om een gehallucineerde `source_ref` te injecteren en bevestigt dat die nooit de return-waarde van `generate_prep_summary` zelf bereikt. `PrepPoint`/`TimelineEvent` zijn `extra="forbid"`-schema's zonder enig scoreveld (zelfde structuur als `socratic_tutor.schemas.TeamFacingReport`) en `source_ref` is verplicht + non-empty afgedwongen bij constructie (kan niet eens gebouwd worden zonder).
- [x] **10.3** Bouw `frontend/src/routes/instructor/StudentDossier.tsx`: tijdlijnweergave + voorbereidingsoverzicht, elk `PrepPoint` toont zijn `source_ref` als klikbare/zichtbare referentie (NFR-10 moet zichtbaar zijn, niet alleen intern aanwezig). ✅ Gebouwd als `frontend/src/routes/instructor/dossier/StudentDossier.tsx`: eigen submap (zoals de Parallelisatie-overzicht-tabel hierboven al aangaf), raakt bewust geen `index.html`/`main.tsx`/`App.tsx` (Task 5's gelijktijdige integratiewerk daar, bevestigd met de Task 5-agent). Elke `PrepPoint` toont zijn `source_ref` als een echte `<a href="#...">`-citatielink die naar de exacte `TimelineEvent`-kaart in de tijdlijn springt en die daar met `:target`-CSS oplicht, niet alleen aanwezig in de JSON. Mockdata is verbatim gevangen uit een echte `demo_prep.py`-run (zelfde "zelfde demo, zelfde data"-conventie als `Dashboard.tsx`). `tsc --noEmit -p tsconfig.app.json` en `oxlint` beide schoon.
- [x] **10.4** Bevestig instructor-only-toegang: dezelfde toegangsscheiding als `rubric_scores`: geen teamgericht kanaal geeft ooit toegang tot dit dossier. ✅ Dezelfde code-pad-scheiding als Task 3's `rubric_scores`: de drie nieuwe tools zitten uitsluitend in `dashboard_query.prep_agent`/`agent.py`; `socratic_tutor` (het enige teamgerichte kanaal in deze codebase) heeft geen enkele import ervan: geverifieerd via echte `ast`-parsing van importstatements (niet een kwetsbare substring-scan op docstrings), in beide richtingen getest.
- [x] **10.5** Schrijf `agents/dashboard_query/prep_agent.evalset.json`: elke gegenereerde `PrepPoint` in de evalset-cases moet een geldig `source_ref` hebben, anders faalt de test (AC-14). ✅ 5 cases, elk afgeleid uit een échte run van de deterministische pipeline (niet met de hand getypt): gate-progressie, wiki-weigering-dan-acceptatie, een vervallen peer review, een pending+override-paar, en een gemengde volledige geschiedenis over alle drie brontabellen. 100%-source_ref-dekking bevestigd; een aparte adversarial test bewijst dat de checker zelf een ongeldige case daadwerkelijk zou laten falen (niet triviaal altijd slaagt).
- [x] **10.6** Schrijf `agents/tests/test_assessment_prep.py`: test dat een `PrepPoint` zonder `source_ref` nooit de tool-output bereikt. ✅ 38 tests. De kernbewering is op twee niveaus bewezen: puur tegen `_filter_valid_points` met adversarial input, én end-to-end tegen de publieke `generate_prep_summary` zelf via een monkeypatchte generator die een gehallucineerde `source_ref` injecteert; beide bevestigen dat zo'n punt de return-waarde nooit bereikt.
- [x] **10.7** Implementeer `export_student_history(first_name, team_id) -> bytes` (FR-09): rendert de volledige tijdlijn uit 10.1 (gate-geschiedenis, iteraties, feedback) naar PDF of doc, ten behoeve van het criterion-based interview (LRD Deel 9): hergebruikt de append-only `log.md`/tijdlijndata, geen apart bijgehouden exportformaat. ✅ Hergebruikt `build_student_timeline` rechtstreeks (geen apart exportmechanisme); rendert naar PDF via `fpdf2` (nieuwe afhankelijkheid: `pypdf`, al aanwezig, leest/samenvoegt bestaande PDF's maar heeft geen tekst-auteurs-API). Geverifieerd door de output terug te lezen via `pypdf` en te bevestigen dat de geëxtraheerde tekst de echte team_id/source_refs bevat.
- [x] **10.8** Pas de `@log_action`-decorator uit Task 2.10 toe op `build_student_timeline`, `generate_prep_summary` en `export_student_history` (NFR-08). ✅ Alle drie gedecoreerd (`layer="agentic"`); succes- én foutpad apart getest per tool.

### Test Gate: Task 10
- **Automatisch:** ✅ `.venv/bin/python -m pytest agents/tests/test_assessment_prep.py -v`: **38/38 geslaagd**. Volledige repo-testsuite (`pytest -q`): **198 geslaagd, 1 geskipt** (uitsluitend de vooraf-bestaande live-`AgentEvaluator`-skip zonder `GOOGLE_API_KEY`, zelfde als Task 3/8). Evalset: 100% source_ref-dekking bevestigd, plus een adversarial test die aantoont dat de check echt zou falen op een ongeldige case.
- **Mens-testbaar artefact:** ✅ `.venv/bin/python -m dashboard_query.demo_prep --seed --first-name Fenna --team-id team-07 --export ...pdf` daadwerkelijk gedraaid (zelfde team-07/Fenna-identiteit als Task 9's eigen walkthrough, via de nieuwe `data/smdl/demo_seed.py::seed_student_dossier_demo()`): 6 tijdlijngebeurtenissen over alle drie brontabellen en twee D-fasen, 6 CBI-voorbereidingspunten elk direct naast het exacte brondatapunt (ruwe `detail`-velden) waar het punt naar verwijst: met eigen ogen te controleren "klopt deze claim met deze data" zonder SQL nodig te hebben. AC-14-check en never-invents-check beide `True` in de daadwerkelijke terminal-output. FR-09-PDF-export (1928 bytes) geverifieerd door terug te lezen met `pypdf`: bevat dezelfde team_id/source_refs als de tijdlijn.

### Commit & push: Task 10
- [x] Commit met boodschap die refereert aan FR-09/FR-23/FR-24/NFR-08/NFR-10/AC-14. ✅ Commit `253b1be`.
- [x] Push naar `oe-gate-system` `main`. ✅ Gepusht naar https://github.com/businessdatasolutions/oe-gate-system (`25d1261..253b1be`).
- [x] Vink af: **Main Task 10 afgerond** (12 juli 2026).

---

## Main Task 11: HAN Huisstijl-ontwerpsysteem toepassen

*(Golf 5, vereist Task 5 én Task 10 gepusht, want retrofit raakt bestanden van beide. Niet parallelliseerbaar met iets anders in dezelfde golf.)*

**Waarom dit een aparte task is, later toegevoegd:** TDD Deel 6 noemt `design-documents/frontent-design/HAN Huisstijl Flightpath.dc.html` expliciet als "bindend ontwerp voor de eerste implementatie", maar dat is in dit buildplan nooit vertaald naar een uitvoerbare subtask: Tasks 4/5/9/10 kregen alleen functionele component-eisen (TDD Deel 6.1), geen visuele. Resultaat: elk scherm dat tot nu toe gebouwd is, gebruikt een generiek maar wél al semantisch CSS-custom-property-systeem (`--surface`, `--good`, `--warn`, `--neutral`, …): geen HAN-kleuren, geen Arial, geen logo. Omdat het al een tokensysteem is en geen hardcoded kleuren per component, is dit een **waarden-vervanging op één centrale plek**, geen herschrijving per scherm.

**Geëxtraheerde tokens uit de mockup** (`frontent-design/HAN Huisstijl Flightpath.dc.html`, direct gelezen, niet uit het geheugen):
- Merkroze `#e5007d` (knoppen, links, actieve fase-cirkel, Gate-B-accent, footer-CTA), hover `#a80060`
- Near-black `#1a1a1a` (tekst, headerrand, footerbalk, Socrates-avatar-cirkel)
- Grijstinten: paginabg `#f0f0f0`, kaartbg `#fafafa`/`#fff`, subtiele badge `#f5f5f5`, randen `#e2e2e2`/`#ddd`/`#eee`, gedempte tekst `#6b6b6b`/`#999`, footer-tekst `#d9d9d9`
- Semantisch: gate-open groen `#16a34a`/`#3a7a4e`; gate-pending gebruikt bewust **merkroze**, geen geel/oranje (`#e5007d`/`#a83e6c`); lichtroze badge-bg `#fce6f2`; footer-accent `#ff5fa8`
- Typografie: uitsluitend `Arial, Helvetica, sans-serif`, geen custom webfont; gewichten 800/700/400
- Vorm: 4px/6px border-radius, 1px randen, kaarten met optioneel 4px gekleurde linkerrand voor gate-status

**Bestanden:**
- Create: `frontend/src/styles/han-huisstijl-tokens.css` (of vergelijkbaar, centrale tokendefinitie), `frontend/src/components/HanHeader.tsx`, `frontend/src/components/PhaseTracker.tsx`, `frontend/src/components/NoGradeFooter.tsx`
- Modify: `frontend/src/routes/instructor/Dashboard.{tsx,css}` (Task 4), `frontend/src/routes/admin/*.{tsx,css}` (Task 9), `frontend/src/routes/team/CheckpointView.{tsx,css}` (Task 5), `frontend/src/routes/instructor/dossier/StudentDossier.{tsx,css}` (Task 10)

- [x] **11.1** Definieer de geëxtraheerde tokens hierboven als CSS custom properties op `:root` in `han-huisstijl-tokens.css`; importeer dit bestand als enige plek waar deze waarden letterlijk voorkomen. ✅ Alle geëxtraheerde tokens 1-op-1 overgenomen (hex-voor-hex, elk met een commentaarregel die naar het mockup-element verwijst), plus een apart, expliciet gelabeld tweede blok "EXTRAPOLATED tokens" voor functionele statussen die de mockup (één statische snapshot) niet toont: override-paars, attention/danger-rood, info-als-merkroze-link (letterlijk `a{color:#e5007d}` uit de mockup, geen verzinsel), gate-open-bg-tint, backdrop-dim. Geïmporteerd in beide Vite-entrypoints (`main.tsx` én `routes/admin/main.tsx`) zodat zowel de team/instructor-SPA als het losse admin-bundle dezelfde tokens hebben.
- [x] **11.2** Herwijs de bestaande custom properties (`--surface`, `--good`, `--warn`, `--neutral`, `--text`, `--text-muted`, …) in élk bestaand stylesheet naar de nieuwe HAN-tokens, waarden-vervanging, geen structuurwijziging van de bestaande componenten. ✅ `index.css` (globale `--text`/`--text-h`/`--bg`/`--border`/`--accent`/…), `Dashboard.css`, `CheckpointView.css`, `StudentDossier.css` allemaal herwezen. `admin.css` had géén eigen custom properties (leunde al op `index.css`'s tokens) maar wél ~13 losse hardcoded hex/rgba-waarden die nooit via een custom property liepen (success/warning/danger-kleuren, modal-backdrop); ook die herwezen, want de instructie "de ENIGE plek waar letterlijke hexwaarden voorkomen" liet daar geen ruimte voor uitzonderingen. **Eén echte bug gevonden en gerepareerd tijdens de screenshot-verificatie (zie Test Gate):** `admin.css`'s formuliervelden en de "iedereen"-bevestigingsdialoog hergebruikten het generieke `--bg`-token, dat vóór deze task toevallig wit was maar nu bewust de grijze HAN-paginaondergrond is: resultaat was onzichtbare grijs-op-grijs invoervelden. Gerepareerd door die twee regels rechtstreeks naar `var(--han-surface)` te wijzen.
- [x] **11.3** Bouw `HanHeader.tsx`: het roze-vlag-logomerk (rotated rectangle, exact zoals de mockup) + "HAN_UNIVERSITY OF APPLIED SCIENCES"-wordmark (roze underscore-accent) links, cursusnaam rechts, witte achtergrond, 4px zwarte onderrand. Eén gedeelde component, gebruikt op elke route (team/instructor/admin). ✅ Logomerk op de letterlijke mockup-pixelwaarden (52×40-doos, 54×15px roze rechthoek, -20° rotatie). **Sharing-beslissing (gedocumenteerd in de component zelf):** één gedeelde component, maar élk scherm rendert zijn EIGEN instantie (`Dashboard.tsx`/`CheckpointView.tsx`/`StudentDossier.tsx`/`AdminApp.tsx` elk hun eigen `<HanHeader/>`) in plaats van één instantie in `App.tsx`: noodzakelijk sowieso voor admin (apart Vite-bundle, geen gedeelde React-tree met de SPA), en ook handig voor de andere schermen omdat elk scherm zijn eigen rechter "contextnaam" toont (bv. "Instructor Dashboard", "Studentdossier", "Cursusvoorbereiding").
- [x] **11.4** Bouw `PhaseTracker.tsx`: de 4-fasen-cirkeltracker (Direct/Design/Deliver/Develop) met verbindingslijn, actieve fase roze-gevuld, inactieve fasen grijs-gevuld, team-initialen-cirkeltjes per fase: dit is het visueel meest onderscheidende element uit de mockup, bouw het nauwkeurig. Hergebruik in `Dashboard.tsx` (cohortbreed) én `CheckpointView.tsx` (één team); overweeg of dezelfde component met een `scope`-prop beide aankan of dat twee lichte varianten nodig zijn; jouw call, documenteer de keuze. ✅ Eén component met `scope: 'team' | 'cohort'`-prop (de door het buildplan zelf gesuggereerde route, gekozen boven twee losse varianten, reden gedocumenteerd in de component's module-doc). `scope="team"` (CheckpointView) reproduceert het volledige mockup-paneel (60px-cirkels, verbindingslijn, weekranges, avatar-cirkels onder de actieve fase, privacy-notitie) én behoudt CheckpointView's al bestaande fase-navigatie (echte `<Link>`'s naar andere stage-checkpoints van hetzelfde team): functionaliteit die deze retrofit niet mocht laten regresseren. `scope="cohort"` (Dashboard) is een compacte, niet-interactieve variant per teamkaart. **Gedocumenteerde, eerlijke afwijking:** de mockup toont avatar-cirkels ook onder een INACTIEVE fase (fase 2, TY/MI); de echte databron (`submitted_by`) rapporteert alleen leden van de HUIDIGE fase, dus avatars verschijnen hier alleen onder de actieve fase: geen verzonnen lidmaatschap voor fases waar de data niets over zegt. Kleine, gedocumenteerde toevoeging bovenop de letterlijke mockup: een groen vinkje-badge op "done"-fases (de mockup, één statische snapshot, hoefde nooit een afgeronde-maar-niet-actieve fase te tonen).
- [x] **11.5** Bouw `NoGradeFooter.tsx`: zwarte balk, lichtgrijze tekst, roze accent voor de "volgende stap"-boodschap: reproduceer de mockup's exacte formulering "Geen cijfer hier — alleen open of dicht" op elk team-gericht scherm waar dat relevant is (rechtstreekse echo van FR-05/AC-03, nu ook zichtbaar, niet alleen technisch afgedwongen). ✅ Mockup's exacte Nederlandse zin als component-default. Verankerd in `CheckpointView.tsx` (het enige team-gerichte scherm) met de ECHTE live `footer_note`/`next_phase_signal`-servervelden als props (server's eigen tekst is iets korter: "Geen cijfer zichtbaar hier — alleen open/dicht.", bewust niet overschreven, want dat is echte server-content). De roze CTA-tekst wordt puur presentationeel berekend uit al bekende velden (fase-volgorde + het systeem's vaste 2-gates-structuur); live geverifieerd in beide richtingen tijdens de screenshot-walkthrough: "→ Design is open" zodra beide gates open zijn, "→ Deliver opent bij 2/2 gates" terwijl nog pending.
- [x] **11.6** Retrofit `Dashboard.tsx`/`.css` (Task 4): `HanHeader`, tokens, kaartstijl met gekleurde linkerrand voor gate-status. ✅ Eigen `HanHeader`-instantie, tokens herwezen, teamkaart-linkerrand van 5px naar het HAN-token van 4px, en de oude platte-tekst-fase-pill vervangen door een compacte cohortbrede `PhaseTracker` per teamkaart.
- [x] **11.7** Retrofit de admin-schermen (Task 9): `HanHeader`, tokens: admin heeft geen `PhaseTracker`/`NoGradeFooter` nodig (geen team-gate-context). ✅ Eén `HanHeader`-instantie in `AdminApp.tsx` (gedeelde shell boven alle vier tabbladen), tokens herwezen (incl. de ~13 losse hex-waarden, zie 11.2). **Cross-screen-consistentiebeslissing:** admin's succes-/waarschuwingsstatussen hergebruiken nu dezelfde groen-/roze-tint-families als de team-gerichte gate-open/gate-pending-kleuren (was generiek groen `#188038`/amber `#b06000`): dezelfde kleur betekent nu overal in de app hetzelfde, niet alleen op de teamschermen.
- [x] **11.8** Retrofit `CheckpointView.tsx`/`.css` (Task 5): `HanHeader`, `PhaseTracker`, `NoGradeFooter`, Socrates-kaart (zwarte cirkel-avatar, geciteerde-vraag-stijl, roze CTA-knop), peer-partnerkaart, Gate A/B-kaarten met gekleurde linkerrand: dit scherm staat het dichtst bij de mockup zelf, vergelijk 1-op-1. ✅ Grootste herstructurering van de vier schermen: het oude ene gecombineerde `gate-status-card` met badge-chips vervangen door de mockup's eigen twee-rijen-layout (Socrates+peer-partnerrij, dan een aparte Gate-A/Gate-B-rij, elke gate zijn eigen kaart met 4px gekleurde linkerrand + statuszin). Socrates-kaart kreeg de zwarte cirkel-avatar + geciteerde-vraag-typografie; de echte "vraag de tutor"-actie is de kaart IN verplaatst en roze gestyled zoals de mockup's CTA: **bewust het echte label/gedrag behouden** in plaats van de mockup's letterlijke "Schrijf je antwoord →"-tekst te kopiëren, want er bestaat in deze build geen "schrijf een antwoord aan Socrates"-functionaliteit; die tekst zou een actie beloven die niet bestaat.
- [x] **11.9** Retrofit `StudentDossier.tsx`/`.css` (Task 10): `HanHeader`, tokens (geen `PhaseTracker`/`NoGradeFooter`: instructor-only, geen team-gate-weergave). ✅ Eigen `HanHeader`-instantie, tokens herwezen, incl. gedocumenteerde kleurfamilie-keuzes voor de 3 semantische kleuren die dit scherm gebruikt maar die geen 1-op-1 mockup-equivalent hebben (info=merkroze, want de mockup's eigen linkkleur IS merkroze; warn=gedempt pending-roze; attention=near-black, een bewuste echo van de mockup's eigen zwarte "geen cijfer"-footerbalk voor de guardrail-banner die dezelfde boodschap draagt).

### Test Gate: Task 11
- **Automatisch:** ✅ `.venv/bin/python -m pytest -q`: **198 geslaagd, 1 geskipt**, ongewijzigd t.o.v. de baseline vóór deze task (dezelfde vooraf bestaande live-`AgentEvaluator`-skip zonder `GOOGLE_API_KEY` als in Tasks 3/8/9/10; deze task raakt geen enkel Python-bestand). `npm run build` (`tsc -b && vite build`) en `npm run lint` (`oxlint`) allebei schoon, geen enkele waarschuwing.
- **Mens-testbaar artefact:** ✅ `npm run dev` + `.venv/bin/python -m team_api.server` + `.venv/bin/python -m admin_api.server` daadwerkelijk gedraaid tegen échte, al aanwezige geseede lakehouse-data (leftover van Task 5's eigen walkthrough: team-07/Ryanair/Fenna+Timo+Bram, beide gates open). Playwright-screenshots genomen van alle vier routes: inclusief `/instructor/dossier`, dat Task 10 bewust ongewired liet "als vervolgintegratiestap voor een latere task"; deze task IS die latere task, dus één route + één navlink toegevoegd aan `App.tsx` zodat het scherm daadwerkelijk te screenshotten was, plus twee extra CheckpointView-toestanden (team-07/design: groen done-vinkje + grijze not-started-gate + "dicht"-footervariant; team-09/direct: roze pending-gate + het lege inzendformulier) en twee admin-tabbladen, allemaal in dezélfde browser gescreenshot als de daadwerkelijke mockup-HTML zelf (lokaal via `python -m http.server`, want de browser-tool blokkeerde `file://`) voor een eerlijke, gelijke vergelijking. **Matcht zeer nauw:** header/logomerk/wordmark, de volledige fase-tracker (cirkels, lijn, weekranges, avatars, privacy-notitie), Socrates-kaart, Gate A/B-kaarten (open=groen, pending=roze, not-started=grijs, alle drie live geverifieerd), de zwarte footerbalk met roze CTA. **Bewust (nog) niet gereproduceerd, geen verzonnen data:** de mockup's "4 weken op rij ingeleverd"/"Nog 3 dagen"-badges en de mockup's specifieke vraagkop "Hoe concurreert IKEA?" (er bestaat geen "hoe concurreert deze case"-veld in het datamodel: de headline toont in plaats daarvan de echte `company_case`-waarde).

### Commit & push: Task 11
- [x] Commit met boodschap die refereert aan TDD Deel 6 ("bindend ontwerp"). ✅ Commit `e792608`.
- [x] Push naar `oe-gate-system` `main`. ✅ Gepusht naar https://github.com/businessdatasolutions/oe-gate-system (`253b1be..e792608`).
- [x] Vink af: **Main Task 11 afgerond** (12 juli 2026).

---

## Main Task 12: LRD-uitbreiding (onboarding, hybride leerarrangement, kwaliteitsmanagement)

*(Golf 7: mag direct starten, geen afhankelijkheid van Task 6/7. Geen coding-subagent: samen met de opdrachtgever direct in de HTML geschreven, zelfde patroon als het oorspronkelijke LRD/TDD-werk. Zie de brainstorm-beslissingen in `/Users/witoldtenhove/.claude/plans/can-we-start-building-compiled-narwhal.md`.)*

**Bestanden:** Modify: `design-documents/lrd-operational-excellence.html`

- [x] **12.1** Nieuw Deel "Onboarding-fase (week 0)": pedagogisch ontwerp van een pre-week-1-sequentie: verwachtingen, zelfassessment (reflectief, geen scoring/verdict), tooling-walkthrough (wiki/AI-tutor/gates), team-koppeling-reveal. Formuleer als nieuwe FR's, doorlopend genummerd na FR-24. ✅ Geschreven als Deel 6.14 (subsectie, geen top-level Deel; zie afwijking hieronder). FR-25.
- [x] **12.2** Nieuw Deel "Hybride leerarrangement": voeg een "vorm"-kolom toe aan de bestaande Deel-8-weektabel (hoorcollege / werkcollege / tutoring / zelfstudie-AI per week): planning + lichte in-sessie tools (aanwezigheid-check-in, live polls, wekelijkse vraag-thread), geen volledige sessiefacilitatie. Nieuwe FR's voor sessieplanning, aanwezigheid, in-sessie polls. ✅ Deel 6.15 + Deel 8 volledig herschreven naar 18 weken (2 periodes × 7 lesweken + 2 tentamenweken, bevestigd door de opdrachtgever) met een 5e kolom "Vorm". Bonusresultaat: Ch6+7 en Ch15+16 nu gekoppeld i.p.v. het oude Ch15-17-drietal in één week. Dit lost het al langer bekende Develop-capaciteitsprobleem op. FR-26/27/28.
- [x] **12.3** Nieuw Deel "Kwaliteitsmanagement & continue verbetering": pedagogiek van de survey (dimensies: bepaal hier definitief, bv. naar het model Informatie/Interactie/Inspiratie van `hanbedrijfskunde/retrospective` of cursus-eigen dimensies), waarom naam-gekoppeld (`first_name`+`team_id`, per gelogde fysieke sessie getriggerd) toch privacy-conform blijft, en de rol/grenzen van de AI-adviseur (instructor-only rapport, nooit een zichtbaar oordeel, mens houdt eindregie, analoog aan Socrates' never-scores-garantie). Nieuwe FR's. ✅ Deel 6.16. Dimensies vastgesteld: Informatie/Interactie/Toepasbaarheid (aangepast aan collegecontext i.p.v. 1-op-1 het workshopvoorbeeld). FR-29/30/31.
- [x] **12.4** Kleine toevoeging aan bestaande Deel 6.7 (dashboard): één zin over het team-niveau wiki-betrokkenheidssignaal (geaggregeerd, geen individuele tracking, expliciet consistent met NFR-05), met FR-verwijzing. ✅ Toegevoegd, verwijst naar 6.16/NFR-05.
- [x] **12.5** Ken de nieuwe FR/NFR/AC-nummers definitief toe en werk de bestaande FR/NFR/AC-tabellen (Deel 7/11) bij met elk nieuw nummer. ✅ FR-25–31, NFR-11–12, AC-15–16 toegevoegd aan Deel 7.1/7.2/11; grep-geverifieerd: 31/12/16 totaal, nul dubbele ID's.
- [x] **12.6** Docent-sanity-check met de opdrachtgever op de hybride weekindeling vóór afronding: dit is een inhoudelijke, geen technische, beoordeling. ✅ Voorgelegd (periode-indeling, weektabel, onboarding-scope, survey-dimensies): akkoord zonder wijzigingen.

**Afwijking van de letterlijke taakomschrijving, bewust en gemeld:** 12.1/12.2/12.3 zijn geschreven als subsecties **Deel 6.14–6.16** in plaats van drie nieuwe top-level Delen. Nieuwe top-level Delen tussen het bestaande Deel 6 en Deel 7 hadden Delen 7–13 en tientallen kruisverwijzingen (`Deel 8`, `Deel 11`, `Deel 13`, `§7.2`, …) door het hele document moeten hernummeren: te veel stilzwijgend-breekrisico voor een puur cosmetisch verschil. Deel 6 is al "de kern van dit document" (zie de "Hoe te lezen"-paragraaf), dus dit past inhoudelijk.

**Bijvangst tijdens het herschrijven:** naast de drie nieuwe Delen bleken ook Deel 2.1 (Bloom-leerlijn), Deel 6.2 (4D-navigatiediagram), Deel 6.4a, Deel 6.9, Deel 8.1 (wiki-ankertabel), Deel 9, Deel 10, Deel 13 en Bijlage A allemaal specifieke, nu-verouderde weeknummers te bevatten, stuk voor stuk gevonden via een grep-sweep ná de eerste edit-ronde (niet alles was in één keer compleet) en gecorrigeerd. Zie de test gate hieronder voor de verificatiemethode.

### Test Gate: Task 12
- **Automatisch:** ✅ grep-check bevestigt nul dubbele FR/NFR/AC-ID's en de verwachte totalen (31 FR / 12 NFR / 16 AC); een meerronde grep-sweep op "week N"/"wk N" vond en corrigeerde 6 aanvankelijk gemiste verouderde weekverwijzingen buiten Deel 8 zelf; `python3 html.parser` bevestigt geldige HTML; Deel 8's tabel telt exact 18 rijen (geverifieerd via script, niet handmatig geteld); lokale statische server + curl bevestigt HTTP 200 en de aanwezigheid van alle nieuwe Delen/FR's/NFR's in de geserveerde HTML.
- **Mens-testbaar artefact:** ✅ Voorgelegd aan de opdrachtgever in leesbare vorm (weektabel + drie nieuwe Delen): expliciet akkoord ontvangen, geen wijzigingen gevraagd.

### Commit & push: Task 12
- [x] Commit in de documentatierepository (`operational-excellence-course`), boodschap verwijst naar de nieuwe FR-reeks en naar het brainstorm-plan.
- [x] Push naar `main`.
- [x] Vink af: **Main Task 12 afgerond**.

---

## Main Task 13: TDD-uitbreiding (architectuur voor onboarding, hybride sessies, QMS, analytics, advisor)

*(Golf 8: vereist Task 12 gepusht. Geen coding-subagent, zelfde aanpak als Task 12.)*

**Bestanden:** Modify: `design-documents/tdd-operational-excellence.html`

- [x] **13.1** Nieuw agentcomponent beschrijven: **Course Quality Advisor Agent**: architectuur, tools, en het source_ref-traceerbaarheidspatroon (expliciet hergebruik van het bestaande never-invents-patroon uit Deel 5.7/5.8, niet opnieuw uitvinden). ✅ Deel 5.9. Draait per periode (niet per sessie, te weinig signaal anders), instructor-only, geen wiki-terugschrijfpad.
- [x] **13.2** Nieuwe datatypes toevoegen aan Deel 7 (Datamodel): `session` (type werkcollege/hoorcollege/tutoring, week, onderwerp, locatie, materiaal-link, tijd), `attendance-log`, `survey-response` (session_id, team_id/first_name, ratings per dimensie uit Task 12.3, vrije tekst), `wiki-engagement-aggregate` (team_id, week, paginabezoek-telling, team-niveau, geen individuele records). ✅ Ontwerpbeslissing gemaakt en gedocumenteerd (Deel 4.10): `session` is een OKF-concept (laagfrequent, docent-beheerd, zelfde patroon als `assessment-schedule`); `attendance_log`/`survey_responses`/`wiki_engagement` zijn DuckLake-tabellen (hoogfrequent, zelfde patroon als `action_log`/`override_log`). Daarnaast is `poll_responses` toegevoegd voor FR-28, niet expliciet genoemd in deze subtaak-tekst maar nodig voor volledige FR-traceerbaarheid.
- [x] **13.3** Nieuwe frontend-oppervlakken beschrijven in Deel 6: cursusbrede student journey-view, onboarding-flow, sessie-/roosterview met materiaal en check-in, live-poll-widget, post-sessie-survey-widget, docent-sessie-CRUD (admin-uitbreiding), quality-advisor-rapportview. ✅ Deel 6.7–6.11.
- [x] **13.4** Wiki-analytics-implementatiekeuze documenteren (aanbeveling: een van Quartz' zelf-hostbare, privacy-vriendelijke providers, bv. GoatCounter, plus een team-niveau rollup-job) in Deel 3 (wiki-laag) of Deel 10 (deployment), waar het beste past. ✅ Deel 3.7 (provider-config) + Deel 4.10 (rollup-job), GoatCounter aanbevolen, bevestiging bij Task 19-kickoff als open punt in Deel 14.
- [x] **13.5** Deployment-sequencing-notitie toevoegen aan Deel 10/13 (fasering): een "volledige course website" vraagt om echte hosting, dus Task 7 (deployment) kan eerder verweven worden dan pas na alle nieuwe features, als aanbeveling, geen harde eis. ✅ Toegevoegd aan Deel 13.
- [x] **13.6** FR/NFR/AC-kruistabel (Deel 8) bijwerken met elk nieuw FR/NFR/AC-nummer uit Task 12, gekoppeld aan het component uit 13.1–13.4. ✅ FR-25–31/NFR-11–12/AC-15–16 toegevoegd aan Deel 8.1/8.2; guardrails-tabel (Deel 11) en evaluatiestrategie (Deel 12.1) ook bijgewerkt.

### Test Gate: Task 13
- **Automatisch:** ✅ grep-diff FR/NFR/AC-kruiscontrole tussen LRD en TDD (script, niet handmatig): volledige match in beide richtingen, geen wees-nummers. Duplicate-ID-check binnen TDD: de enige herhaalde ID's (FR-20/NFR-08/NFR-09/AC-12) zijn pre-existente narratieve vermeldingen in een toelichtingskader, geen dubbele tabelrijen, apart geverifieerd. `python3 html.parser` + tag-balance-check bevestigen geldige HTML. Lokale statische server + curl bevestigt HTTP 200 en aanwezigheid van alle nieuwe secties.
- **Mens-testbaar artefact:** leesbare, renderende HTML, voorgelegd aan de opdrachtgever ter bevestiging vóór commit (zelfde patroon als Task 12.6).

### Commit & push: Task 13
- [x] Commit in de documentatierepository, boodschap verwijst naar de FR/NFR/AC-kruistabel-update.
- [x] Push naar `main`.
- [x] Vink af: **Main Task 13 afgerond**.

---

## Main Task 14: Sessie- & planningslaag

*(Golf 9, parallel aan Task 16/19: vereist Task 13 gepusht.)*

**Bestanden (in `oe-gate-system`):**
- Modify: `data/smdl/okf.py`, `data/smdl/ducklake.py`, `agents/admin_api/routes.py`, `agents/admin_api/server.py`
- Create: `frontend/src/routes/admin/Sessions.tsx`, `frontend/src/routes/team/Schedule.tsx`, `data/tests/test_session_concepts.py`

- [x] **14.1** Implementeer het `session`-OKF-concept-type in `okf.py` conform TDD Deel 7 (13.2): `session_id`, `type` (werkcollege|hoorcollege|tutoring), `week`, `onderwerp`, `locatie`, `materiaal_links`, `start_tijd`/`eind_tijd`, `cohort`/`klas_id`. ✅ `Session` toegevoegd als elfde OKF-concept-type (`CONCEPT_TYPES` ging van 10 naar 11), veldnamen letterlijk overgenomen uit TDD 7.1's eigen YAML-voorbeeld (`session_id, cohort, klas_id, kind, week, topic, location, materials, start_time, end_time`). **Let op:** deze buildplan-regel gebruikt Nederlandse prozanamen (`onderwerp`/`locatie`/`materiaal_links`/`start_tijd`/`eind_tijd`); de TDD's letterlijke YAML-frontmatter (Deel 7.1, gekopieerd in de opdracht van deze task) gebruikt de Engelse veldnamen die hierboven staan, dezelfde exacte-YAML-kopieerregel die elk ander concept-type in `okf.py` al volgt (zie bv. `assessment-schedule`). Leeft in het klas-lakehouse (`Lakehouse.for_klas`), zelfde tier als `assessment-schedule`. Getest: `data/tests/test_session_concepts.py`.
- [x] **14.2** Implementeer de `attendance-log`-DuckLake-tabel in `ducklake.py`: `session_id`, `team_id`/`first_name`, `checked_in_at`. ✅ `AttendanceLogRow` + `write_attendance_log`/`query_attendance_log`, append-only via `ducklake.append_rows()` (zelfde patroon als `override_log`/`wiki_quality_log`). Alleen de tabel zelf: het check-in-endpoint dat er daadwerkelijk naar schrijft is bewust Main Task 15's eigen scope ("Aanwezigheid & in-sessie tools"), hier niet gebouwd.
- [x] **14.3** Voeg CRUD-endpoints voor sessies toe aan `admin_api` (create/list/update/delete), analoog aan het bestaande course-period/teams/assessments-CRUD-patroon uit Task 9. ✅ `get_session`/`list_sessions`/`create_session`/`update_session`/`delete_session` in `agents/admin_api/routes.py` + bijbehorende HTTP-routes in `server.py` (`GET/POST/PUT/DELETE /api/sessions`). **Gedocumenteerde afwijking van het letterlijke "analoog aan assessments"-patroon:** `session` heeft, anders dan `assessment-schedule`, een eigen door de docent toegekende business-key (`session_id`, bv. "wc-wk06"), daarom singleton-geadresseerd via `Bundle.write_singleton` (zelfde adressering als `team-roster`, gesleuteld op `session_id`) in plaats van assessment-schedule's event-log-stijl met gegenereerde bestandsnaam-suffix. Toegelicht in de docstring van de nieuwe sectie in `routes.py`, niet stilzwijgend een andere architectuur gekozen.
- [x] **14.4** Bouw `frontend/src/routes/admin/Sessions.tsx`: nieuw admin-tabblad: formulier voor type, week, onderwerp, locatie, materiaal-link, tijd. ✅ Vijfde tabblad in `AdminApp.tsx` naast CoursePeriod/Teams/Assessments/DocumentUpload (TDD 6.8's eigen "vijfde tabblad"-instructie). Formulier + tabel met quick-edit (onderwerp) en verwijderen, zelfde stijl als `Assessments.tsx`.
- [x] **14.5** Bouw `frontend/src/routes/team/Schedule.tsx`: student-facing route (`/team/:teamId/schedule`) die alle sessies toont, gegroepeerd per week, met materiaal-links. ✅ Route toegevoegd aan `App.tsx` vóór de generieke `/team/:teamId/:stage`-route. Haalt data op via `admin/api.ts`'s `listSessions` (gedocumenteerde, opzettelijke keuze: sessies zijn `admin_api`-eigendom, niet `team_api`, zie de module-docstring in `Schedule.tsx`). Gebruikt de bestaande HAN Huisstijl-tokens (`han-huisstijl-tokens.css`) en `HanHeader`, geen nieuwe stijlarchitectuur.
- [x] **14.6** Schrijf `data/tests/test_session_concepts.py` en tests voor de nieuwe CRUD-endpoints (analoog aan Task 9's teststructuur). ✅ 7 tests in `test_session_concepts.py` (roundtrip, klas-lakehouse-co-existentie met `assessment-schedule`, kind-restrictie, materials-default, `read_concept`-dispatch, registry-registratie, non-empty-type). 8 nieuwe CRUD-tests toegevoegd aan `agents/tests/test_admin_crud.py` (create/read/update/delete, duplicate-afwijzing, list-per-klas-filtering, kind-restrictie, action-log-verificatie). Daarnaast 2 nieuwe DuckLake-tests in `test_ducklake_tables.py` (write/query, append-only). Bestaande `test_every_concept_type_has_nonempty_type_field` bijgewerkt van 10 naar 11 concept-types (nodig, niet stilzwijgend overgeslagen).
- [x] **14.7** Pas de `@log_action`-decorator (Task 2.10) toe op de nieuwe endpoints (NFR-08-consistentie). ✅ Alle vijf publieke functies (`get_session`/`list_sessions`/`create_session`/`update_session`/`delete_session`) gedecoreerd met `@log_action(layer="data")`, zelfde patroon als course-period/teams/assessments. Live geverifieerd tegen de draaiende server: `create_session`/`list_sessions`-rijen met `outcome=success` in `action_log`.

### Test Gate: Task 14
- **Automatisch:** ✅ Nieuwe tests: 7 (`test_session_concepts.py`) + 2 (`test_ducklake_tables.py`, attendance_log) + 8 (`test_admin_crud.py`, session-CRUD) = 17 nieuwe pytest-tests, allemaal groen. Volledige testsuite (`.venv/bin/python -m pytest -q`): **232 geslaagd, 1 geskipt** (baseline was 198 geslaagd, 1 geskipt vóór deze task; het verschil is deze 17, plus tests die parallelle Task 16/19 in dezelfde golf toevoegden aan de gedeelde working tree). Frontend: `tsc --noEmit -p tsconfig.app.json` en `npm run build`/`oxlint` allemaal schoon.
- **Mens-testbaar artefact:** ✅ Volledige handmatige doorloop met daadwerkelijk draaiende servers (`npm run dev`, `.venv/bin/python -m admin_api.server`, `.venv/bin/python -m team_api.server`), aangestuurd via echte browser-automation (Playwright): een werkcollege-sessie `wc-wk06` (week 6, "Ch6 Process design + Ch7 Layout", Arnhem, materiaal `han-huisstijl-slides-wk6.pdf`, 21-09-2026 13:00–14:30) aangemaakt via het echte admin-formulier op het nieuwe "Sessies"-tabblad, bevestigd zowel in de UI-tabel als rechtstreeks op schijf (`lakehouse/2026-2027-oe-klas-klas-a/okf_bundle/session/wc_wk06.md`) en in `action_log`. Vervolgens `/team/team-07/schedule` bezocht: toont "Week 6" met exact diezelfde sessiekaart (werkcollege-badge, tijd, locatie, materiaal-link), geverifieerd met een echte screenshot. **Afwijking van de brief, expliciet gemeld:** de drie parallelle Task 14/16/19-agents deelden dezelfde browsersessie (geen aparte Playwright-contexts): dit veroorzaakte tijdelijke tab-hijacking-races tijdens de walkthrough; opgelost door in een eigen, expliciet geselecteerde tab te werken en elke stap te bevestigen via zowel de UI-screenshot als een directe `curl`-aanroep naar `admin_api` en een directe read van het OKF-bestand op schijf.

### Commit & push: Task 14
- [x] Commit met boodschap die refereert aan de nieuwe FR-nummers uit Task 12/13. ✅ Commit `898ccde` (refereert FR-26/FR-27).
- [x] Push naar `oe-gate-system` `main`. ✅ Gepusht naar https://github.com/businessdatasolutions/oe-gate-system.
- [x] Vink af: **Main Task 14 afgerond** (13 juli 2026).

---

## Main Task 15: Aanwezigheid & in-sessie tools

*(Golf 10, parallel aan Task 17: vereist Task 14 gepusht.)*

**Bestanden (in `oe-gate-system`):**
- Modify: `agents/team_api/server.py`, `data/smdl/ducklake.py`
- Create: `frontend/src/components/PollWidget.tsx`, `agents/tests/test_attendance_and_polls.py`
- ✅ **Werkelijke bestandenlijst, groter dan hierboven gepland** (nodig omdat de subtaak-tekst zelf verwijst naar zowel een check-in-endpoint (student, `team_api`) als een docent-poll-CRUD (`admin_api`) -- beide bestonden nog niet): ook gewijzigd `agents/team_api/service.py`, `agents/admin_api/routes.py`, `agents/admin_api/server.py`, `frontend/src/routes/team/api.ts`, `frontend/src/routes/admin/api.ts`, `frontend/src/routes/team/Programma.tsx`, `frontend/src/routes/team/Programma.css`, `frontend/src/routes/admin/Sessions.tsx`; ook aangemaakt `frontend/src/components/PollWidget.css`.

- [x] **15.1** Check-in-endpoint: `POST` die een `attendance-log`-rij schrijft (`team_id`/`first_name` + `session_id` + timestamp). ✅ **Correctie op de subtaak-tekst:** `Schedule.tsx` (waar 15.2 hieronder oorspronkelijk naar verwijst) bestaat niet meer -- Main Task 23 (commit `7621091`) heeft het geconsolideerd in `frontend/src/routes/team/Programma.tsx` (route `/team/:teamId/programma`), dat nu het volledige 18-weekse programma toont inclusief per-week sessiedata via `weekProgramma.ts`. De check-in-knop/poll-widget zijn daarom op `Programma.tsx` aangebracht, niet op een niet-bestaand bestand. `AttendanceLogRow`/`write_attendance_log`/`query_attendance_log` bestonden al uit Task 14 (`data/smdl/ducklake.py`) -- alleen het HTTP-endpoint ontbrak nog, exact het soort gat dat Task 23's eigen PDF-export-bugfix leerde te verwachten. Nieuwe functie `team_api.service.check_in` (`@log_action(layer="data")`) + `POST /api/check-in` op `team_api/server.py`, plus een read-only `get_attendance_status`/`GET /api/attendance-status` (ongedecoreerd, zelfde "plain read is not its own auditable action"-conventie als `get_team_roster`) zodat de knop na een pagina-refresh zijn "ingecheckt"-status terugkrijgt. Beide leven in het KLAS-lakehouse (`Lakehouse.for_klas`), naast `session` zelf -- niet het team-lakehouse dat `submit_checkpoint_answer` gebruikt.
- [x] **15.2** Check-in-knop op `Schedule.tsx` (Task 14): toont "ingecheckt"-status na een succesvolle aanroep. ✅ Op `Programma.tsx`, per sessie binnen een weekkaart (niet per week -- een week kan meerdere sessies plannen, LRD Deel 8's "vorm"-kolom). Nieuwe `SessionCheckIn`-subcomponent leest bij mount `fetchAttendanceStatus` (survives page refresh), toont daarna "✓ Ingecheckt" of een "Inchecken"-knop. Eén gedeeld "Jouw voornaam"-tekstveld bovenaan de pagina (`useState`, geen `localStorage`, KISS) wordt hergebruikt voor zowel check-in als poll-beantwoording.
- [x] **15.3** Implementeer een `poll`-datamodel (DuckLake-tabel: vraag, opties, `session_id`) en een `poll-response`-tabel. ✅ `PollRow`/`write_poll`/`query_polls` (tabel `polls`: `poll_id` (server-gegenereerde UUID), `session_id`, `question`, `options` (lijst-kolom, DuckDB-lijsttype geverifieerd), `created_at`) + `PollResponseRow`/`write_poll_response`/`query_poll_responses` (tabel `poll_responses`, EXACT TDD Deel 7.2-veldenlijst: `session_id`, `poll_id`, `team_id`, `first_name`, `choice`, `ts`), beide append-only via `ducklake.append_rows` (zelfde patroon als `attendance_log`). Beide in `data/smdl/ducklake.py`, klas-lakehouse. Geen apart open/closed-statusveld (KISS, TDD 6.8): "actieve poll" = de nieuwste poll voor een `session_id` (`team_api.service.get_active_poll`).
- [x] **15.4** Bouw `PollWidget.tsx`: docent-kant (poll starten, resultaten met een korte polling-interval, geen WebSocket-infra nodig, KISS), student-kant (antwoorden). ✅ `frontend/src/components/PollWidget.tsx`: één bestand, twee named exports (`PollWidgetDocent`/`PollWidgetStudent`) die de `Poll`-vorm en het pollingmechanisme delen maar structureel andere props hebben. **Schermplaatsing (buildplan's eigen "jouw keuze, documenteer het"):** docent-kant op `frontend/src/routes/admin/Sessions.tsx` (al het bestaande vijfde-tabblad docent-sessiescherm uit Task 14.4 -- een "Poll"-knop per sessierij opent een inline paneel), student-kant op `Programma.tsx` per sessie (naast de check-in-knop). Polling-interval 4s (TDD Deel 14's bevestigde bandbreedte "elke 3-5s"), backend-endpoints `POST /api/polls`/`GET /api/polls`/`GET /api/poll-results` (admin_api) en `GET /api/active-poll`/`POST /api/answer-poll` (team_api). Privacy: `poll_responses` is docent-only (TDD 7.2) -- `PollWidgetStudent` roept nergens `fetchPollResults` aan, alleen zijn eigen "verstuurd"-bevestiging.
- [x] **15.5** Schrijf `agents/tests/test_attendance_and_polls.py`: check-in-flow en poll-aanmaak/-beantwoording/-aggregatie. ✅ 19 nieuwe tests: check-in (schrijven, status-lezen, team-scoping, sessie-scoping, append-only/niet-gededupliceerd), poll-aanmaak (validatie: minstens 2 opties, geen duplicaten), "nieuwste poll is actief" (KISS, met expliciete timestamps i.p.v. `sleep()` voor determinisme), poll-beantwoording + aggregatie over meerdere teams, validatie (onbekende poll_id, keuze buiten de poll's eigen opties), en NFR-08-actielog-dekking (zowel het succes- als het foutpad).
- [x] **15.6** Pas `@log_action` toe op de nieuwe endpoints. ✅ `check_in`/`answer_poll` (`team_api/service.py`) en `create_poll` (`admin_api/routes.py`) zijn `@log_action(layer="data")`-gedecoreerd, zelfde conventie als elke eerdere schrijvende actie in deze twee modules. **Bewuste afwijking, gevonden tijdens de mens-testbare walkthrough:** `get_poll_results` (admin_api) is NIET gedecoreerd -- hij wordt elke 4s aangeroepen door `PollWidgetDocent`'s live-resultatenpaneel zolang een docent het pollpaneel open heeft staan, en `@log_action`'s default-schrijfdoel is de gedeelde, cohort-onafhankelijke **system**-lakehouse die ook `team_api`'s eigen `@log_action`-aanroepen (ander OS-proces) raken; decoreren zou een toch al zeldzame cross-proces-DuckLake-attach-race (zie Test Gate hieronder) van incidenteel naar vrijwel continu maken. Zelfde "plain read is not its own auditable action"-conventie als `get_active_poll`/`get_attendance_status`/`get_team_roster`, hier met een scherpere reden gedocumenteerd in de functie's eigen docstring.

### Test Gate: Task 15
- **Automatisch:** ✅ 19 nieuwe tests in `agents/tests/test_attendance_and_polls.py`, allemaal groen. Volledige testsuite (`.venv/bin/python -m pytest -q`): **268 geslaagd, 1 geskipt** (baseline 249/1 uit de opdracht + deze 19 nieuwe, geen regressies). Frontend: `npx tsc -b`, `npm run lint` (oxlint) en `npm run build` allemaal schoon.
- **Mens-testbaar artefact:** ✅ `.venv/bin/python -m admin_api.server` + `.venv/bin/python -m team_api.server` + `npm run dev` daadwerkelijk gedraaid, Playwright-walkthrough in twee browsertabs tegelijk: (1) als student op `/team/team-07/programma`: voornaam ingevuld, op week 6's werkcollege-sessie "Inchecken" geklikt → "✓ Ingecheckt" verschijnt, bevestigd via `GET /api/attendance-status` en na een paginareload nog steeds zichtbaar (persistente status, niet enkel React-state); (2) als docent op `/admin/sessions`: "Poll"-knop bij sessie `wc-wk06`, vraag + drie opties ingevuld, "Poll starten" → live-resultatenpaneel verschijnt direct; (3) terug als student: de `PollWidgetStudent`-poll verschijnt vanzelf (via de eigen 4s-polling, geen paginareload nodig om de nieuwe poll te zien) met de knoppen uitgeschakeld totdat een voornaam is ingevuld; na invullen en klikken op "Product layout" → "Bedankt, jullie antwoord is verstuurd"; (4) terug op het docent-tabblad, ZONDER die pagina te verversen: een tweede team's antwoord ("Process layout") via een directe API-aanroep gesimuleerd, en binnen de bestaande 4s-`setInterval` (geen handmatige actie) telt het resultatenpaneel zichtbaar op van "1 reactie(s)" naar "2 reactie(s)" met bijgewerkte staafdiagrammen -- het exacte "docent ziet de resultaten verschijnen"-bewijs dat deze Test Gate vraagt, aantoonbaar via de polling-interval en niet via een reload.
  **Tijdens deze walkthrough gevonden en gefixt (twee bugs, buiten de oorspronkelijke subtaak-tekst maar binnen de geest van Task 23's eigen concurrency-precedent):**
  (a) een echte regressie in eigen code: na het bewust ongedecoreerd laten van `get_poll_results` (zie 15.6) miste de bijbehorende `admin_api/server.py`-handler nog een `**_ACTOR`-argument dat de (nu verwijderde) decorator eerder consumeerde -- gaf een directe `TypeError` op elke aanroep; gefixt door de handler-aanroep aan te passen, geen `@log_action` meer terug te zetten.
  (b) een pre-existent, tot nu toe niet-ontdekt cross-proces-concurrencygat: `admin_api` en `team_api` draaien als twee losse OS-processen die beide naar dezelfde gedeelde, cohort-onafhankelijke **system**-lakehouse schrijven voor `action_log`; Task 23's `_DUCKLAKE_LOCK` serialiseert alleen aanroepen BINNEN één proces. Task 15 is de eerste feature waarbij één scherm (`Programma.tsx`) daadwerkelijk beide serverprocessen tegelijk nodig heeft voor één gebruikersflow (inchecken via `team_api`, poll via `admin_api`), waardoor deze cross-proces-race voor het eerst betrouwbaar zichtbaar werd (rechtstreeks gereproduceerd met gelijktijdige `curl`-aanroepen, los van de frontend). Een volledige herontwerp van `ducklake.connect()`'s retourwaarde (bv. een cross-proces `flock` voor de hele open-tot-close-levensduur van de connectie) zou de contractvorm van deze centrale, overal-gebruikte functie raken voor tientallen bestaande aanroepers -- te groot een blast radius voor Task 15's scope. In plaats daarvan: een kleine, begrensde retry (`_ATTACH_RETRY_ATTEMPTS = 5`, korte backoff) rond specifiek de `ATTACH`-aanroep in `data/smdl/ducklake.py::connect()`, alleen bij de exacte transiënte "Could not set lock on file"-`IOException`. Geverifieerd met 18 gelijktijdige cross-proces `curl`-aanroepen (6× elk van drie endpoints, verdeeld over beide serverprocessen): alle 18 slaagden na de fix, tegenover herhaalde 500-fouten ervoor. Dit is bewust een gerichte mitigatie, geen volledige architecturale oplossing -- vervolgtaak-waardig als de bredere multi-proces-opzet ooit onder hogere gelijktijdigheid moet draaien dan deze lokale dev-opstelling.

### Commit & push: Task 15
- [x] Commit met boodschap die refereert aan de sessie/aanwezigheid-FR's uit Task 12. ✅ Refereert FR-27 (check-in/`attendance_log`) en FR-28 (in-sessie-polls/`poll_responses`) uit Task 12's FR-traceerbaarheidsmatrix.
- [x] Push naar `oe-gate-system` `main`.
- [x] Vink af: **Main Task 15 afgerond** (13 juli 2026).

---

## Main Task 16: Onboarding-flow

*(Golf 9, parallel aan Task 14/19: vereist Task 13 gepusht.)*

**Bestanden (in `oe-gate-system`):**
- Modify: `data/smdl/okf.py` (uitbreiding team-roster met een onboarding-voortgangsvlag)
- Create: `frontend/src/routes/onboarding/Welcome.tsx`, `frontend/src/routes/onboarding/SelfAssessment.tsx`, `frontend/src/routes/onboarding/ToolingWalkthrough.tsx`, `frontend/src/routes/onboarding/TeamReveal.tsx`

- [x] **16.1** Bepaal de minimale databehoefte: een `onboarding_completed`-vlag als uitbreiding van het bestaande team-roster-concept (Task 2): geen nieuw datamodel nodig. ✅ Eén nieuw boolean-veld `onboarding_completed: bool = False` op `TeamRoster` (`data/smdl/okf.py`), met `False` als standaardwaarde zodat elk al bestaand team-roster-bestand op schijf gewoon blijft parsen. Geen nieuwe concept-class, geen nieuwe `okf_bundle`-submap; hergebruikt exact het bestaande `write_singleton`/`team-roster`-mechanisme uit Task 9's admin-CRUD.
- [x] **16.2** Bouw de schermenreeks conform het nieuwe LRD-Deel (Task 12.1): welkom/verwachtingen, zelfassessment (reflectief, geen scoring), tooling-walkthrough (wiki/AI-tutor/gates), team-koppeling-reveal (eigen team + ring-partner). ✅ Vier schermen onder `frontend/src/routes/onboarding/`: `Welcome.tsx` (dubbele rol: entry-variant `/welcome` én de afsluitende `/klaar`-variant, gedocumenteerd waarom dat één component is), `TeamReveal.tsx`, `SelfAssessment.tsx`, `ToolingWalkthrough.tsx`. **Bewuste, gedocumenteerde afwijking van deze subtask-tekst:** `TeamReveal.tsx` toont uitsluitend het eigen team, NIET de ring-partner. LRD Deel 6.14 (de tekst waar deze subtask zelf naar verwijst) is daar expliciet over: "week 0 introduceert uitsluitend het eigen team, niet de ring-partner"; de ring-partner-reveal blijft in week 1's al bestaande `PeerPartnerCard` (`CheckpointView.tsx`). De subtask-samenvatting hierboven ("+ ring-partner") is dus zelf onnauwkeurig t.o.v. de LRD die ze aanhaalt: de LRD/TDD-tekst is leidend, niet de subtask-samenvatting. **Mid-task correctie verwerkt:** de opdrachtgever corrigeerde tijdens de bouw twee van de drie zelfassessment-assen (LRD 6.14): (a) van generieke "voorkennis operations management" naar expliciet de propedeuse-inleiding Operations Management ("wat is blijven hangen, wat ben je kwijt"), en (c) van "verwachtingen van de AI-tutor/het gate-systeem" naar het bredere "verwachtingen omtrent de module: inhoud, project, samenwerking" (dat AI-tutor/gate-mechanisme leeft al apart in `ToolingWalkthrough.tsx`). `SelfAssessment.tsx` is met de gecorrigeerde formulering gebouwd/bijgewerkt, niet als TODO achtergelaten.
- [x] **16.3** Routeer nieuwe studenten naar `/onboarding` met een duidelijke afsluiting ("verder naar week 1"). ✅ `App.tsx`: `/onboarding` → demo-default `/onboarding/team-07/welcome` (zelfde bookmarkbare-default-conventie als `/team`), dan `welcome → team → self-assessment → tooling → klaar`. De afsluitende `klaar`-schermvariant is de ENE plek die de 16.1-vlag schrijft (`POST /api/complete-onboarding`, idempotent) en pas ná succesvolle write doornavigeert naar de al bestaande teamroute (`/team/:teamId/direct`): een expliciete knopklik, geen side-effect-op-mount.
- [x] **16.4** Schrijf tests voor de opslag van onboarding-voortgang. ✅ `agents/tests/test_onboarding.py` (7 tests: ontbrekende roster → duidelijke fout, lezen met default-`False`, geen cross-team-lek, vlag-flip + verse herlezing van schijf, idempotentie, en het structurele "nooit een score"-contract op `TeamRosterView`) + 2 nieuwe round-trip-tests in `data/tests/test_okf_concepts.py` voor de `TeamRoster`-vlag zelf.

### Test Gate: Task 16
- **Automatisch:** ✅ 9 nieuwe tests slagen (7 in `test_onboarding.py`, 2 in `test_okf_concepts.py`). `npx tsc -b` en `npm run lint` (oxlint) allebei schoon, `npm run build` slaagt. Volledige suite: **232 geslaagd, 1 geskipt** (dezelfde vooraf bestaande live-`AgentEvaluator`-skip als in eerdere tasks): dit is hoger dan de 198/1-baseline uit de opdracht omdat Task 14 (sessies) en Task 19 (wiki-analytics) parallel eigen tests toevoegden; t.o.v. de staat direct vóór deze task's eigen wijzigingen zijn er geen regressies. Eén incidentele, niet-reproduceerbare flake in `test_review_ring.py` (een bestand dat deze task niet aanraakt) tijdens één suite-run, met succes geïsoleerd herhaald en verdwenen bij een volgende volledige run. Vermoedelijke oorzaak: resourcecontentie door de twee gelijktijdig draaiende parallelle taken op dezelfde machine, geen regressie van deze task.
- **Mens-testbaar artefact:** ✅ `npm run dev` + `.venv/bin/python -m team_api.server` daadwerkelijk gedraaid, Playwright-walkthrough door de volledige sequentie als nieuwe student: `/onboarding` → welkomscherm → teamreveal (team-07: Fenna/Timo/Bram, klas-a) → zelfassessment (ingevuld, geverifieerd dat dit scherm **nul** netwerkverzoeken naar de team-API doet) → tooling-walkthrough (statisch) → afsluitscherm → knop "Verder naar week 1 →" → landt op de al bestaande `/team/team-07/direct`-checkpointroute. Vlag geverifieerd op twee plekken ná de klik: via `GET /api/team-roster` (`onboarding_completed: true`) én rechtstreeks op schijf in `okf_bundle/team-roster/team_07.md`.

### Commit & push: Task 16
- [x] Commit met boodschap die refereert aan de onboarding-FR's uit Task 12. ✅ Commits `598d33f` (hoofdimplementatie, refereert FR-25) en `773279e` (mid-task-correctie van zelfassessment-assen (a)/(c), zie 16.2). **Gedeelde-working-tree-incident:** door het ontbreken van worktree-isolatie tussen de drie parallelle Golf-9-taken raapte Task 19's `git commit` kortstondig ook deze task's nog-ongecommitte bestanden mee in commit `699346a6`; Task 19 herkende dit zelf, voerde een `git reset HEAD~1` uit (geen contentverlies) en committe beide taken's werk daarna gescheiden opnieuw. Het resultaat hieronder is het schone eindresultaat, geen omissie.
- [x] Push naar `oe-gate-system` `main`. ✅ Gepusht (`898ccde..773279e`).
- [x] Vink af: **Main Task 16 afgerond** (13 juli 2026).

---

## Main Task 17: Student journey-view

*(Golf 10, parallel aan Task 15: vereist Task 14 gepusht; bouwt inhoudelijk voort op Task 16 maar is er niet hard van afhankelijk.)*

**Bestanden (in `oe-gate-system`):**
- Modify: `agents/dashboard_query/agent.py` (nieuwe team-scoped, read-only query, dus niet instructor-only)
- Create: `frontend/src/routes/team/Journey.tsx`, `agents/tests/test_journey_view.py`

- [ ] **17.1** Ontwerp een cursusbrede tijdlijn: onboarding + 4 D-fasen + capstone (15 weken), huidige positie gemarkeerd, per week de geplande sessie(s) (Task 14) en gate-status waar relevant.
- [ ] **17.2** Backend: een team-scoped query (hergebruik het `dashboard_query`-patroon) die deze tijdlijn samenstelt: structureel nooit meer dan het eigen team tonen.
- [ ] **17.3** Bouw `frontend/src/routes/team/Journey.tsx` + navigatie-link vanaf `CheckpointView`/`Schedule`.
- [ ] **17.4** Schrijf `agents/tests/test_journey_view.py`: correcte scoping (team A ziet nooit team B's data via dit endpoint) + component-render.

### Test Gate: Task 17
- **Automatisch:** nieuwe tests slagen, inclusief de expliciete cross-team-scoping-test.
- **Mens-testbaar artefact:** een student ziet zijn volledige 15-weekse pad met de huidige positie gemarkeerd.

### Commit & push: Task 17
- [ ] Commit met boodschap die refereert aan de journey-view-FR uit Task 12/13.
- [ ] Push naar `oe-gate-system` `main`.
- [ ] Vink af: **Main Task 17 afgerond**.

---

## Main Task 18: Survey/QMS-laag

*(Golf 11: vereist Task 15 gepusht, want de survey wordt getriggerd door de check-in-flow.)*

**Bestanden (in `oe-gate-system`):**
- Modify: `data/smdl/ducklake.py`, `agents/team_api/server.py`, `frontend/src/routes/instructor/Dashboard.tsx`
- Create: `frontend/src/components/SurveyWidget.tsx`, `agents/tests/test_survey.py`

- [ ] **18.1** Implementeer `survey_response` als append-only DuckLake-tabel: `session_id`, `team_id`, `first_name`, ratings per dimensie (definitief bepaald in Task 12.3), vrije tekst, timestamp.
- [ ] **18.2** `POST`-endpoint dat een survey-response opslaat, client-side getriggerd na check-in (Task 15) + een korte vertraging of sessie-einde-signaal.
- [ ] **18.3** Bouw `SurveyWidget.tsx`: sterren-rating per dimensie + vrije tekst. Hergebruik de eenvoudige UX van het referentievoorbeeld (`hanbedrijfskunde/retrospective`); geen ingewikkelde flow.
- [ ] **18.4** Basis instructor-aggregaatview op `Dashboard.tsx`: gemiddelde ratings per sessie/week, puur deterministische SQL-aggregatie, nooit een LLM-samenvatting van de cijfers zelf (never-invents-patroon).
- [ ] **18.5** Schrijf `agents/tests/test_survey.py`: opslag, aggregatie-query, en dat individuele antwoorden nooit buiten instructor-only-context zichtbaar worden op een manier die verder gaat dan het bestaande privacymodel.

### Test Gate: Task 18
- **Automatisch:** nieuwe tests slagen; bestaande suite ongewijzigd groen.
- **Mens-testbaar artefact:** een post-sessie-survey invullen als student, terugzien dat die gelogd is en meetelt in het instructor-aggregaat.

### Commit & push: Task 18
- [ ] Commit met boodschap die refereert aan de kwaliteitsmanagement-FR's uit Task 12.
- [ ] Push naar `oe-gate-system` `main`.
- [ ] Vink af: **Main Task 18 afgerond**.

---

## Main Task 19: Wiki-gebruiksanalyse

*(Golf 9, parallel aan Task 14/16: vereist Task 13 gepusht.)*

**Bestanden:**
- Modify (`oe-wiki`): `quartz.config.ts`
- Create (`oe-gate-system`): `data/smdl/engagement_rollup.py`, `data/tests/test_engagement_rollup.py`
- Modify (`oe-gate-system`): `frontend/src/routes/instructor/Dashboard.tsx` (team-activiteitsindicator)

- [x] **19.1** Schakel een zelf-hostbare, privacy-vriendelijke analytics-provider in `quartz.config.ts` in (aanbeveling: GoatCounter, te bevestigen bij kickoff of dit de definitieve keuze blijft, zie Task 13.4). ✅ GoatCounter bevestigd bij kickoff. `quartz.config.ts` schakelt Quartz' ingebouwde GoatCounter-optie in zodra `QUARTZ_GOATCOUNTER_SITE_CODE` gezet is (`analytics: goatcounterSiteCode ? {provider: "goatcounter", websiteId: ...} : null`), géén nieuwe code, alleen config, exact zoals TDD 3.7 voorschrijft. Er bestaat geen live GoatCounter-account in deze omgeving, dus er is bewust **geen** site code gehardcodeerd (geen verzonnen credential); `deploy.yml` geeft alleen de (nu nog lege) matching GitHub Actions-secret door. Empirisch bewezen met twee echte `npx quartz build`-runs: zónder de env-var blijft de build 100% identiek aan de oude `analytics: null`-situatie (geen `goatcounter`-referentie in de output); mét een test-sitecode verschijnt het GoatCounter-scriptje in `postscript.js` en stuurt uitsluitend `{path: location.pathname}`, nooit een cookie/sessie-ID. Restpunt: iemand moet ooit handmatig een GoatCounter-account aanmaken (https://www.goatcounter.com/signup) en de site-code als GH-secret zetten. Dat is een bewust buiten-de-sessie-stap, geen onvolledige wiring.
- [x] **19.2** Zorg dat paginabezoeken herleidbaar zijn tot een team via het bestaande URL-pad-patroon (of een pagina-tag), zonder individuele bezoeker-identificatie op te slaan. ✅ Twee lagen, allebei structureel afgedwongen: (1) GoatCounter's eigen clientscript rapporteert uitsluitend `{path: location.pathname}` (geverifieerd in de gebouwde `postscript.js`, zie 19.1): geen cookie/fingerprint/sessie-ID komt ooit bij de provider terecht; (2) `data/smdl/engagement_rollup.py`'s `extract_team_id()` leest `team_id` uitsluitend uit het bestaande teampagina-URL-pad-patroon (`wiki/syntheses/<cohort>/<team_id>-<stage>.md` → `/syntheses/<cohort>/<team_id>-<stage>`, TDD 3.2/3.4) via een regex op het pad, nooit via een andere identifier. Een pad zonder herkenbare `team_id` (cursusinhoud) wordt stilzwijgend uitgesloten, nooit aan een gegokt team toegeschreven (bewezen in `test_rollup_never_invents_a_team_for_an_unattributable_path`).
- [x] **19.3** Schrijf `data/smdl/engagement_rollup.py`: haalt periodiek de provider's aggregaat-cijfers op en schrijft ze weg als `wiki-engagement-aggregate` (`team_id`, `week`, telling) in de team-lakehouse. ✅ `rollup_wiki_engagement(path_counts, cohort, week, root)` is de pure, deterministische kerntransformatie (zelfde "een cron-style aanroeper of een test levert de input" patroon als `review_ring_coordinator.check_lapses`, buildplan 4.5): sommeert per team en schrijft één `wiki_engagement`-DuckLake-rij per team in diens eigen lakehouse via het nieuwe `ducklake.WikiEngagementRow`/`write_wiki_engagement`/`query_wiki_engagement`-trio (achtste tabel naast de bestaande zeven, exact hetzelfde Pydantic-rijmodel + `write_*`/`query_*`-patroon). `run_rollup()`/`fetch_goatcounter_path_counts()` verbinden dit met een echt GoatCounter-account via `OE_GOATCOUNTER_SITE_CODE`/`OE_GOATCOUNTER_API_TOKEN`. Ontbreken die (zoals in deze omgeving), dan faalt de functie luid met een duidelijke instructie in plaats van stilzwijgend niets te doen of een site code te verzinnen. Nergens een LLM-call: puur DuckDB/dict-optelwerk (TDD 4.4).
- [x] **19.4** Voeg een team-activiteitsindicator toe aan `Dashboard.tsx` (instructor-kant) en/of een lichte indicator op `Schedule.tsx`/`Journey.tsx` (team-kant). ✅ `Dashboard.tsx`'s bestaande `team-card__meta`-rij (naast "Hidden score"/"Overrides used") kreeg een derde `<dt>/<dd>`: "Wiki visits (wk …)" met een badge in de bestaande HAN Huisstijl-neutrale badge-tokens. De kaart is niet verder herstructureerd. `wiki_engagement: {week, page_visits} | null` is `null`-bewust: ontbrekende rollup-data toont `—`, nooit een verzonnen `0`. In een echte browser geverifieerd (Playwright, `npm run dev`): team-05/team-07 tonen respectievelijk 23/8 bezoeken, team-09 (nog geen rollup gedraaid) toont `—`. Team-kant (`Schedule.tsx`) bewust **niet** aangeraakt: dat bestand is in-flight bij Task 14 (parallelle sessie, gedeelde working tree); TDD 3.7/buildplan 19 vereisen alleen de instructor-kant, dus dit is geen onvolledige uitvoering.
- [x] **19.5** Schrijf `data/tests/test_engagement_rollup.py`: gegeven ruwe aggregaat-cijfers, de juiste DuckLake-rij. ✅ 9 tests: URL-pad-attributie (positief/negatief), correcte som over meerdere paden per team, isolatie tussen teams (nooit cross-team-lakehouse, TDD 4.1), append-only-gedrag over meerdere weken, "nooit een team verzinnen voor een niet-toewijsbaar pad", "nooit meer schrijven dan de provider rapporteerde" (never-invents-numbers, TDD 4.4), en een credentials-niet-verzinnen-test + een end-to-end pipeline-test (mocked provider → rollup → DuckLake-rij) die samen de mens-testbare loop hieronder dekken.

### Test Gate: Task 19
- **Automatisch:** ✅ `.venv/bin/python -m pytest data/tests/test_engagement_rollup.py -v`: **9/9 geslaagd**; volledige suite vóór dit werk 198 passed/1 skipped, ná dit werk (samen met de twee parallelle Task 14/16-commits die in dezelfde golf landden) 232 passed/1 skipped, geen regressies.
- **Mens-testbaar artefact:** ✅ Er bestaat geen live GoatCounter-account in deze omgeving (zie 19.1): de mens-testbare loop is daarom end-to-end gesimuleerd met een gemockte providerrespons in plaats van een echte browserbezoeker, met expliciete eerlijke `RuntimeError` als het pad naar een echt account ontbreekt (`test_run_rollup_refuses_to_fabricate_credentials_when_unconfigured`). Handmatig gedraaid: vóór de rollup was `wiki_engagement` voor team-07 leeg (`[]`); na het simuleren van drie teampaginabezoeken (5+2 op team-07-pagina's, 40 op een niet-team-pagina die genegeerd werd) en één `rollup_wiki_engagement()`-aanroep stond er zichtbaar één rij `{team_id: team-07, page_visits: 7, week: 2026-W29}`, waarmee de teamtelling "bewoog zichtbaar" zoals de Test Gate vraagt. Diezelfde pijplijn is via Playwright in een echte browser bevestigd op `Dashboard.tsx` (screenshot: team-05/07 tonen resp. 23/8 bezoeken, team-09 toont eerlijk `—`). Restpunt (eerlijk gemeld): de laatste stap (een écht GoatCounter-account koppelen zodat dit met echte bezoekersdata in plaats van een mock draait) vereist een eenmalige handmatige accountaanmaak buiten deze sessie (zie 19.1).

### Commit & push: Task 19
- [x] Commit in `oe-wiki` (config) én `oe-gate-system` (rollup-code), beide met boodschap die refereert aan het wiki-betrokkenheidssignaal uit Task 12.4. ✅ `oe-wiki` commit `65c453b`; `oe-gate-system` commit `a03704e` (na een gecorrigeerde `git reset HEAD~1` + herstage, zie onder).
- [x] Push beide. ✅ `oe-wiki` gepusht naar https://github.com/businessdatasolutions/oe-wiki (`9c133dd..65c453b`); `oe-gate-system` gepusht naar https://github.com/businessdatasolutions/oe-gate-system, bevestigd aanwezig op `origin/main` als `a03704e`.
- [x] Vink af: **Main Task 19 afgerond** (13 juli 2026). **Noot over gedeelde working tree:** tijdens dit werk deelden drie parallelle agents (Task 14/16/19) dezelfde `oe-gate-system`-working tree/`.git`-index zonder worktree-isolatie. Een `git commit` in deze sessie greep per ongeluk ook Task 16's op dat moment gestagede, nog niet gecommitte onboarding-bestanden mee (een gedeelde-index-race, niet een inhoudelijk conflict). Direct gedetecteerd, beide andere agents gewaarschuwd via `SendMessage`, hersteld met een niet-destructieve `git reset HEAD~1` (unstaged alles terug, geen enkel bestand verloren) gevolgd door een opnieuw correct geschoonde commit (alleen de eigen vijf bestanden, met `git apply --cached` voor de gedeelde `ducklake.py` om Task 14's `attendance_log`-hunk niet mee te nemen). Door beide agents bevestigd als correct hersteld.

---

## Main Task 20: Course Quality Advisor Agent

*(Golf 12: vereist Task 18 én Task 19 gepusht, want de agent heeft zowel survey- als engagement-data nodig.)*

**Bestanden (in `oe-gate-system`):**
- Create: `agents/quality_advisor/agent.py`, `agents/quality_advisor/tools.py`, `agents/quality_advisor/quality_advisor.evalset.json`, `agents/tests/test_quality_advisor.py`
- Modify: `frontend/src/routes/instructor/Dashboard.tsx` (rapportview)

- [ ] **20.1** Scaffold als task-mode `Agent`, zelfde tweelaags patroon (chat-coördinator + task-worker) als `socratic_tutor`/`wiki_quality_check`.
- [ ] **20.2** Tools `query_survey_responses(cohort, period)` en `query_engagement(cohort, period)`: puur deterministische DuckDB-aggregatie (never-invents-patroon, TDD Deel 4.4).
- [ ] **20.3** Tool `generate_quality_report(cohort, period)`: LLM synthetiseert sterke/zwakke punten, met verplicht `source_ref` per bewering: hergebruik het structurele patroon uit `dashboard_query`'s `generate_prep_summary`/`wiki_quality_check` (nooit een niet-traceerbare bewering).
- [ ] **20.4** Instructor-only rapportview op `Dashboard.tsx` (nieuwe kaart/route).
- [ ] **20.5** Schrijf een evalset + `agents/tests/test_quality_advisor.py`, inclusief een adversarial test dat een poging tot een niet-traceerbare bewering geweigerd wordt (zelfde patroon als Task 10's hallucinatietest).
- [ ] **20.6** Pas `@log_action` toe op de nieuwe tools.

### Test Gate: Task 20
- **Automatisch:** nieuwe tests slagen, inclusief de adversarial source_ref-test; bestaande suite ongewijzigd groen.
- **Mens-testbaar artefact:** na het seeden van voorbeeld-surveydata genereert de agent een rapport dat naar specifieke antwoorden citeert, zichtbaar in de instructor-UI.

### Commit & push: Task 20
- [ ] Commit met boodschap die refereert aan de kwaliteitsmanagement-FR's uit Task 12/13.
- [ ] Push naar `oe-gate-system` `main`.
- [ ] Vink af: **Main Task 20 afgerond**.

---

## Main Task 21: Onboarding-verrijking (WHY/HOW/WHAT, praktische startinfo, geruststelling)

*(Golf: geen afhankelijkheid van Task 15/17/18/20 — vereist alleen Task 16 gepusht. Geen coding-subagent voor de LRD/TDD-tekst zelf, wel voor de code; zie plan `can-we-start-building-compiled-narwhal.md` voor de volledige aanleiding.)*

**Waarom een aparte task in plaats van Task 16 stilzwijgend herschrijven:** Task 16 leverde correct wat LRD §6.14 op dat moment vroeg. Een persona-oefening ("leef je in in een tweedejaars HBO Bedrijfskunde-student") plus een gerichte Explore-agent bevestigden acht concrete gaten in wat een student nodig heeft om goed te starten — geen van alle geraakt door de toenmalige onboarding. De opdrachtgever voegde daar zelf een negende, grotere eis aan toe: een Golden-Circle WHY/HOW/WHAT van de hele module, niet alleen de gate-mechaniek.

**Bestanden:**
- Modify: `design-documents/lrd-operational-excellence.html` (Deel 6.14 — al gedaan, zie hieronder), `design-documents/tdd-operational-excellence.html` (Deel 6.7 — al gedaan)
- Create (`oe-gate-system`): `frontend/src/routes/onboarding/WhyHowWhat.tsx`, `frontend/src/routes/onboarding/Praktisch.tsx`
- Modify (`oe-gate-system`): `frontend/src/routes/onboarding/OnboardingSteps.tsx`, `frontend/src/routes/onboarding/TeamReveal.tsx`, `frontend/src/routes/onboarding/ToolingWalkthrough.tsx`, `frontend/src/routes/onboarding/useOnboardingParams.ts`, `frontend/src/App.tsx`

- [x] **21.1** LRD Deel 6.14 herschrijven: nieuwe bullet "Waarom dit vak (Golden Circle)" (WHY §1.2/3.1, HOW Deel 2/6/8, WHAT §3.2 teruggekoppeld naar WHY), team-koppeling-reveal uitgebreid met de vangnet-zin (6.6), nieuwe bullet "Praktische startinformatie" (boektitel/editie generiek, link naar sessierooster 6.15, case-selectierichtlijnen: vrije keuze + criteria), tooling-walkthrough uitgebreid met het *waarom* (UnBlooms, formatief/summatief) en de vier contactvormen (6.15), plus een geruststellende afsluitzin over het ongebruikelijke beoordelingsmodel. ✅ Geschreven, HTML-parse geverifieerd.
- [x] **21.2** TDD Deel 6.7 bijgewerkt: onboarding-flow van vier naar zes schermen, expliciet genoteerd dat de twee nieuwe schermen puur statische content zijn, geen nieuw datamodel/endpoint. ✅ Geschreven, HTML-parse geverifieerd.
- [ ] **21.3** Docent-sanity-check op de nieuwe LRD/TDD-tekst vóór commit — inclusief de expliciete vraag of "kort dagdeel" nog een passende tijdsaanduiding is nu de sessie inhoudelijk zwaarder is geworden (zie LRD 6.14's eigen openingszin, die "kort dagdeel" bewust heeft losgelaten in afwachting van dit antwoord).
- [ ] **21.4** `frontend/src/routes/onboarding/WhyHowWhat.tsx`: nieuw scherm, drie kaarten (WHY/HOW/WHAT), WHAT-kaart bevat een expliciete terugkoppeling naar de WHY-kaart. Route direct na Welcome, vóór Team-reveal.
- [ ] **21.5** `frontend/src/routes/onboarding/Praktisch.tsx`: nieuw scherm, boek-referentie + link naar `/team/:teamId/schedule` + case-selectierichtlijnen. Route tussen Team-reveal en Zelfassessment.
- [ ] **21.6** `OnboardingSteps.tsx` + `useOnboardingParams.ts` + `App.tsx`: stappenreeks van vier naar zes ("Welkom, Waarom dit vak, Je team, Praktisch, Zelfassessment, Tooling"), nieuwe steppaden `waarom`/`praktisch` toegevoegd.
- [ ] **21.7** `TeamReveal.tsx`: één alinea toegevoegd met de vangnet-geruststelling (LRD 6.6).
- [ ] **21.8** `ToolingWalkthrough.tsx`: bestaande Socrates-/gate-kaarten uitgebreid met het *waarom* (micro-niveau, te onderscheiden van WhyHowWhat.tsx's macro-niveau); nieuwe vierde kaart over de vier contactvormen.
- [ ] **21.9** Smoke-test dat de zesstappenflow van begin tot eind doorloopt en `onboarding_completed` nog steeds correct wegschrijft (hergebruik van Task 16's bestaande tests, geen nieuw datamodel dus geen nieuwe backendtests nodig).

### Test Gate — Task 21
- **Automatisch:** ✅ `tsc --noEmit`/lint/build schoon; volledige testsuite 237 geslaagd, 1 geskipt (was 232/1 — de +5 komen van de losstaande PDF-exportbugfix die in dezelfde sessie is meegenomen, zie de correctienotitie onderaan dit document, geen regressie).
- **Mens-testbaar artefact:** ✅ zelf een browser-doorloop van alle zes stappen (Playwright), docent-sanity-check op de nieuwe copy ontvangen vóór commit (21.3).

### Commit & push: Task 21
- [x] Commit in `operational-excellence-course` (LRD/TDD-tekst) — apart van de code-commit. ✅ Al gecommit door een parallelle sessie (`8265722`/`643b633`) — geverifieerd dat de Golden-Circle-tekst in de huidige `HEAD` staat, geen aparte commit hier nodig.
- [x] Commit in `oe-gate-system` (code), boodschap verwijst naar FR-25.
- [x] Push beide.
- [x] Vink af: **Main Task 21 afgerond** (13 juli 2026).

---

## Main Task 22: Navigatiestructuur & rolgescoped views (AppShell, admin-consolidatie, onboarding-terugnavigatie)

*(Golf: geen afhankelijkheid van Task 15/17/18/20 — vereist Task 21 gepusht: beide raken dezelfde zes onboarding-bestanden (`Welcome.tsx`/`WhyHowWhat.tsx`/`TeamReveal.tsx`/`Praktisch.tsx`/`SelfAssessment.tsx`/`ToolingWalkthrough.tsx`) plus `OnboardingSteps.tsx`/`useOnboardingParams.ts`/`App.tsx`, dus sequentieel, niet parallel met Task 21.)*

**Waarom een aparte task:** een live browserwalkthrough + broncode-inspectie op de al gebouwde schermen legde drie losstaande navigatie-UX-problemen bloot die geen bestaande task dekte: het admin-onderdeel (Task 9) verliest de hoofdnavigatie volledig omdat het een apart Vite-bundle is, bereikbaar alleen als hard `<a href="/admin.html">`; de onboarding-sequentie (Task 16/21) heeft geen terugknop en een expliciet non-interactieve stapindicator; en de gedeelde navigatiebalk (Task 5) toont ongeacht wie kijkt altijd alle zes links, zonder enige rolscoping. Zie LRD 6.17/TDD 6.12.

**Bestanden:**
- Modify: `design-documents/lrd-operational-excellence.html` (Deel 6.17, FR-32–35, NFR-13), `design-documents/tdd-operational-excellence.html` (Deel 6.12, Deel 8.1-matrix, Deel 14-tabel)
- Create (`oe-gate-system`): `frontend/src/nav-config.ts`, `frontend/src/components/AppShell.tsx`, `frontend/src/components/AppShell.css`
- Modify (`oe-gate-system`): `frontend/src/App.tsx`, `frontend/src/routes/admin/AdminApp.tsx`, `frontend/src/routes/admin/CoursePeriod.tsx`/`Teams.tsx`/`Assessments.tsx`/`Sessions.tsx`/`DocumentUpload.tsx`, `frontend/src/routes/onboarding/Welcome.tsx`/`WhyHowWhat.tsx`/`TeamReveal.tsx`/`Praktisch.tsx`/`SelfAssessment.tsx`/`ToolingWalkthrough.tsx`, `frontend/src/routes/onboarding/OnboardingSteps.tsx`, `frontend/src/routes/onboarding/useOnboardingParams.ts`, plus elk bestaand scherm's `HanHeader`-aanroep (`CheckpointView.tsx`, `Schedule.tsx`, `Dashboard.tsx`, `StudentDossier.tsx`)
- Delete (`oe-gate-system`): `frontend/admin.html`, `frontend/src/routes/admin/main.tsx`

- [x] **22.1** LRD Deel 6.17 (navigatiestructuur & rolgescoped views) + Deel 6.18 (Onderwijsprogramma, zie Task 23) geschreven, FR-32–36 toegevoegd aan Deel 7.1, FR-26 herzien, NFR-13/14 toegevoegd aan Deel 7.2. ✅ Geschreven direct in `lrd-operational-excellence.html`, samen met Task 23's LRD-tekst (één samenhangende sessie, zelfde patroon als Task 12/13's "samen met de opdrachtgever direct in de HTML geschreven").
- [x] **22.2** TDD Deel 6.12 (routetabel, AppShell/NavLink/zone-afleiding, admin-consolidatie, onboarding-terugfix, nav-config-als-single-source) + Deel 6.13 (Task 23, zie aldaar) geschreven, Deel 6.10 aangepast met een expliciete verwijzing naar 6.13, Deel 8.1-matrix uitgebreid met FR-32–36/NFR-13/14, Deel 14-rij "Toegangsrol admin-onderdeel" aangepast met de expliciete "lost dit niet op"-zin, Deel 6.8 aangepast voor de FR-26-herziening. ✅ Geschreven direct in `tdd-operational-excellence.html`.
- [x] **22.3** Docent-sanity-check op de nieuwe LRD/TDD-tekst vóór commit — met name de "dit is geen toegangscontrole"-framing (zelfde soort check als Task 21.3). ✅ Akkoord ontvangen (13 juli 2026).
- [x] **22.4** `frontend/src/nav-config.ts`: array van navigatie-items (`zone`/`path`/`label`) + `zoneOf(pathname)` + `useNavContext()`-hook. ✅ Implementatiekeuze (buildplan liet dit expliciet open): elk `NavItem` kreeg één veld boven de letterlijke `{zone,path,label}`-schets — een pure `match(pathname)`-predicaat. Reden: `path` is altijd het concrete, direct-klikbare nav-balk-doel (zelfde "demo-team is direct bookmarkbaar"-conventie als `App.tsx`/`useOnboardingParams.ts`), en dat kan niet ALS pattern dienen tegen een willekeurig team's echte URL (`/team/team-12/design`, niet alleen de demo `/team/team-07/...`). Zonder `match()` zou `zoneOf`/`useNavContext` óf elk niet-demo-team verkeerd herkennen, óf zou er een tweede, apart onderhouden opzoektabel nodig zijn — precies de drift die dit bestand moet voorkomen.
- [x] **22.5** `frontend/src/components/AppShell.tsx`(+`.css`): layout-route met persistente nav (`NavLink`, actieve status via `aria-current`), zone-filter + expliciete zone-wisselschakelaar, wrapt `<Outlet/>`; herwijst `App.tsx`'s bestaande inline `<nav className="app-nav">` hierheen, geen visuele wijziging (hergebruikt `App.css`). ✅ Bekende, geaccepteerde NavLink-bijwerking gedocumenteerd in `AppShell.tsx`'s eigen module-doc: omdat "Team" (`/team`) en "Rooster" (`/team/team-07/schedule`) een URL-prefix delen, markeert NavLink's eigen niet-`end`-prefixmatching BEIDE als actief op de Rooster-pagina — geverifieerd in de Playwright-walkthrough (`aria-current="page"` op beide), een geaccepteerd gevolg van TDD 6.12's expliciete instructie om NavLink's ingebouwde mechanisme te gebruiken in plaats van iets handmatig te bouwen, niet een bug.
- [x] **22.6** `App.tsx`: routes genest onder `AppShell` via React Router v7's layout-route-patroon; vijf nieuwe `/admin/*`-routes toegevoegd, gemapt op `AdminApp.tsx`'s bestaande vijf schermen. ✅ `/admin` zelf redirect (`index`-route) naar `/admin/course-period`, zelfde "direct klikbaar/bookmarkbaar"-conventie als `/team`/`/instructor`.
- [x] **22.7** `admin.html` + `routes/admin/main.tsx` verwijderd; `AdminApp.tsx`'s lokale `useState<Tab>`-tabwissel vervangen door de vijf routes uit 22.6, elk tabblad zelfstandig deelbaar/terug-navigeerbaar. ✅ `AdminApp.tsx` is zelf nu een layout-route (`<Outlet/>` voor het actieve tabblad); `vite.config.ts`'s `build.rollupOptions.input`-override (het tweede build-entrypoint) volledig teruggedraaid naar de standaard single-entry-config, niets dubbelhangend achtergelaten. `CoursePeriod.tsx`/`Teams.tsx`/`Assessments.tsx`/`Sessions.tsx`/`DocumentUpload.tsx` zelf ongewijzigd (ze renderen al puur op basis van hun eigen state/props, `<Outlet/>` maakt ze transparant beschikbaar zonder dat ze zelf iets van routing hoeven te weten).
- [x] **22.8** Terugknop in alle zes onboarding-stapschermen: lege `<span/>` in `.onboarding-view__actions` vervangen door `<Link to={onboardingStepPath(previousStep, onboarding)} className="btn">`, behalve `Welcome.tsx`'s `entry`-variant (eerste stap, geen vorige). Nieuwe `previousOnboardingStep()`-helper in `useOnboardingParams.ts`. ✅ `Welcome.tsx`'s `closing`-variant (`klaar`, laatste stap) kreeg wél een terugknop (naar `tooling`) — expliciet niet uitgesloten door de buildplan-tekst, alleen `entry` was uitgezonderd. Geverifieerd in de Playwright-walkthrough: elke terugknop navigeert daadwerkelijk (niet alleen correcte `href`).
- [x] **22.9** `OnboardingSteps.tsx` interactief: `done`/`current`-stappen worden `<Link>` (zelfde patroon als `PhaseTracker.tsx` `scope="team"`), `upcoming`-stappen blijven `<span>` (geen vooruitspringen). ✅ Component kreeg een nieuwe verplichte `onboarding`-prop (de zes call sites geven allemaal hun al-bestaande `useOnboardingParams()`-resultaat door) zodat `onboardingStepPath()` binnen de stapindicator zelf bruikbaar is.
- [x] **22.10** Elk bestaand scherm z'n eigen `HanHeader`-aanroep laten pullen via `useNavContext()` in plaats van een hardcoded `contextName`-string. ✅ Geraakt: `CheckpointView.tsx` (was geen prop, nu "Team"), `Schedule.tsx` (was al "Rooster", nu identiek via de hook), `Dashboard.tsx` ("Docentendashboard" → "Docent"), `StudentDossier.tsx` ("Studentdossier" → "Dossier"), `AdminApp.tsx` ("Cursusvoorbereiding" → "Admin"), alle zes onboarding-schermen (was al overal letterlijk "Onboarding", nu via de hook — geen zichtbare wijziging). De drie labelwijzigingen (Docent/Dossier/Admin) zijn een bewuste, buildplan-gedreven vereenvoudiging: nav-label en scherm-context worden letterlijk dezelfde string, zodat ze nooit meer uit elkaar kunnen lopen — expliciet gemeld, geen stille cosmetische wijziging.
- [x] **22.11** Smoke-test: docent navigeert van `/instructor/dashboard` naar elk `/admin/*`-tabblad en terug zonder de browser-terugknop nodig te hebben; student doorloopt de zesstappen-onboarding heen én terug; beide zones tonen alleen hun eigen navigatie-items plus de zone-wisselaar. ✅ Playwright-walkthrough uitgevoerd tegen een echte `npm run dev`-server + de al draaiende `team_api`/`admin_api`-servers (zie Test Gate hieronder voor het volledige bewijs).

### Test Gate — Task 22
- **Automatisch:** ✅ `tsc --noEmit` schoon. `npm run build` schoon (emit nu weer uitsluitend `dist/index.html`, geen `dist/admin.html` meer). `npm run lint` (oxlint) schoon, exit 0. Volledige backend-testsuite ongewijzigd groen: `.venv/bin/python -m pytest -q` → **237 passed, 1 skipped** (identiek aan de baseline vóór deze task; geen enkel `.py`-bestand aangeraakt).
- **Mens-testbaar artefact:** ✅ Playwright-browserwalkthrough tegen `npm run dev` (poort 5183) + de reeds draaiende `team_api`(8801)/`admin_api`(8799)-servers.
  - **(a)** Alle vijf admin-schermen bezocht via echte URL's (`/admin/course-period`, `/admin/teams` via een klik op de "Teams"-tab-link, `/admin/assessments`, `/admin/sessions`, `/admin/upload`): de gedeelde hoofdnavigatie (Docent/Dossier/Admin + "← Studentzone") blijft op elk scherm zichtbaar en functioneel, `AdminApp.tsx`'s eigen vijf-tabblad-nav toont vijf echte routes (accessibility-snapshot bevestigt `/url` per link).
  - **(b)** Onboarding volledig heen doorlopen (`welcome` → klik "Start onboarding" → `waarom`, daarna direct-URL naar `tooling` → klik "Volgende: afronden" → `klaar`) en terug: op `waarom` een "← Vorige"-link geklikt die daadwerkelijk terugnavigeert naar `welcome` (niet alleen een correcte `href`); op `tooling`/`klaar` toont de stapindicator stappen 1-5 (of 1-6 op `klaar`) als ✓ en klikbaar (`<Link>`, `/url` klopt), upcoming-stappen als niet-klikbare `<span>`. `Welcome.tsx`'s `closing`-variant (`klaar`) toont zowel een terugknop (naar `tooling`) als de "Verder naar week 1 →"-knop.
  - **(c)** Studentzone (`/team/*`, `/onboarding/*`) toont uitsluitend Team/Rooster/Onboarding + "Docentzone →"; nooit Docent/Dossier/Admin. Docentzone (`/instructor/*`, `/admin/*`) toont uitsluitend Docent/Dossier/Admin + "← Studentzone"; nooit Team/Rooster/Onboarding. Beide geverifieerd via accessibility-snapshots op `/team/team-07/direct`, `/team/team-07/schedule`, `/instructor/dashboard`, `/instructor/dossier`, en alle vijf `/admin/*`-routes.

### Commit & push: Task 22
- [x] Commit in `operational-excellence-course` (LRD/TDD-tekst) — apart van de code-commit. ✅ Al gedaan (commit `643b633`, vóór deze bouwsessie); deze sessie voegt alleen de buildplan-afvinking toe.
- [x] Commit in `oe-gate-system` (code), boodschap verwijst naar FR-32–35.
- [x] Push beide.
- [x] Vink af: **Main Task 22 afgerond**.

---

## Main Task 23: Onderwijsprogramma — cursusbreed programma-overzicht (samenvoeging Rooster + Journey-view)

*(Golf: vereist Task 22 gepusht (heeft `nav-config.ts`/`AppShell` nodig voor de nav-labelwijziging en de zone-indeling) én Task 14 gepusht (hergebruikt sessiedata) — beide al hetzij gepland hetzij gedaan. Niet parallelliseerbaar met Task 22: bouwt er direct op voort.)*

**Waarom een aparte task i.p.v. Task 17 stilzwijgend herschrijven:** Task 17 ("Student journey-view") staat al gepland maar is nooit gebouwd (17.1–17.4 nog `- [ ]`) en had zelf al geen LRD/FR-basis (TDD oud-6.10 citeerde, in tegenstelling tot elke andere §6.x-subsectie, geen enkel FR-nummer). De opdrachtgever breidde de eis tijdens de navigatie-herziening uit tot een samenvoeging met de al gebouwde "Rooster"-view (Task 14) plus concrete nieuwe content (cohortbrede teamposities, weekthema+boeklink, eigen CBI-datum). Dat is meer dan Task 17's oorspronkelijke tekst dekte, dus een eigen task i.p.v. Task 17 stilzwijgend herschrijven — zelfde aanpak als Task 21 t.o.v. Task 16.

**Bestanden:**
- Modify: `design-documents/lrd-operational-excellence.html` (Deel 6.18, FR-36, NFR-14, FR-26-herziening, Deel 6.15/Deel 8-inleiding), `design-documents/tdd-operational-excellence.html` (Deel 6.13, Deel 6.10-verwijzing, Deel 6.8, Deel 8.1-matrix)
- Create (`oe-gate-system`): `frontend/src/routes/team/Programma.tsx` (of hernoemde `Schedule.tsx` — implementatiekeuze, zie TDD 6.13)

**Daadwerkelijk (23.4-23.8, ná build):**
- Created (`oe-gate-system`): `frontend/src/routes/team/Programma.tsx`, `Programma.css`, `weekProgramma.ts`, `agents/tests/test_dashboard_query.py`
- Modified (`oe-gate-system`): `agents/dashboard_query/agent.py` (`build_cohort_positions`/`TeamPositionRow`/`CohortPositionsResult`), `agents/team_api/service.py` (`get_cohort_positions`), `agents/team_api/server.py` (nieuwe route + concurrency-lock-bugfix), `agents/admin_api/server.py` (concurrency-lock-bugfix), `frontend/src/nav-config.ts`, `frontend/src/App.tsx`, `frontend/src/components/AppShell.tsx`, `frontend/src/routes/onboarding/Praktisch.tsx`, `frontend/src/routes/team/api.ts`
- Deleted (`oe-gate-system`): `frontend/src/routes/team/Schedule.tsx`, `Schedule.css`

- [x] **23.1** LRD Deel 6.18 (Onderwijsprogramma) geschreven, samen met Task 22.1: FR-36 toegevoegd aan Deel 7.1, NFR-14 toegevoegd aan Deel 7.2, FR-26 herzien (verplicht → optioneel), Deel 6.15 en de Deel 8-inleiding bijgewerkt met de optionaliteits-toelichting. ✅
- [x] **23.2** TDD Deel 6.13 (Onderwijsprogramma: consolidatie, databronnen-hergebruik, de begrensde cross-team-uitzondering) geschreven, samen met Task 22.2: Deel 6.10 aangepast om naar 6.13 te verwijzen, Deel 6.8 bijgewerkt voor de FR-26-herziening + naamswijziging, Deel 8.1-matrix uitgebreid met FR-36/NFR-14. ✅
- [x] **23.3** Docent-sanity-check op de nieuwe LRD/TDD-tekst vóór commit — met name de cross-team-privacygrens (NFR-14) en de FR-26-herziening (optioneel i.p.v. verplicht). ✅ Akkoord ontvangen (13 juli 2026).
- [x] **23.4** Backend: nieuwe, gereduceerde team-scoped query op de Dashboard Query Agent (5.3) — hergebruikt de bestaande cohortbrede aggregatie (`query_dashboard`), maar projecteert voor het student-endpoint uitsluitend `team_id`+huidige `stage`, nooit scores/namen (NFR-14). Test: een student-request kan nooit meer dan teamnaam+fase van andere teams terugkrijgen. ✅ `build_cohort_positions()` (`agents/dashboard_query/agent.py`) hergebruikt uitsluitend `_discover_team_ids`/`_query_team_status` uit `build_dashboard`'s eigen helpers — roept `_query_hidden_score`/`_query_overrides` NOOIT aan (dus `rubric_scores`/`override_log` worden op dit pad niet eens geopend, laat staan gelezen); `TeamPositionRow`/`CohortPositionsResult` zijn `extra="forbid"` met precies twee resp. drie velden. Blootgesteld via een nieuw `GET /api/cohort-positions` op `team_api` (het student-gerichte kanaal, niet de instructor-chat-agent). Structurele test bewijst de garantie door `_query_hidden_score`/`_query_overrides` te monkeypatchen zodat ze een `AssertionError` gooien als ze ooit worden aangeroepen — geen enkele test hoeft daarna nog te "hopen" dat de huidige implementatie toevallig niets teveel teruggeeft.
- [x] **23.5** Consolideer `Schedule.tsx` en de nooit-gebouwde `Journey.tsx`-scope tot één component: eigen positie + gate-geschiedenis (hergebruik bestaande team-scoped dashboard-tool), weekthema+hoofdstuk uit de LRD Deel 8-tabel (statisch, meegeleverd als data), boeklink naar de bijbehorende wiki-conceptpagina, activiteiten uit de (nu optionele) sessiedata, eigen CBI-datum uit `assessment-schedule`, en de nieuwe cohortbrede teamposities uit 23.4. ✅ Gebouwd als `frontend/src/routes/team/Programma.tsx` (naamgevingskeuze: zie 23.6-notitie), route `/team/:teamId/programma`. "Eigen positie + gate-geschiedenis" hergebruikt uiteindelijk `build_checkpoint_view` (Task 5, al score-vrij getest) voor de huidige fase, NIET een letterlijk nieuw team-scoped tool op de Dashboard Query Agent — TDD-oud-6.10 (bewaard voor traceerbaarheid in TDD 6.13) beschrijft die laatste expliciet als "een NIEUWE tool" die nooit gebouwd is (Task 17 verviel); een tweede, redundante tool bouwen puur om die letterlijke formulering te volgen had TDD 6.13's eigen kernregel ("allemaal hergebruik, geen nieuw datamodel") juist geschonden. `weekProgramma.ts`: 18 weken handmatig overgetypt uit LRD Deel 8, incl. boeklinks naar de echte `oe-wiki`-conceptpagina's (`ch01`…`ch19`, geverifieerd tegen de daadwerkelijke `oe-wiki`-checkout). **Belangrijke bevinding:** de `oe-wiki` GitHub Pages-site is nog NIET live — `GET /repos/businessdatasolutions/oe-wiki/pages` geeft 404, en de eigen "Deploy Quartz site to GitHub Pages"-workflow faalt op elke push tot nu toe (geverifieerd via `gh run list`/`gh api`). De boeklinks worden desondanks gebouwd en getoond (dat is een deploymentkwestie in een ander repo, geen frontend-bug in dit repo) — vandaag 404-en ze dus in een echte browser.
- [x] **23.6** Nav-label + koptekst "Rooster" → "Onderwijsprogramma" in `nav-config.ts` (Task 22) en op het scherm zelf. ✅ Tevens `App.tsx`/`AppShell.tsx`/`Praktisch.tsx` (onboarding-verwijzing) bijgewerkt. **Naamgevingskeuze (TDD 6.13 liet dit bewust open):** bestand heet `Programma.tsx`, geen hernoemde `Schedule.tsx` — TDD Deel 6.12's eigen routetabel noemt het doelcomponent al letterlijk `Programma.tsx` op route `/team/:teamId/programma`; die naam volgen voorkomt een tweede, los daarvan verzonnen naam voor hetzelfde scherm. `Schedule.tsx`/`Schedule.css` zijn verwijderd.
- [x] **23.7** Verifieer of `Sessions.tsx`/de sessieplanning-validatie (Task 14) vandaag "precies één moment per week" hard afdwingt; indien ja, maak dat optioneel conform de herziene FR-26 — te verifiëren in `oe-gate-system`, niet aangenomen. ✅ **Bevinding: geen enkele "precies één sessie per week"-afdwinging bestaat vandaag** — niet in het `Session`-schema (`data/smdl/okf.py`, `week` is een kale `int`, geen unieke-sleutelbeperking), niet in `create_session`/`update_session` (`agents/admin_api/routes.py`, enige controle is of `session_id` al bestaat, niets over `week`), niet in `Sessions.tsx` (het formulier staat gewoon toe meerdere sessies voor dezelfde week aan te maken; `test_list_sessions_returns_every_session_in_klas_grouped_by_week` bewijst dit zelfs al impliciet). Er was dus niets te versoepelen — de bestaande implementatie was al verenigbaar met de herziene, optionele FR-26; dit is nu expliciet met een test vastgelegd i.p.v. stilzwijgend verondersteld.
- [x] **23.8** Schrijf/breid tests uit: cross-team-projectie bevat nooit scores/namen (adversarial test, zelfde patroon als Task 10's hallucinatietest); Onderwijsprogramma-component-render met en zonder ingevulde sessiedata. ✅ `agents/tests/test_dashboard_query.py` (12 nieuwe tests): schema-garanties (`extra="forbid"`, precies de toegestane velden), de structurele "nooit-aangeroepen"-monkeypatchtest, content-niveau-check dat gezaaide score/instructor-note-waarden nergens in de JSON-payload voorkomen, en een test op de `team_api.service`-wrapper. Component-render "met en zonder sessiedata": dit codebase heeft nergens een frontend-testrunner (geen vitest/RTL/jest in `package.json`, consistent met alle voorgaande tasks) — bewezen via een echte Playwright-doorloop tegen een draaiende `npm run dev` + `team_api`/`admin_api` (zie Test Gate hieronder): week 1/6 tonen echte sessiedata, de overige 16 weken tonen stuk voor stuk "Geen sessie gepland voor deze week." zonder fout.

### Test Gate — Task 23
- **Automatisch:** ✅ nieuwe tests slagen (12/12, incl. de expliciete cross-team-privacytest NFR-14 en de structurele "helper nooit aangeroepen"-test); `tsc --noEmit`/`npm run build`/`npm run lint` schoon; volledige suite 249 passed, 1 skipped (was 237 passed, 1 skipped — uitsluitend de 12 nieuwe tests toegevoegd, bestaande suite ongewijzigd groen).
- **Mens-testbaar artefact:** ✅ Playwright tegen een echte `npm run dev` + draaiende `team_api.server`(8801)/`admin_api.server`(8799), met echte gezaaide lakehouse-data (team-05/07/09/11/13/15 op resp. direct/direct/direct/design/deliver/develop-fase, team-19 zonder gate_events, één CBI-datum voor team-07/Fenna, sessies in week 1 en 6). `/team/team-07/programma` toont: eigen positie (Direct, Gate A open/Gate B pending, geen cijfer zichtbaar), CBI-datum (Fenna — 2026-12-08 · 13:00-13:30 · Arnhem), de andere zes teams uitsluitend op teamniveau (team-ID + faseprogressie-widget, nooit namen/cijfers — geverifieerd zowel in de UI als in de rauwe `/api/cohort-positions`-JSON), en het volledige 18-weekse programma met boekthema+wikilink+"Vorm"+activiteiten per week; weken 1 en 6 tonen de echte sessie, de overige 16 weken tonen "Geen sessie gepland voor deze week." — geen foutmelding. `/team/team-19/programma` (team zonder gate-geschiedenis) toont "Nog geen checkpoint gestart" + "Nog geen CBI-datum gepland" — geen crash, geen 500. Nul console-errors op beide routes. Tijdens deze doorloop een echte, reproduceerbare bug gevonden en gefixt (zie commit-boodschap): `Programma.tsx` was het eerste scherm dat twee gelijktijdige requests naar `admin_api` vuurt die hetzelfde klas-lakehouse-catalog openen, wat een DuckLake-attach-race blootlegde (`Binder Error: Unique file handle conflict`) — opgelost met een serialiserende lock per server (`admin_api/server.py`, `team_api/server.py`), vóór de fix reproduceerbaar met kale `curl`, ná de fix 10/10 gelijktijdige requests succesvol.

### Commit & push: Task 23
- [x] Commit in `operational-excellence-course` (LRD/TDD-tekst) — apart van de code-commit. (23.1-23.3, eerdere sessie)
- [x] Commit in `oe-gate-system` (code), boodschap verwijst naar FR-26/FR-36/NFR-14. ✅ commit `7621091`
- [x] Push beide.
- [x] Vink af: **Main Task 23 afgerond**.

---

## Main Task 24: Studentportaal — vijf kernviews (Programma/Onboarding/OE-Wiki/Socratische AI-tutor/Review-ring)

*(Golf 15: vereist Task 22 (AppShell/nav-config) én Task 23 (Programma.tsx) gepusht; niet parallelliseerbaar met iets anders dat `nav-config.ts`/`AppShell.tsx` aanraakt.)*

**Waarom een aparte task:** een nieuw claude.ai/design-mockup (`Studentportaal.dc.html`, geïmporteerd via de `claude_design`-MCP-tool) werkt de studentzone-navigatie uit Task 22 verder uit tot vijf persistente tabs in plaats van drie. Vergelijking met het al gebouwde systeem legde twee soorten werk bloot: een pure visuele retrofit (Programma, al gebouwd in Task 23) en drie echte scope-uitbreidingen die verder gaan dan wat vandaag bestaat — een in-app OE-Wiki-browser (vandaag uitsluitend een externe Quartz-link, NFR-02 regelt expliciet dat de wiki geen nieuwe hosting krijgt), een persistente Socratische AI-tutor-tab met de volledige vier-gates-geschiedenis (vandaag toont `CheckpointView.tsx` alleen de huidige fase), en een dedicated Review-ring-tab (vandaag een kaart binnen `CheckpointView.tsx`, geen eigen route). De opdrachtgever is expliciet geraadpleegd over de gewenste scope (niet aangenomen): gekozen is voor de volledige uitbouw van alle vier tabs, mét een nieuwe live leeslaag voor de wiki-tab (een verdergaande, expliciet te bevestigen NFR-02-uitbreiding, zie LRD 6.19/TDD 2.2/5.10). Een vijfde tab, Onboarding, is op uitdrukkelijk verzoek toegevoegd nadat de eerste planontwerp die miste: de al bestaande zesstappen-flow (Task 16/21) blijft **inhoudelijk volledig ongewijzigd** en wordt uitsluitend als persistente `nav-config.ts`-entry geregistreerd, met dezelfde `AppShell`-nesting als de andere vier tabs — expliciet niet als apart routeboompje of aparte bundle, om niet exact het neveneffect te herhalen dat Task 22 net oploste voor het admin-onderdeel.

**Bestanden:**
- Modify: `design-documents/lrd-operational-excellence.html` (Deel 6.19, FR-35-herziening, FR-37–40, NFR-15, AC-17, Deel 12), `design-documents/tdd-operational-excellence.html` (Deel 6.12-routetabel, Deel 6.14, Deel 2.2, Deel 5.10, Deel 6.1-notitie, Deel 8.1/8.2-matrix, Deel 14)
- Create (`oe-gate-system`): `frontend/src/routes/team/WikiBrowser.tsx`(+`.css`), `frontend/src/routes/team/TutorHistory.tsx`(+`.css`), `frontend/src/routes/team/ReviewRingStatus.tsx`(+`.css`), `agents/wiki_query/agent.py`(+service+tests)
- Modify (`oe-gate-system`): `frontend/src/nav-config.ts`, `frontend/src/components/AppShell.tsx`(+`.css`, Studentportaal-tokenretrofit per 24.4), `frontend/src/App.tsx` (drie nieuwe routes), `frontend/src/routes/team/CheckpointView.tsx` (peer-partner-kaart-consolidatie per 24.3), `agents/team_api/service.py`/`server.py` (nieuwe endpoints voor tutor-history en ring-status)

- [x] **24.1** LRD Deel 6.19 ("Studentportaal: geïntegreerde navigatie") schrijven: vijf-tabs-structuur, FR-35-herziening, FR-37–40, NFR-15, AC-17, plus de drie open beslissingen expliciet benoemd in Deel 12 (checkpointscherm-consolidatie, tokenreconciliatie met Task 11, NFR-02-uitbreiding voor de wiki-leeslaag). ✅ Geschreven direct in `lrd-operational-excellence.html`.
- [x] **24.2** TDD Deel 6.14 ("Studentportaal: AppShell + vijf kernviews") schrijven, samen met Deel 2.2 (NFR-02-uitbreiding), Deel 5.10 (nieuwe Wiki Query Service), Deel 6.1 (sibling-route-notitie bij de peer-partner-kaart), Deel 6.12-routetabel (drie nieuwe rijen + onboarding-aantekening), Deel 8.1/8.2-matrix (FR-37–40/NFR-15/AC-17) en Deel 14 (drie nieuwe open-beslissing-rijen). ✅ Geschreven direct in `tdd-operational-excellence.html`.
- [x] **24.3** Docent-sanity-check op de nieuwe LRD/TDD-tekst vóór commit — met name de drie expliciet opengelaten beslissingen. ✅ Opdrachtgever gaf expliciet de opdracht om de frontend te bouwen ná review van de LRD/TDD-tekst; onderstaande defaults zijn daarbij toegepast en hier gedocumenteerd, niet stilzwijgend aangenomen: (a) de peer-partner-kaart in `CheckpointView.tsx` **blijft bestaan** naast de nieuwe Review-ring-route (niet-destructieve default: allebei lezen dezelfde data, verwijderen kan altijd nog later); (b) het Studentportaal-kleurenpalet leeft **naast** de bestaande HAN Huisstijl-tokens, gescoped tot uitsluitend de drie nieuwe componenten via een `.studentportaal`-wrapper-class (`studentportaal-palette.css`) — geen enkele bestaande stylesheet aangeraakt; (c) de live `wiki_query`-leeslaag is gebouwd (NFR-02-uitbreiding geaccepteerd). Alle drie keuzes zijn herzienbaar; niets hieronder sluit een latere heroverweging uit.
- [x] **24.4** Design-tokens uit `Studentportaal.dc.html` geëxtraheerd (merkroze `#e50056`, Archivo/Public Sans, pill-vormige statusbadges) naar `frontend/src/styles/studentportaal-palette.css`. ✅ Gescoped onder een `.studentportaal`-wrapper-class (niet `:root`), toegepast door alle drie de nieuwe componenten; `han-huisstijl-tokens.css` (Task 11) en alle bestaande, al-gescreenshotte schermen blijven volledig ongewijzigd — zie 24.3(b).
- [x] **24.5** `agents/wiki_query/agent.py`: `list_wiki_pages()`/`get_wiki_page(id)`. ✅ Hergebruikt `wiki_quality_check.wiki_index.scan_wiki()`/`WikiPage` in plaats van een derde onafhankelijke frontmatter-parser te schrijven (er bestonden al twee: `socratic_tutor.tools.query_wiki`'s eigen parsing en `wiki_index.scan_wiki`). `@log_action(layer="agentic")` op beide, zelfde patroon als `query_wiki`. Nieuw package geregistreerd in `pyproject.toml`. 9 tests (`agents/tests/test_wiki_query.py`): fixture-wiki (lijst/detail/not-found) + een sanity-check tegen de echte `oe-wiki`-checkout (66 pagina's — de 4 `week-landing`-pagina's vallen bewust buiten `scan_wiki`'s bestaande scope, gedocumenteerd in de test, geen bug).
- [x] **24.6** `frontend/src/routes/team/WikiBrowser.tsx`(+`.css`): lijst gegroepeerd op type + leespaneel (mockup 1-op-1), gevoed door 24.5's endpoints via twee nieuwe routes (`GET /api/wiki-pages`/`GET /api/wiki-page`) op `team_api`. ✅ Zoekveld bewust een disabled placeholder, exact zoals in de mockup zelf (gedocumenteerde, geen ontbrekende functionaliteit). Wiki-body is ongefilterde markdown (geen renderer-dependency toegevoegd, KISS): de redundante `# Titel`-regel wordt gestript, de rest toont met `white-space: pre-wrap`.
- [x] **24.7** Nieuw student-facing endpoint (`GET /api/tutor-history` → `team_api.service.build_tutor_history`) dat alle vier gates' rapporten teruggeeft, hergebruikt `socratic_tutor.tools.read_team_facing_report` per fase (dezelfde functie die `SocratesCard` al voor de huidige fase gebruikt), structureel zonder scoreveld (`TutorHistoryEntry`/`TutorHistoryView`, `extra="forbid"`). ✅ Status per fase (afgerond/open/vergrendeld) zelfstandig afgeleid uit welke fases al een rapport hebben — geen afhankelijkheid van `dashboard_query`'s aparte "huidige positie"-begrip. 4 tests (`agents/tests/test_tutor_history.py`), incl. een AC-03-stijl structurele score-veld-check.
- [x] **24.8** `frontend/src/routes/team/TutorHistory.tsx`(+`.css`): vier-gates-overzicht met status (afgerond/vergrendeld/open) + Socratische vragen per afgeronde gate, gevoed door 24.7. ✅
- [x] **24.9** `frontend/src/routes/team/ReviewRingStatus.tsx`(+`.css`): stappen-tracker (Inleveren/Reviewen/Verwerken) + escrow-gegeven/-ontvangen. ✅ Geen nieuw backend-endpoint: hergebruikt `fetchCheckpointView`'s bestaande `peer_partner`-veld (zelfde data als `CheckpointView.tsx`'s peer-partner-kaart), plus `fetchCohortPositions` om de eigen huidige fase te bepalen (zelfde patroon als `Programma.tsx`). "Reviewen"-stap is afgerond zodra `own_review_given && feedback_received` allebei waar zijn.
- [x] **24.10** Onboarding: geverifieerd dat dit al een gewone `NavItem` in `nav-config.ts` was (zelfde `{zone, path, label, match}`-vorm, genest onder dezelfde `AppShell`) vóórdat deze task begon — niets aan te passen, de architectuur-eis was al voldaan.
- [x] **24.11** `nav-config.ts` uitgebreid met drie nieuwe `NavItem`s (OE-Wiki/Socratische AI-tutor/Review-ring, elk met een eigen `isXPath()`-predicaat; `isTeamPath()` aangepast om de drie nieuwe paden uit te sluiten, zelfde patroon als de bestaande Team/Onderwijsprogramma-uitsluiting); `App.tsx` kreeg de drie nieuwe routes (`/team/:teamId/wiki`/`tutor`/`ring`, gedeclareerd vóór de generieke `:stage`-route). `CheckpointView.tsx`'s peer-partner-kaart **behouden** (24.3(a)). `AppShell.tsx` zelf ongewijzigd (mapt al generiek over `NAV_ITEMS`).
- [x] **24.12** Playwright-walkthrough tegen een echte `npm run dev` + draaiende `team_api`(8801)/`admin_api`(8799)-servers. ✅ Alle vijf tabs bezocht via echte URL's en via navigatie-klik, nul console-errors/-warnings op alle vijf. Volledige happy-path live gedreven: checkpoint ingediend → Socratische tutor gevraagd (Gate A open) → eigen peer-review ingediend aan team-09 (Gate B open, escrow-onthulling van team-05's al-geseede review zichtbaar) → Tutor-tab toont Direct "Afgerond" met de echte vragen, Design "Nu bezig"; Review-ring-tab toont Inleveren/Reviewen "Afgerond", Verwerken "Gegeven"/"Ontvangen". Regressiecontrole: Programma (Task 23), Onboarding (Task 16/21/22), Instructor Dashboard (Task 4/11, bekende gedocumenteerde mock-fallback-warning ongewijzigd) en Admin (Task 9/22) alle vier bezocht, nul nieuwe console-errors.

### Test Gate — Task 24
- **Automatisch:** ✅ `.venv/bin/python -m pytest -q`: **277 passed, 1 skipped** (was 249/1 vóór deze task; 13 nieuwe tests: 9 `wiki_query` + 4 `tutor_history`, bestaande suite ongewijzigd groen). `tsc --noEmit` schoon. `npm run build` schoon. `npm run lint` (oxlint) schoon, exit 0.
- **Mens-testbaar artefact:** ✅ Playwright-walkthrough zoals hierboven (24.12) beschreven — alle vijf tabs, volledige happy-path live gedreven, nul console-errors, nul regressies op de vier bestaande schermen.

### Commit & push: Task 24
- [ ] Commit in `operational-excellence-course` (LRD/TDD-tekst) — apart van de code-commit.
- [ ] Commit in `oe-gate-system` (code), boodschap verwijst naar FR-37/38/39/40/NFR-15/AC-17.
- [ ] Push beide.
- [ ] Vink af: **Main Task 24 afgerond**.

---

## Main Task 25: Klas-scoping — peers op Onderwijsprogramma & docentoverzicht per klas

*(Golf 16: vereist Task 24 gepusht; niet parallelliseerbaar met iets anders dat `dashboard_query.agent`/`Programma.tsx`/`Dashboard.tsx` aanraakt.)*

**Waarom een aparte task i.p.v. Task 23 stilzwijgend herschrijven:** de opdrachtgever wees erop dat het Onderwijsprogramma-scherm (Task 23) "de andere teams" cohortbreed toont, terwijl elke andere klas-gescoped constructie in dit systeem (de review-ring, Task 4; de klas-lakehouse-uitzondering, Task 2/9) al klas is, nooit cohort — en vroeg om hetzelfde soort peer-overzicht ook expliciet klas-gescoped, plus een docentzijdig equivalent ("voor docenten zou het handig zijn om dit per klas te kunnen zien"). Dit is een scope-correctie op een al gebouwde, geteste functie (Task 23.4/23.5), niet een geheel nieuwe capability — zelfde aanpak als Task 21 t.o.v. Task 16 en Task 23 t.o.v. Task 17: een eigen task, geen stille herschrijving van de vorige.

**Bestanden:**
- Modify: `design-documents/lrd-operational-excellence.html` (Deel 6.7, 6.18, nieuwe Deel 6.20, FR-36-herziening, NFR-14-herziening, FR-41, AC-18), `design-documents/tdd-operational-excellence.html` (Deel 6.13-herziening, nieuwe Deel 6.15, Deel 8.1/8.2-matrix, Deel 14)
- Modify (`oe-gate-system`): `agents/dashboard_query/agent.py` (`build_cohort_positions` krijgt verplichte `klas_id`; `TeamDashboardRow`/`build_dashboard` krijgt `klas_id`-veld), `agents/team_api/service.py`/`server.py` (`/api/cohort-positions` krijgt verplichte `klas_id`-queryparameter), `frontend/src/routes/team/api.ts` (`fetchCohortPositions(cohort, klasId)`), `frontend/src/routes/team/Programma.tsx` + `frontend/src/routes/team/ReviewRingStatus.tsx` (beide callers geven eigen `klasId` mee), `frontend/src/routes/instructor/Dashboard.tsx` (teamkaarten gegroepeerd per klas, incl. mockdata), `agents/tests/test_dashboard_query.py` (klas-isolatie- en `klas_id`-tests)

- [x] **25.1** LRD Deel 6.7 (instructor-dashboard, klas-groeperingszin), Deel 6.18 ("dezelfde klas" i.p.v. "het cohort"), nieuwe Deel 6.20 ("Klas-scoping: peers en docentoverzicht per klas"), FR-36-herziening, NFR-14-herziening, FR-41, AC-18 schrijven. ✅ Geschreven direct in `lrd-operational-excellence.html`.
- [x] **25.2** TDD Deel 6.13-herziening ("klas- i.p.v. cohortbreed"), nieuwe Deel 6.15 (backend-mechaniek: hergebruik van `team-roster.klas_id`, geen nieuwe registratie), Deel 8.1/8.2-matrix (FR-36-revisie, FR-41, AC-18), Deel 14 (Dashboard.tsx-klas-groepering blijft tegen het al-bekende niet-live-endpoint-punt van Task 4) schrijven. ✅ Geschreven direct in `tdd-operational-excellence.html`.
- [x] **25.3** Docent-sanity-check op de nieuwe LRD/TDD-tekst vóór commit. ✅ Opdrachtgever gaf expliciet de opdracht om te implementeren ná het formuleren van de klas-scoping-eis zelf; geen aanvullende open vragen bleven over (in tegenstelling tot Task 24 had deze task geen product-ambiguïteit die eerst beantwoord moest worden — de scope was door de opdrachtgever zelf al ondubbelzinnig geformuleerd).
- [x] **25.4** `agents/dashboard_query/agent.py`: `build_cohort_positions(cohort, klas_id, root=...)`. ✅ Nieuwe `_discover_klas_ids`/`_team_ids_in_klas`/`_team_klas_map`-helpers, rechtstreeks `team-roster`-concepten gelezen uit de klas-lakehouse (`Lakehouse.for_klas` + `Bundle.iter_concepts`), geen cross-import van `admin_api`. 3 nieuwe tests incl. de load-bearing `test_team_from_a_different_klas_never_appears` (AC-18): twee klassen geseed, klas A bevat nooit klas B's team en vice versa.
- [x] **25.5** `TeamDashboardRow`/`build_dashboard`: `klas_id`-veld toegevoegd (default `None`), gevuld via `_team_klas_map` (één scan over alle klassen per `build_dashboard`-aanroep, niet per team). ✅ 3 tests: correcte vulling, cohortbreed blijft ongefilterd (FR-41 is geen toegangswijziging), `None` voor een team zonder roster (geen crash).
- [x] **25.6** `team_api`: `/api/cohort-positions` krijgt een verplichte `klas_id`-queryparameter (400 zonder, zelfde `_require`-patroon als `team_id`/`stage`); `team_api/service.py`'s `get_cohort_positions(cohort, klas_id, ...)` geeft `klas_id` door aan `build_cohort_positions`. ✅
- [x] **25.7** `frontend/src/routes/team/api.ts`: `fetchCohortPositions(cohort, klasId)`. ✅ Twee callers aangepast (niet één zoals aanvankelijk aangenomen): `Programma.tsx` (al-bekende `klasId` uit de bestaande sessie-/aanwezigheids-aanroepen) én `ReviewRingStatus.tsx` (Task 24, kreeg een nieuwe `DEFAULT_KLAS`-constante) — de tweede caller kwam pas aan het licht tijdens `npm run build` (tsc-fout), niet vooraf voorzien, gedocumenteerd i.p.v. stilzwijgend gemist. `CohortPositionsCard`'s copy aangepast van "in het cohort" naar "in jullie klas".
- [x] **25.8** `frontend/src/routes/instructor/Dashboard.tsx`: teamkaarten gerenderd in secties per klas (`klasGroups`, `useMemo`, onbekende klas laatst gesorteerd); mockdata uitgebreid met `klas_id` per team plus een vierde mockteam (`team-11`, `klas-b`) zodat de groepering nu al zichtbaar/klikbaar is met twee klassen. ✅
- [x] **25.9** Playwright-walkthrough tegen een echte `npm run dev` + draaiende `team_api`/`admin_api`. ✅ Vier teams geseed via `admin_api.routes.create_team` (team-05/07/09 → klas-a, team-11 → klas-b): Onderwijsprogramma (`/team/team-07/programma`) toont uitsluitend team-05/team-09 onder "Waar staan de andere teams in jullie klas", team-11 nooit; Instructor Dashboard toont twee secties ("klas-a" met 3 kaarten, "klas-b" met 1 kaart); nul console-errors op beide. Regressiecontrole: Review-ring-tab (Task 24) werkte in eerste opzet niet door de `fetchCohortPositions`-signatuurwijziging (zie 25.7), gefixt en herverifieerd; Onboarding/Admin ongewijzigd (niet geraakt door deze task).

### Test Gate — Task 25
- **Automatisch:** ✅ `.venv/bin/python -m pytest -q`: **281 passed, 1 skipped** (was 277/1; 4 nieuwe tests in `test_dashboard_query.py`, bestaande suite ongewijzigd groen). `tsc --noEmit`/`npm run build`/`npm run lint` schoon.
- **Mens-testbaar artefact:** ✅ Playwright-walkthrough zoals 25.9 hierboven beschreven, incl. de expliciete negatieve controle (team-11 nooit zichtbaar in klas-a's overzicht, en vice versa).

### Commit & push: Task 25
- [ ] Commit in `operational-excellence-course` (LRD/TDD-tekst) — apart van de code-commit.
- [ ] Commit in `oe-gate-system` (code), boodschap verwijst naar FR-36/FR-41/NFR-14/AC-18.
- [ ] Push beide.
- [ ] Vink af: **Main Task 25 afgerond**.

---

## Main Task 26: Socratische vragen in het studentdossier (CBI-koppeling)

*(Golf 17: sequentieel; raakt `dashboard_query.prep_agent`, `admin_api/server.py`, `StudentDossier.tsx`, `Dashboard.tsx` — niet parallelliseerbaar met iets anders dat deze bestanden aanraakt.)*

**Waarom een aparte task:** de opdrachtgever stelde vast dat de bedoelde leerlus (team analyseert casus → wordt in de wiki geïngest → tutor reageert de volgende gate met Socratische vragen → die vragen bewaard voor de docent bij het CBI) op één na volledig als ontworpen bestond, maar dat de Socratische vragen de docent nergens terug bereikten: `build_student_timeline` (Task 10) las alleen `gate_events`/`wiki_quality_log`/`reviewring_log`, en de dossier-JSON-endpoints waren gedocumenteerd maar nooit geregistreerd (het scherm viel terug op mockdata). Dit is een gerichte scope-uitbreiding van Task 10, geen nieuwe capability. Ontwerpbeslissing vooraf voorgelegd en vastgelegd in het plan `~/.claude/plans/ik-denk-dat-ik-polymorphic-blum.md`. Scope bewust beperkt tot de dossier/CBI-koppeling; de wiki-ingestie-faciliteiten (nog steeds `PENDING_WIKI_INTEGRATION`) zijn een aparte, latere ronde.

**Bestanden:**
- Modify: `design-documents/lrd-operational-excellence.html` (Deel 6.13, FR-23-herziening, nieuwe AC-19), `design-documents/tdd-operational-excellence.html` (Deel 5.8 + afhankelijkheidsrichting-notitie, Deel 6.6-herziening, Deel 8.1 FR-23-rij, nieuwe AC-19-rij)
- Modify (`oe-gate-system`): `agents/dashboard_query/prep_agent.py` (4e tijdlijnbron), `agents/admin_api/server.py` (twee endpoints geregistreerd), `frontend/src/routes/instructor/dossier/StudentDossier.tsx` (socratic-eventtype + fetches naar `admin_api`), `frontend/src/routes/instructor/Dashboard.tsx`(+`.css`) ("Open dossier"-link), `data/smdl/demo_seed.py` (Socratisch rapport in dossier-demo), `agents/tests/test_assessment_prep.py` + `agents/tests/test_admin_server.py`

- [x] **26.1** LRD/TDD-tekst: Deel 6.13 + FR-23 + AC-19 (LRD); Deel 5.8 (4e bron + afhankelijkheidsrichting), Deel 6.6-herschrijving, Deel 8.1/8.2-matrix (TDD). ✅ Geschreven direct in de HTML-documenten; de nooit-scores-garantie expliciet gemaakt op dossier-niveau (AC-19), naast de al bestaande AC-03/AC-17 op tutorniveau.
- [x] **26.2** `prep_agent.py`: vierde tijdlijnbron via nieuwe `_socratic_events`-helper (leest `socratic_tutor.tools.read_team_facing_report` per stage — bewust het **scorevrije** teamkanaal, niet `read_instructor_facing_report`), `TimelineEvent.event_type`+`source_table`-Literals uitgebreid met `"socratic"`/`"socratic_report"`, `source_ref` = `socratic_report#<team_id>-<stage>` (herhaalbaar via de reader), en een `_heuristic_point_for_socratic`-regel zodat een CBI-punt naar een Socratische vraag kan verwijzen. ✅ `detail` expliciet samengesteld met uitsluitend niet-score-velden (nooit de hele `InstructorFacingReport` gedumpt). PDF-export erft de vierde bron automatisch (hergebruikt `build_student_timeline`).
- [x] **26.3** `admin_api/server.py`: `GET /api/student-timeline` (`h_student_timeline`) en `POST /api/prep-summary` (`h_prep_summary`) geregistreerd in `ROUTES` — dunne adapters over de al `@log_action`-gedecoreerde tools, zelfde patroon als de al gewirede `h_export_student_history`. ✅ Dit is exact het gat waardoor `StudentDossier.tsx` terugviel op mockdata.
- [x] **26.4** `StudentDossier.tsx`: `'socratic'`-eventtype (uil-icoon + label), `fetchTimeline`/`fetchPrepSummary` wijzen nu naar `adminApiBase()` (port 8799) i.p.v. een relatief pad dat het Vite-origin raakte (de eigenlijke oorzaak van de mock-fallback), één Socratisch mock-event + prep-punt voor offline-pariteit, stale header-comment bijgewerkt, en `?team=`-prefill via `useSearchParams`. ✅ `tsc`/`lint` schoon.
- [x] **26.5** `Dashboard.tsx`(+`.css`): per teamkaart een "Open dossier →"-link (`/instructor/dossier?team=<team_id>`), zodat de al-geroute dossier vindbaar is vanaf het dashboard met team voorgeselecteerd. ✅
- [x] **26.6** `demo_seed.py`: `seed_student_dossier_demo` schrijft nu ook een Socratisch rapport voor de Direct-gate (lazy import van de agents-laag, zelfde patroon als `team_api.service`), zodat het mens-testbare artefact de vierde bron met échte data toont. ✅
- [x] **26.7** Tests: `test_assessment_prep.py` (4 nieuwe socratic-tests: herhaalbaar `source_ref`, waarde-scan dat de score 73.5 nergens lekt, chronologische sortering met de andere bronnen, CBI-punt citeert de socratic-bron; plus de bestaande isolatietest herschreven van "importeert nooit socratic_tutor" naar "leest alleen het scorevrije teamkanaal, nooit de score-dragende reader" — de kritische omgekeerde richting blijft afgedwongen) en `test_admin_server.py` (routes geregistreerd, lege-team-lijst, ontbrekende-param-400, prep citeert socratic). ✅

### Test Gate — Task 26
- **Automatisch:** ✅ `.venv/bin/python -m pytest -q`: **290 passed, 1 skipped** (was 281/1; 9 nieuwe tests, bestaande suite ongewijzigd groen). `tsc --noEmit`/`npm run build`/`npm run lint` schoon.
- **Mens-testbaar artefact:** ✅ Playwright-walkthrough tegen een echte `npm run dev` (5174) + draaiende `admin_api` (8799, `OE_ADMIN_API_ALLOWED_ORIGIN` op 5174 gezet voor CORS) met een geseed dossier-demo: na "Dossier laden" verdween de mock-badge, toonde de tijdlijn 8 gebeurtenissen incl. het Socratische-vragen-event met zichtbare `source_ref`, citeerde een CBI-voorbereidingspunt die bron, en was er nergens een cijfer/score zichtbaar (waarde-scan bevestigde: score 78 afwezig; "cijfer" alleen in de guardrail-disclaimer). De "Open dossier"-link op het dashboard vulde team-07 correct voor; de PDF-export bevatte de Socratische regel + bron zonder score; nul console-errors.

### Commit & push: Task 26
- [ ] Commit in `operational-excellence-course` (LRD/TDD-tekst) — apart van de code-commit.
- [ ] Commit in `oe-gate-system` (code), boodschap verwijst naar FR-23/AC-19.
- [ ] Push beide.
- [ ] Vink af: **Main Task 26 afgerond**.

---

## Main Task 27: Toegang — één persoonlijke link per student en per docent

*(Golf 18: vereist Task 7.2's tweede helft (duurzame `okf_bundle`) — een link uitdelen naar een roster die bij elke deploy verdwijnt, heeft geen zin.)*

**Waarom deze task bestaat:** het systeem had **geen enkele authenticatie**, en dat was geen zwakke plek maar een ontbrekende laag. `team_api`'s handlers lazen `team_id` uit het verzoek en geloofden het; `admin_api`'s actor was de constante string `"instructor"`; `StudentDossier.tsx` liet je élke voornaam en élk team intypen. `nav-config.ts` zei het zelf: zone-scoping is *"EXPLICITLY cosmetic... no server-side check exists anywhere"*. Lokaal prima, gedeployed onhoudbaar.

**Correctie op de aanleiding, expliciet vastgelegd:** het voorstel ging uit van "het systeem bevat geen persoonsgegevens". Dat klopt niet — voornaam + klas + team + inzendingen + aanwezigheid + survey + dossier is gepseudonimiseerde data, dus persoonsgegevens (`survey_responses` zegt het zelf: *"Name-linked, NOT anonymous"*), en LRD 9.2 rékent erop dat een docent een student aan zijn voornaam herkent. De links zijn daarom gerechtvaardigd als **proportionele** maatregel, niet omdat er niets te beschermen valt. Zie LRD 6.21, TDD 9.5.

**Bestanden:** Create: `agents/shared/identity.py`, `agents/admin_api/roster_import.py`, `agents/admin_api/access_sheet.py`, `frontend/src/auth.ts`, `frontend/src/routes/Toegang.tsx`, `frontend/src/routes/team/OwnTeamGuard.tsx`, `frontend/src/routes/admin/AccessLinks.tsx`. Modify: `data/smdl/ducklake.py` (`access_grants`, `bundle_log`), `data/smdl/okf.py` (`Bundle.append_log`, `KlasRoster.docent_first_name`), beide servers, beide API-clients, `templates/team-indeling-template.csv`.

- [x] **27.0** Prerequisite: `okf_bundle` duurzaam. ✅ Bucket gemount op `OE_LAKEHOUSE_ROOT`; alles in de bundel is write-once of whole-file-overwrite, dus objectopslag is er veilig. Eén uitzondering: `Bundle.append_log` voegde toe aan een gedeelde `log.md`, en objectopslag kan niet appenden — die verhuisde naar een `bundle_log`-tabel. Signatuur ongewijzigd, ~30 aanroepers onaangeroerd. Live bewezen vóór de fix: inzending zichtbaar → nieuwe revisie → verdwenen.
- [x] **27.1** `access_grants`-tabel: alleen `sha256(link)`, nooit de platte tekst. ✅ Bewust een DuckLake-tabel en geen OKF-concept: concepten zijn leesbare, git-diffbare markdown; een credentialstore hoort daar niet. `subject_id` in plaats van naam-matching — twee docenten "Jan" zouden anders elkaars klassen erven.
- [x] **27.2** `shared/identity.py` + handhaving in beide servers. ✅ Studentkant: de 14 handlers **lezen** `team_id` niet meer, ze leiden hem af. Docentkant: klas-claim centraal gevalideerd in het dispatch-pad (een docent heeft meerdere klassen en moet kiezen). `_OPEN_ROUTES` is een allowlist, dus een nieuwe route is authenticated tenzij iemand zichtbaar anders beslist.
- [x] **27.3** Frontend: credential in het URL-fragment. ✅ Beide servers loggen de volledige requestregel inclusief query-string; de statische server heeft geen log-override; Cloud Run logt paden buiten onze code om. Een geheim in een pad wordt op ≥4 plekken vastgelegd. Fragmenten sturen browsers nooit.
- [x] **27.4** Roster-import met `docentvoornaam`. ✅ **Niet** op `ingest_csv` gebouwd: die leest komma-gescheiden (de template is puntkomma, NL-Excel) en verwerpt `cohort` als gereserveerde kolom. Preview-dan-commit, want een typo in `docentvoornaam` geeft een vreemde toegang tot een klas.
- [x] **27.5** Printvel met QR-codes. ✅ POST, geen GET: alleen digests worden bewaard, dus een vel kan alleen worden geproduceerd dóór uit te geven — en dat vervangt de vorige links. Een GET zou via prefetch of refresh een klas buitensluiten. `reissue_one` bestaat zodat "Fenna is haar link kwijt" niet de andere 24 kost.
- [x] **27.6** Klas-gescoped docentdashboard + `/api/override`. ✅ `Dashboard.tsx` haalde `/api/dashboard` op sinds Task 4 en kreeg altijd 404 — het docentscherm toonde al die tijd verzonnen teams. `OverrideLogRow` kreeg `instructor_id`: tot deze task kón het systeem niet weten wie overrode.
- [x] **27.7** Walkthrough tegen echte servers. ✅ Vier bugs gevonden die alle unittests groen doorstonden — zie hieronder.

### Test Gate — Task 27
- **Automatisch:** ✅ 427 passed / 2 skipped (was 335). Tegenproeven uitgevoerd op de load-bearing tests: de vertrouwende `team_id` terugzetten faalt 2 tests, authenticatie weghalen faalt er 12, de docent-only-check weghalen 1, klas-scoping weghalen 5.
- **Mens-testbaar artefact:** ✅ echte link in een echte browser: fragment uit de adresbalk, portal laadt als die student, credential in **geen enkele** serverlog — geverifieerd mét een controle die aantoont dat de zoekopdracht zou aanslaan als hij er wél stond.

**Wat de walkthrough vond en de tests niet:**
1. **De link wees naar een route die niet bestond.** `/toegang` zat in het printvel, de QR-codes en de docs — en was nooit gebouwd. Elke QR leidde nergens heen. Bovendien redirectte `/team` naar een hardgecodeerde `team-07`, dus élke student landde op één team.
2. **Het studentportaal las van de docent-API.** `Programma.tsx` haalde sessies en CBI-data van `admin_api`'s klas-brede routes, die terecht stopten met antwoorden. Dat legde iets ergers bloot: `listAssessments` stuurde het **hele klasrooster** — elke klasgenoot met naam, datum, tijd, locatie — naar elke browser, en filterde client-side. Nu server-side gefilterd op identiteit.
3. **De UI labelde jouw data als die van een ander.** `/team/team-02/programma` toonde team-01's gegevens onder "TEAM-02". Niets lekte; de pagina loog over van wie het was. `OwnTeamGuard` corrigeert de URL.
4. **`tsc --noEmit` controleerde niets.** `tsconfig.json` is een project-references-stub (`"files": []`), dus `--noEmit` checkt nul bestanden en geeft exit 0 — élke "tsc clean" in dit project was een no-op, en het buildplan noemde die opdracht als conventie. De echte check is `tsc -b` (wat `npm run build` draait) en vond meteen zes fouten.

### Commit & push: Task 27
- [x] Commits in `oe-gate-system`: `d40dd05`, `b3c5cb2`, `64d3aac`, `f732060`, `219d445`, `0211271`, `643d471`, `45d68b1`, `4542ae9`.
- [ ] Push.
- [x] Vink af: **Main Task 27 afgerond** (op 7.5/7.6 na, die een live deploy vragen).

---

## Main Task 28: OE-Wiki-tab — van tweederangs lezer naar contextuele index

*(Golf 19: vereist Task 27 (de credential levert de context).)*

**Waarom deze task bestaat:** de opdrachtgever vroeg wat de OE-Wiki-tab studenten eigenlijk opleverde. Eerlijk antwoord: weinig. Hij toonde 66 pagina's plat op `type`, met een leespaneel dat markdown niet rendert (`**sterretjes**` op het scherm) en een uitgeschakeld zoekveld. De Quartz-wiki doet dat alles beter.

**Een verouderde aanname als fundament:** `weekProgramma.ts` beweerde dat de Quartz-site *"not live yet"* was met een falende deploy — dát was het argument om een in-app lezer te bouwen. De site is wél live (handmatige deploy 13-07-2026), rendert markdown en heeft werkend zoeken.

- [x] **28.1** `stage` en `description` niet meer weggooien. ✅ Alle 70 wiki-pagina's dragen `stage` in hun frontmatter; `wiki_index.WikiPage` parste het en bewaarde het niet — terwijl de *andere* parser (`query_wiki`) het al die tijd wél teruggaf. `_SCAN_GLOBS` ongemoeid: die scan is gedeeld met `check_duplicates`.
- [x] **28.2** `build_wiki_context(chapter, stage)`. ✅ Twee secties, additief op de volledige lijst. De join bestond al aan beide kanten: `chNN`-tags in de wiki, `chapter` op `checkpoint-submission`.
- [x] **28.3** `GET /api/wiki-context`, identity-scoped; `get_wiki_page` en `/api/wiki-page` verwijderd. ✅ Dát wás de NFR-02-uitbreiding.
- [x] **28.4** `WikiBrowser.tsx` als index die doorlinkt naar Quartz. ✅ `path` (relatief, zonder `.md`) bouwt de URL — de bestandsnaam alleen volstaat niet, `concepts/` en `concepts/ai-wiki/` liggen op verschillende diepten.
- [x] **28.5** Eén source of truth voor de weekindeling. ✅ Zie hieronder.
- [x] **28.6** CSS-bug: de `h1` had nooit een `line-height`. ✅ Gemeten: 56px letters op 26,1px regelhoogte (ratio 0,47) — twee regels dwars door elkaar. Elke andere `h1` paste op één regel, dus het viel nooit op.

**28.5 — de weekindeling (SSoT).** Die stond op ~25 plekken tegelijk, met de hand overgeschreven, en was gedivergeerd: **13 van de 19 hoofdstukken spraken elkaar tegen** (wiki: Ch17 → week 13; portal: week 15), en niet met een vaste offset. De wiki citeerde bovendien LRD Deel 8 voor weekbereiken die Deel 8 niet zegt — Deel 8 heeft develop op wk14–16 met Gate 4 in wk17 en de capstone in wk18; de wiki had develop op week 13–14. Die pagina's zijn ooit uit Deel 8 geschreven; daarna kwamen week 9 en week 18 erbij en schoof alles op.

Op aanwijzing van de opdrachtgever niet gesynchroniseerd maar **gededupliceerd**: het weeknummer staat nu uitsluitend in Deel 8 en het daaruit afgeleide `weekProgramma.ts`. De wiki draagt alleen nog `stage` + `chNN`. Wat de wiki niet zegt, kan de wiki niet tegenspreken. 25 bestanden, 200 regels weg (commit `8bd32d7` in `oe-wiki`). Bewaakt door `test_wiki_no_week_numbers.py`, dat beide kanten test — geen weeknummers terug, én de join-sleutels moeten blijven.

### Test Gate — Task 28
- **Automatisch:** ✅ 434 passed / 2 skipped (was 427). `tsc -b`/`build`/`lint` schoon.
- **Mens-testbaar artefact:** ✅ echte servers + echte wiki. Zonder checkpoint: alleen de fase-sectie, met een zin die uitlegt wanneer de andere komt. Na een checkpoint op Ch04: de sectie vult zich met het Ch04-concept én de Toyota-bedrijfscase (die `ch04` als tag draagt) — precies het verband dat een alfabetische lijst van 66 begroef. Alle 9 gerenderde links opgehaald: **allemaal 200**.

**Wat deze task oplevert buiten de UX:** de NFR-02-uitbreiding is **ingetrokken, niet geaccepteerd**. Er gaat geen wiki-content meer door dit systeem. De tab werd nuttiger én de architectuur kleiner.

**Bewust uitgesteld:** echte tutor-citaten. `query_wiki` kent elk pad maar niets bewaart de lijst (TDD 5.4a); alsnog bewaren opent het `extra="forbid"`-schema waarop de nooit-cijfers-garantie rust.

### Commit & push: Task 28
- [x] Commit in `oe-wiki`: `8bd32d7`. Commits in `oe-gate-system`: `10acccc` (CSS), `07a77fd` (tab).
- [ ] Commit in `operational-excellence-course` (LRD/TDD/buildplan).
- [ ] Push alle drie.
- [ ] **Let op:** `oe-wiki` moet opnieuw deployen voordat studenten de opgeschoonde pagina's zien; die workflow faalt op elke push en slaagde alleen via een handmatige `workflow_dispatch`.

---

## Voortgangsoverzicht (samenvatting: werk bovenstaande secties bij, dit is alleen een snelle scan)

- [x] Main Task 0: Repository- en projectscaffolding ✅ https://github.com/businessdatasolutions/oe-gate-system
- [x] Main Task 1: Wiki-content-laag operationeel ✅ 70 pagina's (19 boekconcepten + 5 bedrijfscases + 23 Lean-tool-referenties + 19 AI-Wiki-contentmap-pagina's + 4 week-landingspagina's) + 4D-diagramextensie, 0 OKF-lintfouten, commit `9c133dd`
- [x] Main Task 2: Data-laag + OKF-bundelconventie ✅ tien OKF-concepttypes + zeven DuckLake-tabellen + team/ring/klas/admin/system-lakehouses, commit `c46301f`
- [x] Main Task 3: Socratische Tutor Agent (Gate 1) ✅ tweelaags task-mode worker + chat-mode coördinator, drie `@log_action`-tools (`query_wiki`/`read_team_history`/`write_report`), structurele never-scores-garantie (twee losstaande `extra="forbid"`-schema's, `rubric_score` alleen in een apart docentkanaal + `rubric_scores`-tabel), evalset (5 cases) + componenttest (12 tests, `ScriptedLlm`: geen live API-key in deze omgeving), `demo_run.py` als mens-testbaar artefact, commit `e8c5783`
- [x] Main Task 4: Review-Ring Coordinator + Instructor Dashboard ✅ deterministische ADK `Workflow` (geen LLM), ringtoewijzing/fallback/escrow-onthulling/48u-vervalvenster, `query_dashboard` (cross-lakehouse DuckDB-aggregatie), `Dashboard.tsx` (statuskaarten + override, ~19s handmatige scan-tijd), 18/18 tests incl. FR-18-bewijs op functie- én echte-workflow-niveau, commit `5b52bae`
- [x] Main Task 5: Volledige cyclus, alle vier gates ✅ Gate B eindelijk afgeleid+gepersisteerd (`evaluate_gate_b_status`) en gecombineerd met Gate A (`combine_gate_signal`, FR-05), `agents/team_api/` (structureel score-vrij contract, 8 contract-tests), `CheckpointView.tsx` (TDD 6.1-mapping) + echte app-brede routing (react-router-dom: `/team`, `/instructor/dashboard` ontsluit Task 4's Dashboard, `/admin` blijft apart), 3 AC-05-evalsetcases, `test_full_cycle_integration.py` (3 tests, échte tweelaagse agent door alle vier D-fasen), handmatige browser-doorloop (Playwright) met écht `team_api.server`: beide gates open, footer DICHT→OPEN, nul cijfers in DOM/JSON, commit `25d1261`
- [ ] Main Task 6: Kalibratie — wacht nog steeds op pilotdata (geen pilot gedraaid, dus 6.1's `threshold_analysis.py` heeft niets om tegen te rekenen)
- [~] Main Task 7: Deploymenthardening — **7.1–7.4 afgerond**, 7.5/7.6 vragen een live deploy. Scope werd tijdens de bouw groter dan gepland: 7.2's opengelaten "gemount volume of object-storage"-keuze bleek onveilig (Cloud Storage FUSE heeft geen file locking en verliest schrijfacties stilzwijgend), dus de DuckLake-catalogus verhuisde naar Postgres — een data-laag-wijziging, niet alleen infra. Zie de task zelf + TDD 10.2/10.3/14. Drie echte bugs onderweg gevonden en gerepareerd: loopback-bind in de container (Cloud Run onbereikbaar), ontbrekende Postgres-driver, arm64-image op een amd64-only platform. 335 tests groen (was 325)
- [x] Main Task 8: Wiki Quality Check Agent ✅ vier deterministische tools (extract/check_duplicates/validate_citations/compute_points) + evalset (4 cases, deterministisch geverifieerd) + `demo_submit.py` (3 scenario's handmatig gedraaid). De similariteitscheck gebruikt een TF-IDF-placeholder (embedding-pad wél gebouwd, ongebruikt zonder API-key); kalibratie volgt in Task 6/AC-13, commit `e9ba394`
- [x] Main Task 9: Admin-onderdeel ✅ CRUD voor course-period/team-roster/assessment-schedule (admin/klas-lakehouses), team-roster↔ring-fallback-integratie (echte end-to-end-test tégen Task 4's eigen module, niet een mock), FR-20-uploadroutering (team/ring/klas via de data-laag; "iedereen" via een gedocumenteerd-maar-nog-niet-uitgevoerd wiki-commit-integratiepunt, nooit stilzwijgend), vier admin-schermen incl. een zware "iedereen"-bevestigingsdialoog (LRD 6.10), 36+18 tests (CRUD + twee tijdens de build gevonden-en-gerepareerde beveiligingsfixes: path traversal, CORS-wildcard), commit `c18c28f`
- [x] Main Task 10: Studentdossier + AI-CBI-voorbereiding ✅ drie nieuwe tools op de bestaande Dashboard Query Agent (`build_student_timeline` met echte DuckLake-snapshot-tijdreizen, `generate_prep_summary` met een onvoorwaardelijk in-tool `source_ref`-filter, `export_student_history`→PDF via `fpdf2`), structurele never-cijfer-garantie (`extra="forbid"`, verplicht non-empty `source_ref`, bewezen met een adversarial hallucinatie-test), instructor-only-isolatie bewezen via echte AST-importanalyse (niet een substring-scan), 38 tests + evalset (5 cases uit een échte pipeline-run, 100% source_ref-dekking + een test die de checker zelf op een ongeldige case laat falen), zelfstandige `StudentDossier.tsx` met klikbare/springende citatielinks, commit `253b1be`
- [x] Main Task 11: HAN Huisstijl-ontwerpsysteem toepassen ✅ `han-huisstijl-tokens.css` (enige plek met letterlijke hexwaarden, incl. gelabelde extrapolaties voor niet-in-de-mockup-statussen), alle bestaande stylesheets herwezen (index.css/Dashboard.css/CheckpointView.css/StudentDossier.css/admin.css: één echte grijs-op-grijs-inputbug gevonden en gerepareerd tijdens de screenshot-verificatie), drie nieuwe gedeelde componenten (`HanHeader`: eigen instantie per scherm, want admin.html is een apart Vite-bundle; `PhaseTracker`: één component met een `scope`-prop, behoudt CheckpointView's bestaande fase-navigatie; `NoGradeFooter`: mockup's exacte tekst als default, echte servervelden als props), CheckpointView.tsx het diepst geherstructureerd (mockup's eigen twee-rijen-kaartlayout, aparte Gate A/B-kaarten), `pytest` ongewijzigd 198/1-geskipt, `npm run build`/`npm run lint` schoon, Playwright-screenshots van alle vier routes + 2 extra CheckpointView-states + 2 admin-tabs naast de daadwerkelijke mockup-HTML (zelfde browser, eerlijke vergelijking), commit `e792608`

**Scope-uitbreiding: volledige hybride course website (zie plan `can-we-start-building-compiled-narwhal.md`):**
- [x] Main Task 12: LRD-uitbreiding ✅ Deel 6.14–6.16 (onboarding week 0, hybride 18-weekse periode-indeling met vorm-kolom, kwaliteitsmanagement-survey + AI-adviseur), FR-25–31/NFR-11–12/AC-15–16, docent-akkoord ontvangen
- [x] Main Task 13: TDD-uitbreiding ✅ Deel 5.9 (Course Quality Advisor Agent), Deel 6.7–6.11 (nieuwe frontend-oppervlakken), Deel 3.7/4.10 (wiki-analytics + rollup), FR-25–31/NFR-11–12/AC-15–16 volledig kruisverwezen, opdrachtgever-akkoord ontvangen
- [x] Main Task 14: Sessie- & planningslaag ✅ `Session`-OKF-concept (klas-lakehouse, singleton) + `attendance_log`-DuckLake-tabel, admin-CRUD, `Sessions.tsx` (docent) + `Schedule.tsx` (student, `/team/:teamId/schedule`), 17 nieuwe tests, commits `898ccde` (oe-gate-system) / `6fc4c9d` (buildplan)
- [x] Main Task 15: Aanwezigheid & in-sessie tools ✅ check-in + in-sessie-polls, commit `cf26247` (gepusht)
- [x] Main Task 16: Onboarding-flow ✅ `onboarding_completed`-vlag op bestaande `TeamRoster`, `TeamRosterView`-schema (`extra="forbid"`), vier schermen (Welcome/TeamReveal/SelfAssessment/ToolingWalkthrough), zelfassessment client-only (geen server-schema, nooit verzonden), 9 nieuwe tests, commits `598d33f` + `773279e` (axis-correctie) (oe-gate-system) / `68df223` (buildplan) — zie Main Task 21 voor een latere, bredere inhoudelijke verrijking van deze flow
- [ ] Main Task 17: Student journey-view — vervangen door Main Task 23 (samengevoegd met Rooster/Onderwijsprogramma, zie navigatie-herziening)
- [x] Main Task 18: Survey/QMS-laag ✅ post-sessie-kwaliteitssurvey (FR-29, LRD 6.16, TDD 5.9/6.9), commit `3a32eb8` (gepusht)
- [x] Main Task 19: Wiki-gebruiksanalyse ✅ GoatCounter-config-wiring in `oe-wiki` (privacy-only, geen verzonnen site code), `data/smdl/engagement_rollup.py` + `wiki_engagement`-DuckLake-tabel + `Dashboard.tsx`-badge in `oe-gate-system`, 9/9 nieuwe tests, commits `65c453b` (oe-wiki) / `a03704e` (oe-gate-system)
- [x] Main Task 20: Course Quality Advisor Agent ✅ FR-31, TDD 5.9, LRD 6.16, commit `fd13561` (gepusht)
- [ ] Main Task 21: Onboarding-verrijking (WHY/HOW/WHAT, praktische startinfo, geruststelling)

**Scope-uitbreiding: navigatie-herziening (studenten/docenten meldden verdwijnende navigatie, geen onboarding-terugknop, geen programma-overzicht):**
- [x] Main Task 22: Navigatiestructuur & rolgescoped views ✅ `nav-config.ts` (zone/label/match-predicaat, single source of truth) + `AppShell.tsx` (layout-route, `NavLink`, zone-filter + zone-wisselaar) + vijf echte `/admin/*`-routes (`admin.html`/`routes/admin/main.tsx` verwijderd, `AdminApp.tsx` nu zelf een layout-route) + terugknoppen/klikbare stapindicator in de onboarding-sequentie + `HanHeader`-contextName overal via `useNavContext()`, `pytest` ongewijzigd 237/1-geskipt, `tsc`/`build`/`lint` schoon, Playwright-walkthrough (admin-bereikbaarheid, onboarding heen/terug, zone-scoping) tegen een echte `npm run dev`-server + de draaiende `team_api`/`admin_api`-servers
- [x] Main Task 23: Onderwijsprogramma (samenvoeging Rooster + Journey-view) ✅ `Programma.tsx` (`/team/:teamId/programma`, vervangt Schedule.tsx) + `build_cohort_positions` (structureel `team_id`+`stage`-only cross-team-projectie, NFR-14, nooit `rubric_scores`/`override_log` aangeraakt) + statische 18-weektabel (LRD Deel 8) met wiki-links + optionele sessiedata (FR-26 al verenigbaar, niets te versoepelen) + eigen CBI-datum student-zijdig. 12 nieuwe tests (249/1-geskipt, was 237/1), `tsc`/`build`/`lint` schoon, Playwright-walkthrough tegen echte draaiende servers + een tijdens die walkthrough gevonden-en-gerepareerde DuckLake-concurrency-bug (serialiserende lock in `admin_api`/`team_api`), commit `7621091`
- [x] Main Task 24: Studentportaal — vijf kernviews ✅ code gebouwd en geverifieerd (`wiki_query`-service + `WikiBrowser.tsx`, cross-gate `TutorHistory.tsx`, `ReviewRingStatus.tsx`, Onboarding bevestigd als bestaande `nav-config.ts`-entry), 277/1 (was 249/1), `tsc`/`build`/`lint` schoon, Playwright-walkthrough incl. volledige happy path (checkpoint → Gate A → Gate B → escrow-onthulling) live over alle vijf tabs, nul regressies — gecommit en gepusht in `b7d09d9` (Main Tasks 24–26 samen)
- [x] Main Task 25: Klas-scoping — peers op Onderwijsprogramma & docentoverzicht per klas ✅ code gebouwd en geverifieerd (`build_cohort_positions` vereist nu `klas_id`, `TeamDashboardRow.klas_id` + `Dashboard.tsx`-klas-secties), 281/1 (was 277/1), `tsc`/`build`/`lint` schoon, Playwright-walkthrough met twee geseede klassen bevestigt klas-isolatie op Onderwijsprogramma en klas-groepering op het docentdashboard — gecommit en gepusht in `b7d09d9` (Main Tasks 24–26 samen)
- [x] Main Task 26: Socratische vragen in het studentdossier (CBI-koppeling) ✅ code gebouwd en geverifieerd (`prep_agent._socratic_events` als 4e tijdlijnbron uit het scorevrije teamkanaal, `/api/student-timeline`+`/api/prep-summary` gewired op `admin_api`, `StudentDossier.tsx`-socratic-eventtype + fetches naar `admin_api`, `Dashboard.tsx` "Open dossier"-link, dossier-demo met Socratisch rapport), LRD/TDD bijgewerkt (FR-23/AC-19, Deel 5.8/6.6), 290/1 (was 281/1), `tsc`/`build`/`lint` schoon, Playwright-walkthrough bevestigt echte data (mock-badge weg), Socratische vragen zichtbaar met bron, CBI-punt citeert die bron, nergens een score — gecommit en gepusht in `b7d09d9` (Main Tasks 24–26 samen)

**Scope-uitbreiding: toegang & contextuele wiki (het systeem had geen authenticatie; de OE-Wiki-tab dupliceerde een betere site):**
- [x] Main Task 27: Toegang — één persoonlijke link per student en per docent ✅ `shared/identity.py` + `access_grants` (alleen `sha256(link)`, nooit platte tekst) + handhaving in beide servers (studentkant leidt `team_id` áf i.p.v. het te geloven; docentkant valideert de klas-claim centraal), credential in het URL-fragment (beide servers loggen de volledige requestregel), roster-import met `docentvoornaam` (puntkomma-CSV, preview-dan-commit) + printvel met QR per klas (POST, want uitgeven vervangt de vorige links), klas-gescoped `/api/dashboard` (bestond nog niet: `Dashboard.tsx` kreeg sinds Task 4 altijd 404 en toonde verzonnen teams) + `instructor_id` op `override_log`. 427/2 (was 335/2), tegenproeven gedraaid op elke load-bearing test. Walkthrough vond vier bugs die alle unittests groen doorstonden — waaronder een echte privacylek: `listAssessments` stuurde het hele klasrooster naar elke browser en filterde client-side. Commits `d40dd05`…`4542ae9`, **nog niet gepusht**. LRD 6.21/FR-42–45/NFR-16/AC-20–21, TDD 9.5
- [x] Main Task 28: OE-Wiki-tab — van tweederangs lezer naar contextuele index ✅ de tab toonde 66 pagina's plat op `type`, met een leespaneel dat markdown niet rendert en een uitgeschakeld zoekveld — alles wat de live Quartz-wiki beter doet. Nu een index rond `chapter` (checkpoint) + `stage` (fase), met lezen op Quartz zelf. Gevolg voor de architectuur: **de NFR-02-uitbreiding is ingetrokken, niet geaccepteerd** — er gaat geen wiki-content meer door dit systeem. Inclusief de weekindeling-SSoT (13/19 hoofdstukken spraken elkaar tegen; de wiki citeerde LRD Deel 8 verkeerd): weeknummers staan nu uitsluitend in Deel 8 + `weekProgramma.ts`, de wiki draagt alleen `stage` + `chNN`. 434/2 (was 427/2). Commits `8bd32d7` (oe-wiki), `10acccc` + `07a77fd` (oe-gate-system), **nog niet gepusht**; `oe-wiki` vraagt een handmatige `workflow_dispatch`-deploy. LRD FR-37/6.19, TDD 5.4a/5.10/6.14

**Traceerbaarheid:** elke main task citeert de FR/NFR/AC-nummers die ze dekt (zie de commit-boodschap-eisen per task). Voor de volledige matrix, zie TDD Deel 8.

**Correctie (13 juli 2026): design-documentverwijzingen verwijderd uit gebruikersgerichte UI-tekst.** De opdrachtgever wees erop dat citaten als "(LRD 6.12)"/"(FR-07)"/"(TDD 4.4)" in zichtbare schermtekst niet thuishoren: het systeem is de uitkomst van de designdocumenten, gebruikers (studenten én docenten) hoeven niet te weten hoe het is ontworpen. Een volledige sweep van `frontend/src` vond dit patroon terug in negen bestanden, verspreid over Tasks 4/5/9/10/14/16/21 (niet alleen de net-gebouwde onboarding): `TeamReveal.tsx`, `AdminApp.tsx`, `Sessions.tsx`, `Assessments.tsx`, `Teams.tsx`, `CoursePeriod.tsx`, `DocumentUpload.tsx`, `Schedule.tsx`, `CheckpointView.tsx`, `Dashboard.tsx`, `StudentDossier.tsx`. Alle citaten uit zichtbare tekst (`<p>`, `<dd>`, `<summary>`, `title=`-tooltips) verwijderd, ontwikkelaarsgerichte code-comments en `console.warn`-logs bewust ongemoeid gelaten (die zijn juist wél voor developers). `tsc`/lint/build/pytest (232/1) ongewijzigd groen ná de correctie.

**Bugfix (13 juli 2026): FR-09 PDF-export downloadde een onopenbaar bestand.** Opdrachtgever-gemeld tijdens het testen. Root cause: `StudentDossier.tsx`'s exportknop deed een relatieve `fetch('/api/export-student-history')` naar een route die op geen enkele backend-server geregistreerd stond; zonder Vite-proxy viel dit terug op Vite's eigen SPA-fallback `index.html` (HTTP 200) — en waar de twee andere dossier-fetches dit met een `res.json()`-parsefout netjes opvangen (val terug op mockdata), accepteert `res.blob()` elke response zonder klagen, dus werd de HTML-pagina als `.pdf` gedownload. De onderliggende `export_student_history()`-functie zelf bleek prima een geldige PDF te produceren (geverifieerd met `pypdf`) — het ontbrekende stuk was uitsluitend de HTTP-laag. Opgelost: een echte `GET /api/export-student-history`-route toegevoegd aan `admin_api/server.py` (`h_export_student_history`, via een nieuwe `BINARY_ROUTES`-tabel naast de bestaande JSON-only `ROUTES`-dispatch), frontend omgezet naar het absolute-URL-patroon dat de rest van de admin-laag al gebruikt plus een expliciete `content-type`-check (faalt nu hard i.p.v. stil een verkeerd bestand te downloaden, mocht dit ooit opnieuw breken). Terzelfdertijd dezelfde designdocument-citaten (`(FR-09)`, `(LRD Deel 9)`) ook uit de PDF-inhoud zelf verwijderd (`prep_agent.py::_pdf_bytes`) — zelfde categorie fout, nu in gegenereerde PDF-tekst i.p.v. schermtekst. Vijf nieuwe regressietests (`agents/tests/test_admin_server.py`), volledige suite 237/1 (was 232/1), zelf een echte download via Playwright gereproduceerd vóór en ná de fix en de PDF-inhoud met `pypdf` geverifieerd (11 gebeurtenissen, team-07/Fenna, geen citaten meer).
